# erd 스킬 파이썬 의존성
#   pip3 install -r requirements.txt
#
# 이 둘이 전부다. psql(또는 docker)과 한글 폰트는 pip 밖의 준비물이라
# install.sh 가 따로 확인한다.
#
# 아래 하한은 **재는 숫자다.** `install.sh --check` 가 이 파일을 읽어
# importlib.metadata 의 설치 버전과 대조하고, 낮으면 실패로 친다. 예전에는
# `import docx` 만 봐서 python-docx 0.8.11 에서도 초록불이었다 — 그때 이 숫자는
# 아무도 안 재는 숫자였다. 하한을 고치면 그날로 점검의 판정이 바뀐다.

python-docx>=1.1.0     # docx 문서 생성 (import 이름은 docx)
pillow>=10.0.0         # PNG 렌더링
