# 검토 기록

다국어화 작업 뒤에 열세 라운드를 돌렸다. 열한 번(1~10·12)은 독립 검토였고, 11·13
라운드는 검토가 아니라 넷으로 나눠 한꺼번에 고친 수정 라운드다. 매 라운드 다른 검토자가 **직전
라운드와 다른 관점**을 파고, 나온 것을 고치고, 다시 새 검토자를 붙였다. 12라운드부터는
검토자가 **직전 커밋의 주장이 참인지도 함께 판정한다.** 이 문서는 그 기록이다.

읽는 사람이 이 코드를 고칠 사람이라 한국어로만 둔다. 사용자가 읽는 문서는
[README.md](README.md) 쪽이다.

남기는 이유는 두 가지다. **어떤 부류의 실수가 났는지** 알면 다음에 같은 자리를 먼저
보게 되고, **무엇이 조용히 깨졌는지** 알면 회귀 시험에 무엇을 남겨야 하는지 알 수 있다.

---

## 라운드별

| # | 검토 관점 | 심각한 발견 | 직전 수정이 만든 것 |
|---|---|---|---|
| 1 | 번역 품질 (en·ko·ja·es 각각) | 스페인어에서 `word.kind`·`col.type` 이 둘 다 `Tipo` → docx 컬럼표 머리가 `Tipo\|Columna\|Tipo`. `ERD_MAX_AREAS` 기본값이 문서엔 7, 코드엔 12 | — |
| 2 | i18n 리팩터 회귀 · 문서-코드 대조 | 추가 컬럼 강조가 한국어에서만 동작 (마커를 **만드는** 쪽만 카탈로그로 바꾸고 **비교하는** 쪽은 `startswith('[추가]')` 로 남음). `--check` 가 정작 `SKILL.md` 를 점검하지 않음 | 라틴 전용 폰트를 폴백에 넣어, 한글 폰트 없는 환경에서 **명확한 실패**가 **조용한 두부(□)** 로 바뀜 |
| 3 | 실제 PostgreSQL 로 처음 실행 | 같은 이름 테이블이 두 스키마에 있으면 **하나로 합쳐짐** (컬럼 뒤섞임, PK `['id','id']`, 한쪽은 그림에서 소멸). 복합 FK 가 곱으로 불어나 **없는 관계**가 문서에 실림 | — |
| 4 | 적대적 입력 | 설명에 **개행 하나**면 모든 다이어그램이 죽음. 제어문자가 GraphML·SVG 를 XML 로서 깨뜨려 yEd 가 파일 거부. spec 오타 → raw traceback | `{key}` 자리표시자가 `t(key, **kw)` 와 충돌 |
| 5 | 렌더링·레이아웃 로직 | **선이 테이블을 관통** — 문서가 타협 없는 규칙이라 내건 것이 밀집 스키마에서 깨졌고, 검증에 **그 항목 자체가 없어** 0·0·0 이 찍힘. 선을 노드보다 먼저 그려 눈에도 안 보임 | — |
| 6 | 결정성 · 재생성 · DDL 경로 첫 접촉 | `pg_dump` 출력이 **테이블 0개**로 파싱됨 (`CREATE TABLE public.x`·`IF NOT EXISTS` 를 못 잡음). 인라인 `REFERENCES` 미인식으로 관계 전멸 | — |
| 7 | 파서 마스킹 공격 | `DEFAULT '('` 하나에 **다음 컬럼이 사라짐**. 문자열 안의 `--` 에 그 뒤 컬럼이 사라짐. `$$` 함수 본문의 `CREATE TABLE` 이 유령 테이블 | 앞 라운드 재작성이 만든 것 — 낱말 경계 없는 키워드 필터로 `checksum`·`like_count` 등이 소실, ALTER 가 스키마를 버려 **FK 가 남의 테이블에** 붙음 |
| 8 | Postgres 기능 · 권한 · 버전 | DB 경로가 **유니크 인덱스를 참조하는 FK 를 통째로 누락** (이번엔 DB 쪽이 틀림) | 마스크를 셋으로 나누며 **인라인 주석이 전부 빈 문자열**이 됨. 0컬럼 테이블에서 `max()` 크래시 |
| 9 | FK 쿼리 집중 공격 | 부모가 안 보이는 스키마면 **같은 이름의 보이는 테이블로 연결** — 존재하지 않는 컬럼을 가리키는 관계가 그려짐. 값 속 개행이 psql 행 구분을 깨뜨려 **유령 테이블이 문서 첫 장**이 됨 | 주석 경계를 줄 끝까지로 넓혀 한 줄 정의가 다시 깨짐. `REFERENCES` 뒤에 적은 `NOT NULL` 무시 |
| 10 | "수정이 다음 버그를 만든다" 를 끊겠다던 그 커밋 자체 · 첫 실행이 아닌 **그다음** 실행 | **회귀 시험이 무장 해제돼 있었다** — 검증 줄에서 숫자를 긁던 정규식이 마지막 항목을 *0 이 아닐 때만* 놓쳐 `가로선 중첩 44` 가 통과. 컬럼 **위**에 적은 주석이 한 칸씩 밀려 DDL 설명이 전부 남의 컬럼 것이 됨. PostgreSQL 10 이하는 `conparentid` 가 없어 **FK 가 전멸**하는데 `FK 0` 을 사실인 양 출력. 유령 행은 사라진 게 아니라 **자리를 옮겼을 뿐** — 값 속 `\x1e` 로 그대로 재현. 여기에 여덟 라운드가 **새 DB 의 첫 실행만** 봐서 못 본 넷: 새 글 옆에 지난 판 그림, `MANUAL` 이 도구가 인쇄해 준 바로 그 키로는 안 걸림, 일곱 조회 중 하나가 죽어도 완성본처럼 나옴, `parse_ddl` 의 라이브 조회 경로는 아직 개행으로 행을 가름 | 앞의 넷이 전부 9라운드 수정 것이다. 사람 보라고 붙인 `(허용)`·`[경고]` 꼬리가 시험의 눈을 감겼고, 주석 경계 재수정이 반대쪽을 깨뜨렸고, 파티션 중복을 거르려 넣은 `conparentid` 가 구버전을 죽였고, 개행 대신 `\x1e` 로 바꾼 구분자가 같은 버그를 다시 만들었다 |
| 11 | 검토가 아니라 **수정** — 네 갈래로 나눠 넷이 동시에 (파일 묶음 하나씩) | 고치다 넷이 더 나왔다. 끝점을 하나라도 공유하면 합류로 봐줘서 **같은 선을 두 번 그린 것**이 안 잡혔다. 개요도는 라벨을 아예 안 그리는데 라벨 겹침을 `0` 으로 보고했다 — **안 한 검사를 통과로 적었다**. 스물다섯 도판 중 열일곱이 첫 실행부터 `[경고]` 를 달아, 그대로 두면 경고가 회귀 신호가 아니라 소음이 된다. 시험이 부르는 사람의 `ERD_*` 를 물려받아 `ERD_LABEL` 을 켜 둔 셸에서 `--check` 가 멀쩡한 설치를 고장 났다고 알렸다 | `[경고]` 표시를 만든 것이 9라운드라 소음도 그때부터다. 환경 상속은 selftest 를 처음 붙인 커밋 것 — 지운 것이 `ERD_DB`·`ERD_PSQL` 둘뿐이었다 |
| 12 | 버그 찾기에 더해 **직전 커밋의 주장이 참인지 판정** | 열 주장 중 여섯만 참, 셋은 과장, 하나는 거짓. 거짓은 "무작위 200개 스키마에서 경고 0" — 다시 재니 120개 중 19개가 경고했고 **열아홉 개 전부 자기참조를 담고 있었다**. 세는 실수가 아니라 52px 나란히 그려진 진짜 잉크다. 그런데 목록에 없던 것이 더 컸다: **회귀 시험이 반증 불가능했다.** `verify_clean()` 이 아무것도 다시 세지 않고 그림 그린 쪽이 적어 준 `warn` 한 줄을 믿었다 — 모든 항목을 `해당 없음` 으로 만들면 57개 중 3개만 빨강, `warn` 만 비우면 0개, `tolerate` 를 넓히고 진짜 회귀를 넣으면 **0개 · 57개 전부 통과**. 여덟 케이스는 아무것도 재고 있지 않았다 — `<node>` 0개인 GraphML 이 통과하고, 그림 0장에 "그림이 페이지에 맞는다" 가 통과하고, 단정 하나는 무조건 거짓이었다. 과장 둘도 남긴다: "값이 담을 수 있는 어떤 바이트도 구분자로 읽히지 않는다" 는 `introspect.py` 에만 참이고 `parse_ddl.py` 의 라이브 조회 경로에는 거짓, "합류는 끝점을 정확히 하나만 공유해야 한다" 는 코드가 하지 않는 규칙이었다. 그리고 새 자리 하나 — **재는 장치 자체가 실패하면**: `ERD_VERIFY_LOG` 가 쓸 수 없는 경로면 도판을 "검증했다" 고 찍고 나서 죽어 `out/` 이 비었다 | 셋 다 11라운드 것이다. 경로 계산을 두 단계로 나눈 그 수정을 통로 차선에만 적용하고 자기참조에는 안 해서 겹침이 남았는데, "경고 0" 은 바로 그 수정이 내건 자랑이었다. 시험이 읽는 JSONL 도 `verify_clean()` 도 `ERD_VERIFY_LOG` 도 전부 그때 새로 넣은 것이다 — **재는 것을 늘린 커밋이 재는 눈을 감기고 산출물까지 볼모로 잡았다** |
| 13 | 다시 **수정** — 넷이 파일 묶음 하나씩 나눠 동시에, 여기에 쓰지 않고 검증만 한 하나 | 검사자를 다시 세워 '깨끗하다' 의 뜻을 시험이 갖게 했다: 다섯 카운터가 전부 정수이고 전부 0, `해당 없음` 은 라벨을 그리지 않는 개요도의 두 항목에서만, 봐주기는 코드가 주는 것이 아니라 케이스가 숫자로 적는다. 묻는 케이스만 보던 것도 고쳤다 — 스무 개가 그림을 그리는데 셋만 묻고 있었으므로, 케이스가 통과할 때마다 그 케이스가 남긴 기록을 전부 훑는다. 자기참조 겹침을 두 자리에서(팔 높이가 진입 y 보다 먼저 정해지던 것, 팔의 x 가 등록만 되고 검사되지 않던 것), 블록 주석 누출, `ERD_VERIFY_LOG` 가 도판을 못 죽이게, 이름에도 `clean()`, `ERD_MAX_AREAS` 를 문서 전체 기준으로, 범례 폭 측정, 따옴표 친 식별자, 그림 하나 없다고 죽던 docx. 아무것도 안 재던 케이스 여덟을 고치고 못 고칠 둘은 한계를 케이스 안에 적었다. `selftest_history.py` — 열한 라운드에서 소급해 만든 36개, 전부 mutation 으로 확인. 진짜 회귀를 넣고 그린 쪽이 `warn` 을 숨기면 예전엔 57개가 전부 통과, 지금은 **59개 중 21개가 빨강** (커밋이 적은 수치다 — 어떤 회귀를 넣었는지는 안 적혀 있어 그대로는 재현되지 않는다. 아래 "커밋 메시지의 주장은 증거가 아니다" 참고) | 12라운드는 검토라 코드를 안 냈으니 이 칸은 **이번 수정이 만든 것**을 적는다. `selftest_history.py` 36개가 어디에도 등록되지 않았다 — 파일이 스스로 "저쪽에서 이 모듈을 한 줄 import 하면 두 파일이 한 벌로 돈다" 고 적어 두었는데 그 한 줄이 없다. `install.sh --check` 가 도는 것은 여전히 59개뿐이고, 36개는 손으로 불러야 돈다. **재는 것을 늘린 커밋이 늘린 것을 아무도 안 돌리는 자리에 두었다** — 12라운드가 짚은 모양 그대로다. 봐주기를 케이스로 옮긴 자리(`RENDER_ALLOW` 의 가로선 중첩 5)도 그 숫자만큼은 회귀가 숨을 수 있는 곳이라 늘릴 때마다 이유를 적어야 한다 |

각 라운드의 발견은 그 라운드 직후 커밋에 반영돼 있다. `git log --oneline` 의 메시지가
무엇을 왜 고쳤는지 적고 있다.

한 가지 걸리는 것이 있다. 커밋 메시지가 말하는 "N 번째 검토" 는 이 표의 번호보다
하나 작다 (`79ff684` 는 "여덟 번째 검토" 라고 적었지만 표에서는 9라운드다). 왜
어긋났는지는 기록에서 확인되지 않는다. 표 쪽 번호를 정본으로 두고, 커밋을 읽을 때는
하나 더한다. 다만 `f84bfaf` 부터는 **둘** 더해야 한다 — 이 커밋이 12라운드를 "열 번째
검토" 라고 부른다. 11·13 처럼 검토가 아니라 수정인 라운드가 커밋 쪽 셈에서 빠지는
것으로 보이지만, 이것도 확인된 것은 아니다. 차이가 벌어질 수 있다는 것만 알아 두면 된다.

13라운드에서 넷 중 하나는 **쓰지 않고 멈췄다.** 제 몫의 파일을 다른 하나가 이미
만지고 있는 것을 보고, 덮어쓰는 대신 검증으로 돌아섰다. 그 판단이 이 라운드에서 가장
값나갔다 — 그쪽이 낸 것("경고 0" 을 반증한 퍼저, 아무것도 등록하지 않는 등록 줄)이
이번 라운드 산출 중 제일 쓸모 있었다. 동시에 고칠 때 겹치면 멈추는 편이 낫다.

---

## 되풀이된 것

**수정이 다음 버그를 만들었다.** 2·4·7·8·9·10·11·12 라운드의 심각한 발견 중 상당수가
직전 라운드의 수정에서 나왔다. 7→8→9→10 은 네 번 연속이다. 부분 패치가 계속 새서 파서
앞단을 다시 쓴 판단은 맞았지만, 다시 쓸 때마다 이전 판이 하던 일을 하나씩 떨어뜨렸다.
10라운드는 이 패턴을 **끊겠다고 쓴 커밋**을 감사했는데, 그 커밋이 넷을 새로 만들었다.
의도로는 못 끊는다는 뜻이다.

12라운드에서는 심각한 발견 **셋이 전부** 직전 수정 것이었다. 그런데 모양이 달라졌다 —
그 셋이 다 **검증을 늘린 자리**에서 나왔다. 겹침을 줄이려 만든 두 단계 경로 계산이
자기참조를 빼먹었고, 새로 넣은 검증 기록이 시험의 판단을 대신했고, 그 기록을 남기지
못하면 그림까지 사라졌다. 고치는 손보다 **재는 손이 만든 것**을 먼저 보게 된다.

**같은 버그를 반만 고쳤다.** 이것이 가장 자주 나온 모양이다. 한 부류를 고치면서 같은
부류의 나머지 절반을 남겼고, 남긴 절반이 다음 라운드의 심각한 발견이 됐다.

- ALTER·COMMENT 의 스키마 귀속을 고치면서 `CREATE UNIQUE INDEX` 와 스키마 붙은
  `ALTER` 를 빠뜨렸다
- 값 속 개행이 행을 쪼개는 것을 막으면서 `\x1e`·`\x1f` 를 남겼다. 구분자를 바꾸는
  방식이라 **다음 바이트가 똑같이 남아 있었다** — `|` → 개행 → `\x1e` 로 세 번
  되풀이됐고, 전송을 `row_to_json` 으로 바꿔 원리적으로 막고서야 끝났다
- 인라인 주석(`id int,  -- 설명`)의 자리를 고치면서 컬럼 **위**에 적는 주석을
  깨뜨렸다. 두 수정이 서로 자리를 바꿨을 뿐이다 — 쉼표를 기준으로 삼는 한 둘은
  같은 쪽에 있으면서 반대를 뜻하므로 갈릴 수가 없었다. 기준을 줄로 바꿔 끝났다
- FK 를 pg_catalog 로 옮기며 파티션 중복은 걸렀지만, 그 술어가 PG 11 부터 있는
  컬럼이라 10 이하가 통째로 죽었다

고칠 때 물어야 할 것 두 가지다. **같은 부류가 어디에 또 있는가**, 그리고 **이 수정이
경계를 옮기기만 하는가 없애는가.**

**검증이 없는 기능은 조용히 죽는다.** 4라운드에서 넣은 `선↔테이블` 이 5라운드 수정의
결함을 잡았고, 5라운드에서 넣은 `라벨↔라벨` 이 그다음 수정을 잡았다. 반대로 인라인
주석은 재는 것이 없어서 **두 판을 통째로 죽은 채** 지나갔다 — `pg_dump` 는 `COMMENT ON`
을 쓰므로 그쪽 시험만 통과하고 있었다.

**재는 장치도 자기가 재는 실패를 면제받지 않는다.** 10라운드에 처음 이름을 붙였고
("재는 쪽도 조용히 죽는다"), 12·13 라운드에서 다른 모양으로 두 번 더 나왔다. 뿌리는
하나다 — **재는 쪽은 아무도 재 주지 않는다.**

- 10라운드: 시험이 검증의 사람용 줄을 정규식으로 긁고 있었다. `(허용)`·`[경고]` 꼬리가
  붙자 마지막 항목을 놓쳤는데 하필 **0 이 아닐 때만** 놓쳐서 시험은 늘 초록이었다.
  `가로선 중첩 44` 가 통과했다
- 12라운드: 시험이 재는 쪽의 **판정**을 그대로 믿었다. `verify_clean()` 은 다시 세지
  않고 `warn` 목록만 봤는데, 그 목록을 만드는 것이 검사 대상인 코드다. 경고를 잠재우려
  `tolerate` 를 한 줄 넓히는 것은 다음 판이 충분히 할 법한 일이고, 그 한 줄이면 그림
  품질 보증이 통째로 조용해졌다. **검증을 늘린 그 커밋이 같은 손으로 무장을 해제했다**
- 12라운드: `ERD_VERIFY_LOG` 가 산출물을 볼모로 잡았다. 로그를 못 쓰면 그림이 한 장도
  안 나왔다. **재는 도구가 재는 대상을 망가뜨릴 수 있다**
- 13라운드: 소급해 만든 36개가 아무 데도 등록되지 않아, 기본 점검에서 한 번도 돌지
  않는다. 재는 것을 늘려 놓고 **돌지 않는 자리에 두는 것**도 같은 부류다

그래서 지금은 (a) 사람이 읽는 줄과 별도로 기계가 읽을 자리를 두고 **기록이 없으면
실패로 치고**, (b) '깨끗하다' 의 판정 규칙을 시험이 직접 가지며, (c) 봐주기는 케이스가
숫자로만 적고, (d) 계측이 실패해도 산출물은 낸다. 시험을 더할 때 물어야 할 것이 하나
더 있다. **그것이 어디서 돌아가는가** — 아무도 안 부르는 시험은 없는 시험이다.

**안 한 검사를 통과로 적었다.** 5라운드에서는 선↔테이블 항목이 아예 없는데 `0·0·0`
이 찍혔고, 11라운드에서는 라벨을 그리지도 않는 개요도가 라벨 겹침 `0` 을 찍었고,
10라운드에서는 FK 조회가 통째로 실패하고도 `FK 0` 이 찍혔다. 셋 다 같은 말이다.
**재지 않은 것과 재 봤더니 0 인 것은 다르다.** 지금은 앞의 것을 `해당 없음` 으로 적고,
조회가 실패하면 문서를 만들지 않는다.

12라운드는 같은 짓을 **시험 여덟 개**에서 찾았다. `<node>` 가 하나도 없는 GraphML 이
"테이블이 다 실렸다" 를 통과했고(남은 `<edge>` 하나가 두 이름을 다 품고 있었다),
그림이 한 장도 없는 docx 가 "그림이 페이지에 맞는다" 를 통과했고(빈 목록을 도는
반복문), 단정 하나는 조건이 늘 거짓이라 아무것도 못 잡았다. 그러니 `해당 없음` 은
검증의 출력에만 있는 문제가 아니다 — **검사 대상이 없으면 통과가 아니라 실패로 친다.**
지금은 개수와 자리를 못박고(`<node>` 수 = 테이블 수, 그림 수 = 그린 PNG 수), 잴 것이
없으면 그 사실 자체가 실패다.

**첫 실행만 봤다.** 앞선 아홉 라운드가 전부 **빈 디렉터리 · 새 DB · 한 번 실행**만 봤다.
그래서 두 번째 실행에서만 나는 것들이 통째로 남아 있었다 — 지난 판 그림이 새 글 옆에
실리고, 이전 판 문서가 있느냐에 따라 `MANUAL` 키 해석이 달라졌다. 다음 검토자는 같은
디렉터리에 두 번 돌리는 것부터 해 볼 것.

**커밋 메시지의 주장은 증거가 아니다 — 네 번 틀렸다.** 세 번은 "DDL 경로와 DB 경로가
한 건도 다르지 않다" 였고, 세 번 다 다음 검토자가 첫 시도에 반증했다. 확인한 범위
(스키마 하나, varchar 없음)를 확인하지 않은 범위까지 확장해 쓴 탓이다. 네 번째가
11라운드의 "무작위 200개 스키마에서 경고 0" 이고, 12라운드가 120개 중 19개로
반증했다. 넷 다 모양이 같다 — **좁게 재고 넓게 썼다.**

그래서 12라운드부터 검토자에게 버그 찾기와 함께 **직전 커밋의 주장을 하나씩 판정하는
일**을 맡긴다. 첫 판정에서 열 개 중 여섯만 참이었다.

같은 자를 이 기록에도 댄다. 13라운드의 주장을 따로 쓴 퍼저로 다시 재 봤다.

- 자기참조 겹침은 **진짜로 사라졌다**. 자기참조를 반씩 섞은 무작위 스키마 80개(같은
  씨앗)를 두 판에 걸었더니 수정 전은 36개가 경고했고(거의 다 `가로선 중첩`) 수정
  후는 2개다. `가로선 중첩` 경고는 한 건도 남지 않았다
- 다만 그 2개는 `라벨↔라벨 1` 이다. 즉 "경고 0" 은 여전히 **퍼저를 누가 썼느냐에
  딸린 말**이지 스킬의 성질이 아니다. 남은 것은 라벨끼리의 겹침이고, 아직 아무도
  그것이 진짜 잉크인지 세는 실수인지 확인하지 않았다 — 다음 검토자 몫이다
- "21개가 빨강" 은 **재현되지 않았다.** 어떤 회귀를 넣었는지 커밋이 적지 않아서다.
  라우터가 자리를 피하지 않게 되돌리고 그린 쪽이 `warn` 을 숨기게 해 보니 예전 시험은
  2개, 지금 시험은 4개가 빨강이었다(늘어난 둘이 새로 넣은 훑기와 줄 대조다). 방향은
  맞고 크기는 확인되지 않았다. **주장에 숫자를 적을 때는 그 숫자를 낸 조작도 같이
  적는다** — 안 그러면 다음 사람이 참인지 거짓인지를 판정할 수가 없다

**경고는 소음이 되면 죽는다.** 11라운드에서 스물다섯 도판 중 열일곱이 첫 실행부터
`[경고]` 를 달고 나왔다. 그대로 뒀으면 "경고 = 회귀" 라는 문서의 말이 한 주 만에
빈말이 된다. 주장을 낮추는 대신 세는 쪽을 고쳤다 — 라우터가 고칠 수 없는 겹침(컬럼
행에 못박힌 진출입 꼬리)은 세지 않고, 고칠 수 있는 것만 센다.

**눈으로는 못 잡는 것이 있다.** 선이 테이블을 관통해도 선을 노드보다 먼저 그리므로
노드 색에 덮여 보이지 않는다. 자기참조 루프 둘이 정확히 겹쳐도 하나로 보인다. 그림을
띄워 놓고 봐도 모른다 — 숫자로 재야 한다.

---

## 지금 있는 것

`scripts/selftest.py` — 59개 항목, DB 없이 13초. `install.sh --check` 가 이것도 함께
돌린다.

`scripts/selftest_history.py` — 36개, 10초. 열한 라운드를 소급해 만든 것이라 항목마다
어느 라운드의 무슨 결함을 지키는지 적혀 있다. 여기에 진짜 서버가 있어야 도는 6개가
더 있다 (`ERD_SELFTEST_DOCKER=1`, PG 16·10 두 벌).

두 파일에 담긴 것은 전부 **한 번이라도 조용히 깨졌던 것**이다. `selftest.py` 가
'고칠 때 함께 남긴 것' 이면 `selftest_history.py` 는 '고쳤는데 남기지 않은 것' 이다.

**주의: 36개는 아직 어디에도 등록돼 있지 않다.** `install.sh --check` 도
`python3 selftest.py` 도 이것들을 돌리지 않는다 — `python3 selftest_history.py` 로
따로 불러야 한다. 파일 스스로 "저쪽에서 한 줄 import 하면 두 파일이 한 벌로 돈다" 고
적어 두었으니, 그 한 줄이 들어가기 전까지 이 36개가 도는 자리는 사람 손뿐이다.

시험은 부르는 사람의 환경을 하나도 물려받지 않는다. `ERD_*` 를 전부 지우고 돌린다 —
`ERD_LABEL` 을 켜 둔 셸에서 `--check` 가 멀쩡한 설치를 고장 났다고 알린 적이 있다.
시험은 부르는 사람의 설정이 아니라 코드를 재야 한다.

렌더링은 매번 자체 검증이 찍힌다. 봐준 것은 `(허용)` 로 표시하고, 잴 수 없는 항목은
`해당 없음` 으로 적고, 0 이어야 하는데 아닌 것이 있으면 같은 줄에 `[경고]` 가 붙는다.

시험은 그 줄을 읽지 않는다. `ERD_VERIFY_LOG` 를 켜면 `erd.py` 가 도판마다 JSONL 한
줄을 남기고, 시험은 그것을 읽는다. **기록이 없으면 통과가 아니라 실패다.** 사람이
읽는 줄은 사람 좋으라고 서식이 바뀌고, 그 서식 변화가 실제로 시험의 눈을 감겼다.
그 파일은 판마다 처음부터 다시 쓰이므로 언제 열어도 이번 판의 기록만 들어 있고, 쓸 수
없는 자리여도 그림은 전부 나온다 — 계측이 산출물을 볼모로 잡지 않는다.

**'깨끗하다' 의 뜻은 시험이 가진다.** 기록을 그대로 믿지 않고 규칙을 시험이 적용한다.
다섯 카운터(`label_table`·`label_x`·`thru`·`v_overlap`·`h_overlap`)가 전부 정수이고
전부 0 이어야 한다. `해당 없음` 은 라벨을 그리지 않는 개요도의 라벨 두 항목에서만
정직한 값이고 그 밖에서는 실패다 — **재기를 멈춘 검사는 깨끗한 검사가 아니다.** 항목이
이름째 빠져도 실패고(남은 것이 전부 0 이라 조용히 통과한다), 코드가 시험이 모르는
항목을 새로 재도 실패다(아무도 안 보는 숫자가 된다). 봐주기는 코드가 넘기는 `tolerate`
가 아니라 케이스가 `RENDER_ALLOW` 에 숫자로 적는다 — **코드를 고쳐서는 늘릴 수 없는
자리**에 둔 것이 요점이다. 그리고 케이스가 물어봐 주기를 기다리지 않는다. 스무 개가
그림을 그리는데 셋만 묻고 있었으므로, 케이스가 통과할 때마다 그 케이스가 남긴 기록을
전부 훑는다.

**무언가를 고쳤으면 그것을 잡아 줬을 항목을 하나 남긴다.** 이 목록이 계속 값을 하려면
그 습관이 필요하다. 되돌렸을 때 빨간불이 뜨는지까지 확인하고 남긴다 — 안 그러면
항목은 늘어나는데 재는 것은 늘지 않는다.

---

## 이 문서를 어떻게 유지하는가

**라운드를 돌린 사람이, 그 라운드가 끝나는 자리에서, 다음 라운드가 시작되기 전에
쓴다.** 커밋과 함께 낸다.

나중에 몰아 쓰면 안 되는 이유가 있다. 이 문서는 **같은 부류의 실수를 일찍 알아보라고**
있다. 10라운드의 발견 여덟 개 중 넷이 9라운드 수정이 만든 것이었는데, 9라운드가 자기
수정을 "구분자를 바꿔 개행에 안전해졌다" 라고만 적어 두는 바람에 그것이 *경계를 옮긴
것*인지 *없앤 것*인지는 아무 데도 남지 않았다. 그 한 줄이 있었으면 `\x1e` 는 10라운드
발견이 아니라 9라운드 안에서 끝났을 것이다. 뒤늦게 쓰면 무엇이 나왔는지는 적을 수
있어도 **무엇을 놓칠 뻔했는지**는 이미 사라진 뒤다.

한 라운드가 끝나면 세 자리를 채운다.

1. 표에 한 줄. `직전 수정이 만든 것` 칸을 정직하게 쓴다 — 이 표에서 가장 값나가는
   칸이고, 제 손으로 만든 것을 제 손으로 적는 칸이다. 비워 두고 싶으면 그때가 바로
   한 번 더 볼 때다.
2. `되풀이된 것` 에 이번 것이 이미 있는 항목의 반복인지 본다. 반복이면 사례를 붙이고,
   새 모양이면 이름을 붙여 항목을 만든다. 세 번 이상 나온 것은 굵게 이름을 준다.
3. `지금 있는 것` 의 숫자와 설명을 실제와 맞춘다. 시험 개수, 검증이 재는 것,
   검증 결과를 읽는 경로.

문서와 코드가 어긋나면 문서가 틀린 것이다. 고치고 넘어간다.
