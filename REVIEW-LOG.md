# 검토 기록

다국어화 작업 뒤에 **열여섯 라운드**를 돌렸다. 열한 번(1~10·12)은 독립 검토였고,
11·13라운드는 검토가 아니라 넷으로 나눠 한꺼번에 고친 수정 라운드다. 14·15·16라운드는
검토와 수정을 한 라운드에 붙였다 — 여럿이 서로 다른 관점으로 동시에 파고, 나온 것을
여럿이 나눠 고쳤다. 매 라운드 다른 검토자가 **직전 라운드와 다른 관점**을 파고, 나온
것을 고치고, 다시 새 검토자를 붙였다. 12라운드부터는 검토자가 **직전 커밋의 주장이
참인지도 함께 판정한다.** 15라운드는 그 판정만을 관점으로 삼았다 — 셋이 14라운드
수정자들의 주장을 적대적으로 감사하고, 넷이 그 감사가 낸 것을 다시 고쳤다. 16라운드는
다섯이 문서-코드 대조와 `미확인` 56행 되돌리기를 나눠 맡았다. 이 문서는 그 기록이다.

읽는 사람이 이 코드를 고칠 사람이라 한국어로만 둔다. 사용자가 읽는 문서는
[README.md](README.md) 쪽이다.

남기는 이유는 두 가지다. **어떤 부류의 실수가 났는지** 알면 다음에 같은 자리를 먼저
보게 되고, **무엇이 조용히 깨졌는지** 알면 회귀 시험에 무엇을 남겨야 하는지 알 수 있다.

**이 문서를 어떻게 읽는가.** 절이 여섯이고 쓰임이 다르다.

- **`## 라운드 요약`** — 16행 한눈표. 어느 라운드가 무엇을 봤는지만 본다.
- **`## 라운드별 상세`** — 라운드마다 절이 하나다. 날짜·인원·시험 개수 변화·그 라운드
  발견만 담은 표·`직전 수정이 만든 것` 문단. **라운드를 찾아 읽는 자리가 여기다.**
- **`## 남은 버그`** — 아직 안 고쳐진 것. 다음 라운드가 여는 첫 절이어야 한다.
- **`## 되풀이된 것`** — 부류별 정리. 같은 실수를 세 번째 만났을 때 보는 자리.
- **`## 지금 있는 것`** — 지금 재고 있는 것의 실측 상태.
- **`## 이 문서를 어떻게 유지하는가`** — 다음 라운드가 채워야 할 자리.

**발견 번호는 통번호다.** `## 라운드별 상세` 의 각 표는 제 라운드 발견만 싣지만
번호(`#`)는 1부터 143까지 이어지는 하나의 계열이다 — 각주와 `## 남은 버그` 가 그
번호를 가리킨다. `## 남은 버그` 의 번호는 `B` 를 붙여 따로 센다.

**언제 있었던 일인지.** 저장소의 첫 커밋은 2026-07-31 17:55 이고, **1~13라운드는
2026-08-03 하루에 전부 돌았다** — 10:47 부터 18:55 까지, 여덟 시간이다. 14·15·16라운드는
그 다음 날인 2026-08-04 다. 열세 라운드가 여덟 시간 안에 지나갔다는 것 자체가 이
문서가 말하는 되풀이의 조건이다: 한 라운드의 수정이 실제로 무엇을 만들었는지는 다음
라운드가 열기 전에는 드러나지 않는데, 여기서는 다음 라운드가 30분 뒤에 열렸다. 그래서
"직전 수정이 만든 것" 이 이 문서에서 가장 값나가는 자리가 된다 — 시간이 알려 주지 않은
것을 그 문단이 대신 알려 준다.

15라운드까지 오도록 이 문서에는 **날짜가 한 줄도 없었다.** 그래서 "지금 131개" 같은
줄이 언제의 131 인지 아무 데도 남지 않았고, 그 줄은 이미 틀린 채로 두 라운드를
지나왔다. 아래 표의 날짜 칸과 "지금 있는 것" 의 측정 날짜가 그 자리를 메운다.

---

## 라운드 요약

**라운드 단위 요약이고, 각 칸은 한 문장이다.** 발견 하나하나는 `## 라운드별 상세` 의
그 라운드 절에 있다. 날짜는 **그 라운드를 반영한 커밋의 author date** 다 (이 문서를 쓴
날이 아니다). 하루에 열세 라운드가 몰렸으므로 시각까지 적어야 실제로 구분된다. 한
라운드가 여러 커밋에 걸쳤으면 첫~끝을 적는다. `시험 수` 는 **그 라운드가 끝나는 자리에서
실제로 등록돼 도는 케이스 수**이고, 잰 방법은 각 라운드 절에 적었다.

| # | 날짜 (반영 커밋) | 검토 관점 | 발견 수 | 시험 수 | 심각한 것 | 직전 수정이 만든 것 |
|---|---|---|---|---|---|---|
| 1 | 2026-08-03 10:47 | 번역 품질 (en·ko·ja·es 각각) | 2 | 없음 (시험 벌 이전) | 스페인어 docx 컬럼표 머리가 `Tipo\|Columna\|Tipo` 였다. | — |
| 2 | 2026-08-03 10:47 | i18n 리팩터 회귀 · 문서-코드 대조 | 2 | 없음 (시험 벌 이전) | 추가 컬럼 강조가 한국어에서만 동작했다. | 라틴 전용 폰트를 폴백에 넣어 명확한 실패가 조용한 두부(□)가 됐다. |
| 3 | 2026-08-03 11:23 | 실제 PostgreSQL 로 처음 실행 | 2 | 없음 (시험 벌 이전) | 같은 이름 테이블이 두 스키마에 있으면 하나로 합쳐졌다. | — |
| 4 | 2026-08-03 11:28 | 적대적 입력 | 3 | 없음 (시험 벌 이전) | 설명에 개행 하나면 모든 다이어그램이 죽었다. | `{key}` 자리표시자가 `t(key, **kw)` 와 충돌했다. |
| 5 | 2026-08-03 11:35~12:01 | 렌더링·레이아웃 로직 | 2 | 없음 (시험 벌 이전) | 선이 테이블을 관통하는데 검증에 그 항목 자체가 없어 `0·0·0` 이 찍혔다. | — |
| 6 | 2026-08-03 12:22 | 결정성 · 재생성 · DDL 경로 첫 접촉 | 2 | 없음 (시험 벌 이전) | `pg_dump` 출력이 테이블 0개로 파싱됐다. | — |
| 7 | 2026-08-03 12:44~13:01 | 파서 마스킹 공격 | 5 | 없음 (시험 벌 이전) | `DEFAULT '('` 하나에 다음 컬럼이 사라졌다. | 앞 라운드 재작성이 낱말 경계 없는 키워드 필터로 컬럼을 지웠고 ALTER 가 스키마를 버렸다. |
| 8 | 2026-08-03 13:19~13:27 | Postgres 기능 · 권한 · 버전 | 3 | 0 → 34 | DB 경로가 유니크 인덱스를 참조하는 FK 를 통째로 누락했다. | 마스크를 셋으로 나누며 인라인 주석이 전부 빈 문자열이 됐다. |
| 9 | 2026-08-03 13:48~14:00 | FK 쿼리 집중 공격 | 4 | 34 → 41 | 부모가 안 보이는 스키마면 같은 이름의 보이는 테이블로 연결됐다. | 주석 경계를 줄 끝까지로 넓혀 한 줄 정의가 다시 깨졌다. |
| 10 | 2026-08-03 14:58 | "수정이 다음 버그를 만든다" 를 끊겠다던 그 커밋 자체 · **그다음** 실행 | 8 | 41 → 57 | 회귀 시험이 무장 해제돼 있어 `가로선 중첩 44` 가 통과했다. | 여덟 중 넷이 9라운드 수정 것이다 — 사람용 꼬리가 시험의 눈을 감겼고 구분자 교체가 같은 버그를 다시 만들었다. |
| 11 | 2026-08-03 15:10 | 검토가 아니라 **수정** — 네 갈래로 나눠 넷이 동시에 | 5 | 57 → 57 | 끝점을 하나라도 공유하면 합류로 봐줘서 같은 선을 두 번 그린 것이 안 잡혔다. | `[경고]` 소음은 9라운드 것이고, 환경 상속은 selftest 를 처음 붙인 커밋 것이다. |
| 12 | 2026-08-03 15:57 | 버그 찾기 + **직전 커밋의 주장 판정** | 6 | 57 → 59 (+42 는 등록 안 됨) | 회귀 시험이 반증 불가능했다 — `tolerate` 한 줄이면 그림 품질 보증이 통째로 조용해졌다. | 셋 다 11라운드 것이다 — **재는 것을 늘린 커밋이 재는 눈을 감기고 산출물까지 볼모로 잡았다.** |
| 13 | 2026-08-03 16:14~18:55 | 다시 **수정** — 넷이 나눠 동시에, 쓰지 않고 검증만 한 하나 | 13 | 59 → 101 | '깨끗하다' 의 뜻을 시험이 갖게 하고 아무것도 안 재던 케이스 여덟을 고쳤다. | 12라운드 커밋이 만든 42개가 어디에도 등록되지 않아 기본 점검에서 한 번도 돌지 않았다. |
| 14 | 2026-08-04 (미커밋) | 다섯 관점 동시 — 설치·배포 / 산출물 / 설정 / **시험의 반증가능성** / 주장 판정 | 42 | 101 → 141 (옮겨 적음) | 뮤테이션 91개 중 22개가 살아남았고 그 대부분이 재는 쪽이었다(옮겨 적음, 출처: 14R 수정자 — 하네스를 안 남겨 재현 불가). | 13라운드 수정 넷(`sweep_verify`·`RENDER_ALLOW`·`verify_recs`·글로브 등록)이 전부 **저를 지키는 케이스 없이** 들어왔다. |
| 15 | 2026-08-04 (미커밋) | **검증** — 14라운드 주장을 독립 판정 (셋), 그 판정이 낸 것을 다시 수정 (넷) | 33 | 141 → 161 | 14라운드가 "이제 잡힌다" 며 내건 재는 장치를 8개가 그대로 우회했다 — 전부 `all 141 passed` 로 빠져나갔다. | 영역 코드 대소문자 충돌로 문서가 남의 그림을 싣고, 앵커 꼬리 번호가 불안정해지고, `as_file` 폴백에 영어가 하드코딩되고, `_db_case_count` 가 자기일관적 거짓말을 하게 됐다. |
| 16 | 2026-08-04 (미커밋) | 다섯 갈래 — 재는 쪽 / 산출물 / 설정 / **문서-코드 대조** / 렌더, 그리고 `미확인` 56행 되돌리기 | 11 (D 갈래만 번호가 있다) | 161 → 181 | 문서 생성을 막는 게이트 `ERD_STALE` 이 네 언어 문서 어디에도 없었고, 조작 48건 중 47건이 빨강이라 49행이 `반증됨` 으로 확정됐다. | 앵커 체계 교체로 링크가 한 번 끊겼고, `err.spec_dup_code` 하드 에러 신설로 리눅스에서 되던 spec 이 거부되고, 소스 스캔 정규식이 세 갈래로 뚫렸다. |

**커밋 쪽 번호는 이 표와 어긋난다.** 커밋 메시지가 말하는 "N 번째 검토" 는 이 표의
번호보다 하나 작다 (`79ff684` 는 "여덟 번째 검토" 라고 적었지만 표에서는 9라운드다).
왜 어긋났는지는 기록에서 확인되지 않는다. 표 쪽 번호를 정본으로 두고, 커밋을 읽을
때는 하나 더한다. 다만 `f84bfaf` 부터는 **둘** 더해야 한다 — 이 커밋이 12라운드를
"열 번째 검토" 라고 부른다. 11·13 처럼 검토가 아니라 수정인 라운드가 커밋 쪽 셈에서
빠지는 것으로 보이지만, 이것도 확인된 것은 아니다.

**시험 수는 이렇게 쟀다.** 1~13라운드는 그 라운드의 마지막 커밋에서
`git show <rev>:scripts/selftest*.py | grep -c '^@case('` 를 세고, 도커에서만 도는
6개와 **그 시점에 등록되지 않아 돌지 않던 파일**을 뺐다(2026-08-04 실측). 14라운드
값은 문서에 남은 `all 141 passed` 를 옮겨 적은 것이고, 15·16라운드 값은 실행 결과다.

---

## 라운드별 상세

라운드마다 절이 하나다. 절 머리에 **날짜(반영 커밋의 author date, 시각까지)**·**그
라운드에 붙은 사람 수**·**시험 개수 변화**를 적고, 그 라운드가 무엇을 왜 봤는지를 한
문단으로 적고, 그 라운드 발견만 담은 표를 붙이고, 마지막에 **`직전 수정이 만든 것`**
을 문단으로 적는다.

**표의 칸 채우는 규칙 — 다음 라운드도 이대로 쓴다.**

- **발견**: 사용자에게 보이는 **증상** 한 줄. 원인이 아니다. ("`ERD_WORK=''` 하나로
  파일이 cwd 에 흩어진다" 이지 "`os.environ.get` 이 빈 문자열도 값으로 친다" 가 아니다)
- **원인**: 왜 그랬는지 + `파일:줄`. **줄 번호는 원문 근거가 있는 것만 적는다** —
  없으면 파일과 함수 이름까지만 적고, 지어내지 않는다. 추측이면 `추정` 을 붙인다.
- **수정**: 무엇을 어떻게. 끝에 **`[없앰]`/`[옮김]`** 을 붙인다 — 이 문서가 이미 쓰는
  구분이다("이 수정이 경계를 옮기기만 하는가 없애는가"). `[옮김]` 은 다음 라운드가
  같은 부류를 다시 만날 자리라는 표시다.
- **검증 결과**: 독립 검증자의 판정 — `참` / `과장` / `거짓` / `미검증` / `판정불가` /
  `기록 없음`. **누가 어떻게 판정했는지 근거를 한 조각 넣는다**(뮤턴트 이름, 명령, 수치).
  자기가 재현하지 않고 옮겨 적은 수치는 **`옮겨 적음(출처: …)`** 이라고 반드시 표시한다.
- **회귀 여부**: 두 가지를 `·` 로 함께 적는다. ① 이 수정이 **회귀를 만들었나** —
  `없음` 또는 `있음: <무엇>`. ② **되돌리면 빨강인가** — `반증됨(<조작>)` /
  `반증 안 됨` / `미확인 — <누가> 안 돌림` / `기록 없음`. 이 문서의 규율상
  **`반증 안 됨` 은 그 자체로 미완**이고, 그 행들이 다음 라운드의 작업 목록이다.
- **회귀 시험**: 지키는 케이스 이름. 없으면 `없음` — **빈칸으로 두지 않는다.**
- **칸은 한 문장, 140자 이내다.** 길게 쓸 것은 `### 각주` 로 빼거나 절의 문단으로
  내린다. `docs: every row of the REVIEW-LOG tables has the shape its header declares`
  가 이것을 실제로 잰다 — 넘기면 시험이 빨강이다.

1~13라운드는 원본 서술을 발견 단위로 쪼갤 수 있는 만큼 쪼갰다. 그 라운드들에는 독립
검증 기록도 반증 기록도 없어서 `검증 결과`·`회귀 여부` 가 대부분 `기록 없음` 이다 —
**모르는 것을 채우지 않았다.** 12·13라운드부터 판정이 생기므로 그 뒤로는 값이 있다.

### 1라운드 — 번역 품질 (2026-08-03 10:47)

**반영 커밋** `7fc6a33`~`09b5551` (2026-08-03 10:47:17~10:47:57, 다국어화 배치 안에 들어 있다) · **붙은 사람** 검토 1 갈래(인원 수는 기록 없음) · **시험** 없음 → 없음 (시험 벌은 8라운드에 생긴다).

직전 작업이 사람이 읽는 모든 줄을 네 언어 카탈로그로 옮긴 대규모 치환이었다. 치환은
문법을 안 깨뜨리면서 뜻을 깨뜨리므로, 이 라운드는 코드가 아니라 **카탈로그의 낱말**만
en·ko·ja·es 각각으로 읽었다. 스페인어에서 한 낱말이 두 열의 머리로 동시에 쓰인 것이
그렇게 나왔다 — 파이썬으로는 아무 문제가 없는 문자열이다.

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 1 | docx 컬럼표 머리가 스페인어에서 `Tipo\|Columna\|Tipo` 로 나온다 | `lang/es.py` 가 `word.kind` 와 `col.type` 에 같은 낱말을 넣었다 | `word.kind` 를 `Clase` 로 갈랐다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `i18n: four catalogs agree` |
| 2 | `ERD_MAX_AREAS` 기본값이 문서엔 7, 코드엔 12 라 문서대로 잡으면 영역이 더 나온다 | 문서와 코드가 같은 값을 따로 적었다 (`SKILL.md`·`config.py`) | 문서를 코드 값(12)에 맞췄다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | 없음 |

**직전 수정이 만든 것.** — (직전 작업이 다국어화 배치 자체이고, 그 배치가 만든 것은
2라운드가 짚는다.)

**1·2라운드의 날짜는 갈리지 않는다.** 둘의 수정이 다국어화 배치(`7fc6a33`~`09b5551`)
**안에** 들어 있다 — 그 다섯 커밋의 author date 가 10:47:17~10:47:57 로 10초 간격이니
배치째 다시 쓰인 것이고, `e25d8c8` 의 제목("`--check` 가 `SKILL.md` 를 본다")과
`7fc6a33` 의 `lang/es.py`(`word.kind: 'Clase'`)가 이미 두 라운드의 수정을 담고 있다.
즉 이 두 라운드는 **검토가 끝난 뒤 다국어화 커밋을 고쳐서** 반영됐고, 그래서 커밋
기록만으로는 "다국어화 작업" 과도 서로와도 구분되지 않는다. 15라운드에 날짜를 넣으며
처음 보인 것이다 — 날짜가 없을 때는 이 어긋남이 아무 데도 안 보였다.

### 2라운드 — i18n 리팩터 회귀 · 문서-코드 대조 (2026-08-03 10:47)

**반영 커밋** `7fc6a33`~`09b5551` (2026-08-03 10:47:17~10:47:57, 1라운드와 같은 배치) · **붙은 사람** 검토 1 갈래(인원 수는 기록 없음) · **시험** 없음 → 없음.

1라운드가 낱말을 봤다면 이 라운드는 **치환이 동작을 바꾼 자리**와 **문서가 적은 값과
코드가 쓰는 값이 어긋난 자리**를 봤다. 마커를 만드는 쪽만 카탈로그로 옮기고 비교하는
쪽은 하드코딩으로 남은 것이 첫째이고, `ERD_MAX_AREAS` 기본값이 문서엔 7 코드엔 12 인
것이 둘째다. 두 번째 모양은 열네 라운드 뒤 16라운드에서 다시 한 라운드를 통째로
차지한다 — **문서와 코드는 따로 낡는다.**

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 3 | 추가 컬럼 강조가 한국어에서만 동작한다 | 마커를 **만드는** 쪽만 카탈로그로 옮기고 **비교하는** 쪽은 `startswith('[추가]')` 로 남았다 (`build_docx.py`·`build_html.py`) | 비교하는 쪽도 카탈로그를 쓰게 했다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | 없음 |
| 4 | `install.sh --check` 가 `SKILL.md` 를 점검하지 않는다 | `--check` 에 그 절이 아예 없었다 (`install.sh`) | `[ -f "$DEST/SKILL.md" ]` 한 줄을 넣었다 [옮김][^f-check] | 거짓 — 14R 이 "존재만 본다" 로 반증 | 있음: 존재만 보는 검사 · 기록 없음 | `install: --check rejects a SKILL.md without frontmatter` (14R) |

**직전 수정이 만든 것.** 다국어화 배치가 폰트 폴백에 **라틴 전용 글꼴**을 넣었다. 그
전에는 글꼴이 없으면 눈에 띄게 실패했는데, 그 뒤로는 한글·일본어가 조용히 두부(□)로
나왔다. 실패를 폴백으로 덮으면 실패가 사라지는 것이 아니라 **말을 안 하게 된다** —
이 문서가 뒤에 `해당 없음` 과 `0` 을 구분하라고 적는 것과 같은 뿌리다.

### 3라운드 — 실제 PostgreSQL 로 처음 실행 (2026-08-03 11:23)

**반영 커밋** `5110713` (2026-08-03 11:23:22) · **붙은 사람** 검토 1 갈래 · **시험** 없음 → 없음.

두 라운드가 문자열만 봤으니 이번엔 **진짜 서버**에 붙였다. 그때까지 아무도 그러지
않았다. 붙이자마자 나온 둘이 전부 `introspect.py` 의 키 설계 문제였다 — 테이블을
이름만으로 식별했고, 복합 FK 의 열을 짝짓지 않고 곱집합으로 만들었다. 둘 다 스키마가
하나뿐인 장난감 DB 에서는 영원히 안 나온다.

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 5 | 같은 이름 테이블이 두 스키마에 있으면 하나로 합쳐진다 (컬럼 뒤섞임·PK `['id','id']`·한쪽은 그림에서 소멸) | 테이블을 이름만으로 키를 삼았다 (`introspect.py`) | 스키마를 키에 넣었다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `introspect: the same table name in two schemas stays two tables` |
| 6 | 복합 FK 가 곱으로 불어나 존재하지 않는 관계가 문서에 실린다 | 열을 짝짓지 않고 곱집합을 만들었다 (`introspect.py`) | 위치로 짝지었다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `introspect: a composite FK is not multiplied into pairs that do not exist` |

**직전 수정이 만든 것.** — (확인된 것 없음.)

### 4라운드 — 적대적 입력 (2026-08-03 11:28)

**반영 커밋** `7ff2faa` (2026-08-03 11:28:56) · **붙은 사람** 검토 1 갈래 · **시험** 없음 → 없음.

"사람이 손대는 자리에서 죽지 않게 한다" 가 그 커밋의 제목이다. 컬럼 설명·테이블
이름처럼 **사람이 적는 값**에 개행·제어문자를 넣어 봤다. 개행 하나로 모든 다이어그램이
죽었고 제어문자 하나로 GraphML·SVG 가 XML 로서 깨졌다. 여기서 `config.clean()` 이라는
씻는 자리가 처음 생겼고, 그 자리가 있느냐 없느냐가 뒤로 열 라운드를 따라다닌다 —
14라운드는 같은 결함이 **spec 경로**에 그대로 남아 있는 것을 찾는다(#62).

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 7 | 설명에 개행 하나가 들어가면 모든 다이어그램이 죽는다 | 씻는 자리가 없었다 (`config.clean`) | `clean()` 을 넣고 모든 입력이 지나가게 했다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `clean: a newline in a comment does not kill the diagram` |
| 8 | 제어문자가 GraphML·SVG 를 XML 로서 깨뜨려 yEd 가 파일을 거부한다 | `config._CTRL` 씻는 표가 없었다 | 제어문자를 공백으로 바꾸는 표를 넣었다 [없앰] | 과장 — 14R 이 "fixture 가 `\x00`·`\x07` 을 안 쓴다" 로 반증 | 없음 · 반증됨(15R-D, `S-ctrl`: `_CTRL = {}`) | `errors: a control character in a name does not kill the build` |
| 9 | spec 오타 하나에 raw traceback 이 나온다 | `load_spec` 이 `areas` 키만 검사했다 (`config.py`) | `areas` 에 사람 말 오류를 붙였다 [옮김][^f-half] | 거짓 — 14R 이 나머지 키에서 같은 traceback 을 재현 | 있음: 나머지 키는 그대로 · 기록 없음 | `spec: a typo names what is wrong instead of a traceback` |

**직전 수정이 만든 것.** i18n 기반이 `{key}` 자리표시자를 쓰는데 `t(key, **kw)` 의
포맷과 충돌했다. 카탈로그 문자열 안의 중괄호가 사용자 값과 서식 인자 양쪽으로
읽히는 자리다.

### 5라운드 — 렌더링·레이아웃 로직 (2026-08-03 11:35~12:01)

**반영 커밋** `5dcbb44`·`76d0543`·`57c132a`·`2faa741` (2026-08-03 11:35:42~12:01:54) · **붙은 사람** 검토 1 갈래 · **시험** 없음 → 없음.

처음으로 **그림 자체**를 봤다. 그리고 이 문서에서 가장 자주 인용되는 발견이 여기서
나온다 — 선이 테이블을 관통하는데 검증은 `0·0·0` 을 찍고 있었다. **재지 않은 것과
재 봤더니 0 인 것은 다르다.** 검증에 그 항목 자체가 없었을 뿐인데 출력은 완벽해
보였다. 관통한 선이 눈에도 안 보였다는 것이 둘째다 — 선을 노드보다 먼저 그리니 노드
색이 덮었다. **눈으로는 못 잡는 것이 있다.**

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 10 | 선이 테이블을 관통하는데 검증은 `0·0·0` 을 찍는다 | 검증에 그 항목 자체가 없었다 (`erd.draw_erd`) | `thru_nodes()` 를 넣고 기록에 실었다 [없앰] | 기록 없음 | 기록 없음 · 반증됨(15R-D, `S-thru`: `> 4` → `> 4000`) | `verify: the through-table counter still counts a line crossing a table` |
| 11 | 관통한 선이 노드 색에 덮여 눈에도 안 보인다 | 선을 노드보다 먼저 그렸다 (`erd.draw_erd`) | 그리는 순서를 바꿨다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | 없음 |

**직전 수정이 만든 것.** — (확인된 것 없음.)

### 6라운드 — 결정성 · 재생성 · DDL 경로 첫 접촉 (2026-08-03 12:22)

**반영 커밋** `e74addd` (2026-08-03 12:22:50) · **붙은 사람** 검토 1 갈래 · **시험** 없음 → 없음.

DB 없이 DDL 파일만으로 도는 경로를 처음 밟았다. `pg_dump` 출력을 그대로 먹였더니
테이블이 **0개**로 파싱됐다 — `CREATE TABLE public.x`·`IF NOT EXISTS` 모양을 못 잡은
것이다. 인라인 `REFERENCES` 도 안 읽어 관계가 전멸했다. 이 라운드가 연 문이 7·8·9·10
라운드를 통째로 잡아먹는다: `parse_ddl.py` 한 파일에서 네 라운드 연속으로 발견이
나온다.

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 12 | `pg_dump` 출력이 테이블 0개로 파싱된다 | `CREATE TABLE public.x`·`IF NOT EXISTS` 모양을 못 잡았다 (`parse_ddl.py`) | 스키마 접두·`IF NOT EXISTS`·`ONLY` 를 인식하게 했다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: pg_dump shapes — schema-qualified, ONLY, ADD CONSTRAINT` |
| 13 | 인라인 `REFERENCES` 를 못 읽어 관계가 전멸한다 | 컬럼 줄의 `REFERENCES` 를 안 봤다 (`parse_ddl.py`) | 컬럼 줄에서도 FK 를 뽑는다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: a constraint written after REFERENCES still counts` |

**직전 수정이 만든 것.** — (확인된 것 없음.)

### 7라운드 — 파서 마스킹 공격 (2026-08-03 12:44~13:01)

**반영 커밋** `084ec39`·`306a175` (2026-08-03 12:44:34~13:01:59) · **붙은 사람** 검토 1 갈래 · **시험** 없음 → 없음.

6라운드가 연 파서를 **문자열·주석·달러 인용**으로 공격했다. `DEFAULT '('` 하나에
다음 컬럼이 사라지고, 문자열 안의 `--` 뒤가 사라지고, `$$` 함수 본문 안의
`CREATE TABLE` 이 유령 테이블이 됐다. 부분 패치가 계속 새서 앞단을 **문자열·주석을
아는 마스크**로 다시 썼다. 그 판단 자체는 맞았는데, 다시 쓸 때마다 이전 판이 하던
일을 하나씩 떨어뜨린다 — 이 라운드에서만 재작성이 둘을 새로 만들었고(#17·#18) 그
모양이 8·9라운드까지 이어진다.

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 14 | `DEFAULT '('` 하나에 다음 컬럼이 사라진다 | 괄호를 세면서 문자열 안을 구분하지 않았다 (`parse_ddl.py`) | 문자열·주석을 아는 마스크로 앞단을 다시 썼다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: string literals cannot break the split` |
| 15 | 문자열 안의 `--` 뒤 컬럼이 사라진다 | 같은 곳 — 주석 판별이 문자열을 몰랐다 (`parse_ddl.py`) | 같은 마스크로 함께 고쳤다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: string literals cannot break the split` |
| 16 | `$$` 함수 본문 안의 `CREATE TABLE` 이 유령 테이블이 된다 | 달러 인용 구간을 건너뛰지 않았다 (`parse_ddl.py`) | 달러 인용 본문을 통째로 건너뛴다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: dollar-quoted function bodies are skipped` |
| 17 | `checksum`·`like_count` 같은 컬럼이 통째로 사라진다 (직전 재작성이 만든 것) | 낱말 경계 없는 키워드 필터 (`parse_ddl.py`) | 낱말 경계를 넣었다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: columns named after table-level keywords survive` |
| 18 | FK 가 남의 테이블에 붙는다 (직전 재작성이 만든 것) | `ALTER` 가 스키마를 버렸다 (`parse_ddl.py`) | `ALTER` 도 스키마를 들고 다니게 했다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: a schema-qualified ALTER does not land on another schema` |

**직전 수정이 만든 것.** 6라운드의 재작성이 **낱말 경계 없는 키워드 필터**를 써서
`checksum`·`like_count` 같은 컬럼을 통째로 지웠고(#17), `ALTER` 가 스키마를 버려 FK
가 남의 테이블에 붙었다(#18). 두 발견 다 이 라운드가 제 손으로 만든 것을 제 손으로
찾은 것이다.

### 8라운드 — Postgres 기능 · 권한 · 버전 (2026-08-03 13:19~13:27)

**반영 커밋** `81038da`·`d05667e` (2026-08-03 13:19:39~13:27:26) · **붙은 사람** 검토 1 갈래 · **시험** 없음 → **34** (`scripts/selftest.py` 가 `d05667e` 에서 생겼다).

관점을 파서에서 **서버 쪽 기능**으로 옮겼다. FK 조회가 PK 만 부모로 쳐서 유니크
인덱스를 참조하는 FK 를 통째로 누락하고 있었다. 그리고 이 라운드가 이 저장소에서
가장 큰 구조 변화를 낸다 — **회귀 시험 벌이 여기서 처음 생긴다**(`d05667e`,
"재는 것이 없던 기능에 회귀 시험을 붙인다", 34개). 그 전 일곱 라운드는 재는 것 없이
지나갔고, 그래서 1~7라운드 표의 `회귀 시험` 칸이 대부분 비어 있다.

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 19 | DB 경로가 유니크 인덱스를 참조하는 FK 를 통째로 누락한다 | FK 조회가 PK 만 부모로 쳤다 (`introspect.py`) | 유니크 인덱스도 부모로 인정한다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | 없음 (도커 케이스 여부 확인 못함) |
| 20 | 인라인 주석이 전부 빈 문자열이 된다 (직전 수정이 만든 것) | 마스크를 셋으로 나누며 주석 본문을 잃었다 (`parse_ddl.py`) | 주석 본문을 되살렸다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: inline -- comment attaches to its own column` |
| 21 | 컬럼이 0개인 테이블에서 `max()` 가 크래시한다 (직전 수정이 만든 것) | 빈 목록에 `max()` (`erd.py`) | 기본값을 줬다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `render: badge clears the name on a title-only box` |

**직전 수정이 만든 것.** 7라운드가 마스크를 셋으로 나누면서 **주석 본문을 잃었다** —
인라인 주석이 전부 빈 문자열이 됐다(#20). 그리고 컬럼이 0개인 테이블에서 `max()` 가
빈 목록에 걸려 크래시했다(#21). 파서 앞단 재작성이 세 라운드째 뒤끝을 남기는 중이다.

### 9라운드 — FK 쿼리 집중 공격 (2026-08-03 13:48~14:00)

**반영 커밋** `79ff684`·`2f5c066` (2026-08-03 13:48:50~14:00:04) · **붙은 사람** 검토 1 갈래 · **시험** 34 → **41**.

FK 조회 하나만 파고들었다. 부모가 안 보이는 스키마에 있으면 **같은 이름의 보이는
테이블로 연결**돼서, 없는 컬럼을 가리키는 관계가 문서에 실렸다. 그리고 값 속 개행이
psql 행 구분을 깨 유령 테이블이 문서 첫 장이 됐다 — 이 라운드가 그것을 **구분자를
바꿔서** 고쳤고, 그 선택이 10라운드 발견 하나를 통째로 만든다.

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 22 | 부모가 안 보이는 스키마면 같은 이름의 보이는 테이블로 연결된다 — 없는 컬럼을 가리키는 관계가 그려진다 | FK 부모를 이름으로만 찾았다 (`introspect.py`) | 안 보이면 관계를 버린다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `introspect: an FK parent you cannot see is dropped, not rewired` |
| 23 | 값 속 개행이 psql 행 구분을 깨 유령 테이블이 문서 첫 장이 된다 | 행 구분자가 개행이었다 (`introspect.py`) | 구분자를 바꿨다 [옮김][^f-sep] | 거짓 — 10R 이 `\x1e` 로 같은 것을 재현 | 있음: 다음 바이트가 그대로 남음 · 기록 없음 | `introspect: a newline inside a default does not forge a row` |
| 24 | 한 줄 정의가 다시 깨진다 (직전 수정이 만든 것) | 주석 경계를 줄 끝까지로 넓혔다 (`parse_ddl.py`) | 기준을 줄로 바꿨다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: one-line definition keeps every column` |
| 25 | `REFERENCES` 뒤에 적은 `NOT NULL` 이 무시된다 (직전 수정이 만든 것) | `REFERENCES` 뒤를 안 읽었다 (`parse_ddl.py`) | 뒤에 붙은 제약도 읽는다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: a constraint written after REFERENCES still counts` |

**직전 수정이 만든 것.** 8라운드가 주석 경계를 줄 끝까지로 넓혀 **한 줄 정의가 다시
깨졌고**(#24), `REFERENCES` 뒤에 적은 `NOT NULL` 이 무시됐다(#25). 7→8→9 로 세 번째
연속이다.

### 10라운드 — 그 커밋 자체를 감사한다 (2026-08-03 14:58)

**반영 커밋** `0e4c292` (2026-08-03 14:58:17) · **붙은 사람** 검토 1 갈래 · **시험** 41 → **57**.

관점이 처음으로 **메타**로 갔다. "수정이 다음 버그를 만든다" 를 끊겠다고 쓴 그
커밋과 **그다음 실행**만 봤다. 발견이 여덟으로 이 저장소 전체에서 한 라운드 최다에
가깝고, 그중 넷이 직전 라운드 수정 것이다. 여기서 이 문서의 두 이름이 붙는다 —
**재는 쪽도 조용히 죽는다**(시험이 검증의 사람용 줄을 정규식으로 긁다가 `(허용)`·
`[경고]` 꼬리 때문에 마지막 항목을 **0 이 아닐 때만** 놓쳤다: `가로선 중첩 44` 가
통과했다), 그리고 **첫 실행만 봤다**(같은 디렉터리에 두 번 돌리자 지난 판 그림이 새
글 옆에 실렸다).

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 26 | `가로선 중첩 44` 인 그림이 회귀 시험을 통과한다 | 검증 줄을 정규식으로 긁는데 `(허용)`·`[경고]` 꼬리가 붙자 마지막 항목을 **0 이 아닐 때만** 놓쳤다 (`selftest.py`) | 사람용 줄 대신 기계가 읽는 JSONL 을 읽는다 [없앰] | 기록 없음 | 없음 · 반증됨(15R-D, `S-logzero`: `verify_recs` → `return []` → 4개 빨강) | `verify: the printed line and the machine record say the same thing` |
| 27 | 컬럼 **위**에 적은 주석이 한 칸씩 밀려 DDL 설명이 전부 남의 컬럼 것이 된다 | 주석을 다음 컬럼이 아니라 앞 컬럼에 붙였다 (`parse_ddl.py`) | 기준을 줄로 바꿨다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: a comment above a column describes that column` |
| 28 | PostgreSQL 10 이하에서 FK 가 전멸하는데 `FK 0` 을 사실인 양 출력한다 | `conparentid` 는 PG 11 부터다 (`introspect.py`) | 버전을 보고 술어를 가르고, 조회가 죽으면 문서를 안 만든다 [없앰] | 기록 없음 | 없음 · 반증됨(15R-D, `S-minpg`: `MIN_PG` → 0) | `introspect: a server too old to answer is refused, not half-read` |
| 29 | 유령 행이 값 속 `\x1e` 로 그대로 재현된다 | 9R 이 구분자만 바꿔 경계를 옮겼다 (`introspect.py`) | 전송을 `row_to_json` 으로 바꿔 원리적으로 막았다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `introspect: a control character in a value cannot forge a row` |
| 30 | 새 컬럼표 옆에 지난 판 그림이 실린다 | `exists()` 만 보고 시각을 비교하지 않았다 (`build_erd.py`) | 스키마보다 오래된 그림이면 문서를 안 만든다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `artifacts: a diagram older than the schema does not reach the document` |
| 31 | `MANUAL` 이 도구가 인쇄해 준 바로 그 키로는 안 걸린다 | 인쇄하는 키와 찾는 키가 달랐다 (`merge_desc.py`) | 한 자리에서 뽑게 묶었다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `merge_desc: the key the tool prints is the key that works` |
| 32 | 일곱 조회 중 하나가 죽어도 완성본처럼 문서가 나온다 | 조회 실패를 빈 결과로 삼켰다 (`introspect.py`) | 반쪽 읽힌 DB 는 문서를 안 만든다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `introspect: a half-read database is refused, not documented` |
| 33 | `parse_ddl` 의 라이브 조회 경로는 아직 개행으로 행을 가른다 | 같은 부류의 나머지 절반 (`parse_ddl.py`) | 이쪽도 안전한 전송으로 바꿨다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: a newline inside a fetched value does not kill the parser` |

**직전 수정이 만든 것.** 여덟 중 넷이 9라운드 것이다. ① 검증 줄에 사람 좋으라고 붙인
`(허용)`·`[경고]` 꼬리가 **시험의 눈을 감겼다**(#26). ② 구분자를 개행에서 `\x1e` 로
바꾼 수정이 **같은 버그를 다시 만들었다** — 값 속 `\x1e` 로 유령 행이 그대로
재현됐다(#29). ③ FK 를 pg_catalog 로 옮기며 쓴 술어가 PG 11 부터 있는 컬럼이라
10 이하가 통째로 죽었는데 `FK 0` 을 사실인 양 출력했다(#28). ④ 일곱 조회 중 하나가
죽어도 완성본처럼 문서가 나왔다(#32). 넷 다 "경계를 옮기기만 했다" 의 표본이다.

### 11라운드 — 검토가 아니라 수정, 넷이 동시에 (2026-08-03 15:10)

**반영 커밋** `c853bab` (2026-08-03 15:10:37) · **붙은 사람** 수정 4 갈래(동시) · **시험** 57 → **57**.

처음으로 검토가 아니라 **수정**만 한 라운드다. 넷이 갈래를 나눠 동시에 고쳤다. 겹침
판정을 고치고(끝점을 하나라도 공유하면 합류로 봐주던 것을 양 끝이 둘 다 같으면
합류가 아닌 것으로), 라벨을 그리지도 않는 개요도가 라벨 겹침 `0` 을 찍던 것을
`해당 없음` 으로 갈랐다. 그리고 **경고가 소음이 되면 죽는다** 를 여기서 처음 만난다 —
스물다섯 도판 중 열일곱이 첫 실행부터 `[경고]` 를 달고 나왔다. 주장을 낮추는 대신
세는 쪽을 고쳤다.

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 34 | 같은 선을 두 번 그려도 겹침으로 안 잡힌다 | 끝점을 하나라도 공유하면 합류로 봐줬다 (`erd.overlaps`) | 양 끝이 **둘 다** 같으면 합류가 아니다 [없앰] | 기록 없음 | 없음 · 반증됨(15R-D, `S-r11`: 가드 되돌리기) | `verify: the overlap counter still counts two lines drawn on the same spot` |
| 35 | 라벨을 아예 안 그리는 개요도가 라벨 겹침 `0` 을 보고한다 | 안 한 검사를 0 으로 적었다 (`erd.draw_erd`) | 잴 것이 없으면 `해당 없음` 으로 적는다 [없앰] | 기록 없음 | 없음 · 반증됨(15R-D, `S-logzero` 가 함께 잡음) | `verify: a diagram without labels does not report a label check it never ran` |
| 36 | 스물다섯 도판 중 열일곱이 첫 실행부터 `[경고]` 를 단다 — 경고가 소음이 된다 | 라우터가 고칠 수 없는 겹침(컬럼 행에 못박힌 꼬리)까지 셌다 (`erd.overlaps`) | 고칠 수 있는 것만 센다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `verify: a plain hub-and-spoke schema raises no warning on its first run` |
| 37 | `ERD_LABEL` 을 켜 둔 셸에서 `--check` 가 멀쩡한 설치를 고장 났다고 알린다 | 시험이 부르는 사람의 `ERD_*` 를 물려받았다 — 지운 것이 `ERD_DB`·`ERD_PSQL` 둘뿐이었다 (`selftest_kit.run`) | `ERD_*` 를 전부 지우고 돌린다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `selftest: the suite does not inherit the caller ERD_* environment` |
| 38 | 자기참조 루프 둘이 정확히 겹쳐 하나로 보인다 | 두 단계 경로 계산을 통로 lane 에만 적용했다 (`erd.py`) | lane 쪽만 고쳤다 [옮김][^f-self] | 거짓 — 12R 이 "경고 0" 을 120개 중 19개로 반증 | 있음: 자기참조는 그대로 · 기록 없음 | `render: a self-reference does not draw one line on top of another` |

**직전 수정이 만든 것.** `[경고]` 소음 자체는 9라운드 것이고, 시험이 부르는 사람의
`ERD_*` 를 물려받는 문제는 selftest 를 처음 붙인 8라운드 커밋 것이다. 이 라운드
자신이 만든 것은 다음 라운드가 짚는다 — 셋이나 된다.

**이 라운드가 내건 자랑이 다음 라운드에 반증된다.** "무작위 200개 스키마에서 경고 0"
이라고 적었는데 12라운드가 퍼저 120개 중 19개로 반증했다(#42). 좁게 재고 넓게 쓴
자리의 네 번째 표본이다.

### 12라운드 — 버그 찾기 + 직전 커밋의 주장 판정 (2026-08-03 15:57)

**반영 커밋** `f84bfaf` (2026-08-03 15:57:20) · **붙은 사람** 검토 1 갈래 · **시험** 57 → **59** (같은 커밋이 `selftest_history.py` 42개를 만들었는데 **아무 데도 등록되지 않아 돌지 않았다**).

**검토자에게 버그 찾기와 함께 "직전 커밋의 주장을 하나씩 판정하는 일" 을 처음
맡긴 라운드**다. 첫 판정에서 열 개 중 여섯만 참이었다(과장 셋, 거짓 하나). 그리고
목록에 없던 것 하나를 짚었는데 그게 제일 컸다 — **회귀 시험이 반증 불가능했다.**
`verify_clean()` 이 카운터를 다시 세지 않고 검사 대상이 적어 준 `warn` 목록만 봐서,
`tolerate` 를 한 줄 넓히면 그림 품질 보증이 통째로 조용해졌다. 여기서 "'깨끗하다' 의
뜻은 시험이 가진다" 가 규칙이 된다.

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 39 | `tolerate` 를 한 줄 넓히면 그림 품질 보증이 통째로 조용해진다 | `verify_clean()` 이 다시 세지 않고 검사 대상이 적어 준 `warn` 목록만 봤다 (`selftest.py`) | '깨끗하다' 의 규칙을 시험이 갖고 `counts` 만 읽는다 [없앰] | 기록 없음 | 없음 · 반증됨(15R-D, `S-faults`: `verify_faults` → `return []`) | `verify: a clean verdict is computed here, not taken from the record` |
| 40 | `<node>` 0개인 GraphML 이 "테이블이 다 실렸다" 를 통과한다 (여덟 케이스가 아무것도 안 쟀다) | 검사 대상이 없어도 통과로 쳤다 (`selftest.py`) | 개수와 자리를 못박고, 잴 것이 없으면 실패다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `artifacts: html, docx and graphml describe the same schema` |
| 41 | `ERD_VERIFY_LOG` 가 쓸 수 없는 경로면 "검증했다" 만 찍고 죽어 `out/` 이 빈다 | 그림 저장보다 계측을 먼저 했다 (`erd.draw_erd`) | 그림을 먼저 저장하고 계측을 뒤에 남긴다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `errors: a verify log that cannot be written does not cost the figures` |
| 42 | 11R 의 "무작위 200개 스키마에서 경고 0" 이 사실이 아니다 | 좁게 재고 넓게 썼다 (커밋 메시지) | 주장을 낮추는 대신 자기참조 경로를 고쳤다 [없앰] | 거짓 — 12R 이 퍼저 120개 중 19개 경고로 반증 · 옮겨 적음(출처: 12R 검토자) | 기록 없음 · 기록 없음 | `render: a self-reference does not draw one line on top of another` |
| 43 | "값이 담을 수 있는 어떤 바이트도 구분자로 읽히지 않는다" 가 `parse_ddl` 라이브 경로에는 거짓이다 | `introspect.py` 에서만 확인한 것을 전 경로로 썼다 | `parse_ddl` 쪽도 같은 전송으로 [없앰] | 과장 — 12R 판정 · 옮겨 적음(출처: 12R 검토자) | 기록 없음 · 기록 없음 | `parse: a newline inside a fetched value does not kill the parser` |
| 44 | "합류는 끝점을 정확히 하나만 공유해야 한다" 는 코드가 하지 않는 규칙이다 | 문서가 코드보다 강한 말을 적었다 | 주석을 코드가 하는 일에 맞췄다 [없앰] | 과장 — 12R 판정 · 옮겨 적음(출처: 12R 검토자) | 기록 없음 · 기록 없음 | `verify: the overlap counter still counts two lines drawn on the same spot` |

**직전 수정이 만든 것.** 심각한 셋이 **전부** 11라운드 것이고, 모양이 달라졌다 —
셋이 다 **검증을 늘린 자리**에서 나왔다. ① 겹침을 줄이려 만든 두 단계 경로 계산이
자기참조를 빼먹었다(#38). ② 새로 넣은 검증 기록이 시험의 판단을 대신했다(#39).
③ 그 기록을 남기지 못하면 그림까지 사라졌다(#41). **고치는 손보다 재는 손이 만든
것을 먼저 보게 된다.**

**실측으로 바로잡을 것 하나.** 15라운드까지 이 문서는 "13라운드가 소급해 만든 시험
36개" 라고 적었는데, `git log --diff-filter=A -- scripts/selftest_history.py` 는 그
파일이 **`f84bfaf`, 곧 12라운드 커밋**에서 생겼다고 말한다(그 시점 `@case` 36개 +
도커 6개 = 42). 그것이 아무 데도 등록되지 않아 한 번도 돌지 않았다는 **발견**은
13라운드 것이 맞다(#46). 만든 라운드와 발견한 라운드가 다르다.

### 13라운드 — 다시 수정, 넷이 나눠 동시에 (2026-08-03 16:14~18:55)

**반영 커밋** `800f0eb`·`c391783`·`f6fbc05`·`2e87922` (2026-08-03 16:14:11~18:55:05) · **붙은 사람** 수정 4 갈래(그중 하나는 쓰지 않고 검증으로 돌아섰다) · **시험** 59 → **101**.

두 번째 수정 라운드다. 12라운드가 낸 "'깨끗하다' 의 뜻을 시험이 가진다" 를 실제로
넣고, 아무것도 안 재던 케이스 여덟을 고치고, 아무도 안 부르던 42개를 입구에 이었다
(`c391783`). 등록 방법 자체가 조용히 실패할 수 있다는 것도 여기서 나온다 — 순진한
`import selftest_history` 한 줄은 **아무 말 없이 아무것도 등록하지 않는다.** 그래서
목록과 도우미를 제3의 모듈(`selftest_kit.py`)로 빼고 글로브로 찾는 지금 구조가 됐다.

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 45 | 자기참조 팔이 겹친다 — 팔 높이가 진입 y 보다 먼저 정해지고, 팔의 x 가 등록만 되고 검사되지 않았다 | 경로 계산 두 자리 (`erd.py`) | 두 자리를 고쳤다 [옮김][^f-self3] | 과장 — 14R 이 셋째 갈래를 재현 | 있음: x 구간이 걸치는 셋째 갈래 · 기록 없음 | `render: a self-reference loop is not painted through a table` |
| 46 | 소급해 만든 시험 36개가 어디에서도 돌지 않는다 | `selftest.py` 에 그 파일을 부르는 줄이 없었다 | 14R 이 글로브 등록으로 이었다 [옮김][^f-reg] | 참 — 13R 스스로 적었고 14R 이 확인 | 기록 없음 · 반증됨(15R-D, `V16`) | `selftest: every case file beside the kit is registered and says how many it added` |
| 47 | 블록 주석이 컬럼 설명으로 새어 나온다 | 블록 주석 경계 (`parse_ddl.py`) | 경계를 고쳤다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: a block comment never becomes a column description` |
| 48 | `ERD_MAX_AREAS` 가 문서 전체 기준이 아니다 | 자동 분류 가지에만 걸려 있었다 (`erd.py`) | 문서 전체 기준으로 [옮김][^f-cap] | 과장 — 15R 이 spec 가지가 여전히 상한을 안 본다고 확인 | 있음: spec 가지 · 반증됨(16R-D, `M48`) | `erd: ERD_MAX_AREAS is a cap the document actually respects` |
| 49 | 범례가 캔버스 오른쪽 끝을 넘어 잘린다 | 범례 폭을 안 쟀다 (`erd.py`) | 폭을 재서 캔버스를 넓힌다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `render: nothing is drawn past the right edge of the canvas` |
| 50 | 따옴표 친 식별자가 한 이름으로 안 읽힌다 | `"a b"` 를 두 토큰으로 갈랐다 (`parse_ddl.py`) | 따옴표 안을 한 이름으로 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | `parse: a quoted name containing a space is one name` |
| 51 | 그림이 하나도 없으면 docx 가 죽는다 | 빈 목록을 가정하지 않았다 (`build_docx.py`) | 없으면 없는 대로 만든다 [없앰] | 기록 없음 | 기록 없음 · 기록 없음 | 기록 없음 |
| 52 | 라벨↔테이블만 여백 상자로 재고 맞닿음 규칙이 `<` 와 `<=` 로 서로 반대다 | 라벨 겹침을 잉크로 재게 고치면서 `라벨↔라벨` 만 고쳤다 (`erd.py`) | 반쪽만 고쳤다 [옮김][^f-half] | 과장 — 14R 이 나머지 절반을 재현 | 있음: 라벨↔테이블 · 반증됨(15R-D, `S-clean`) | `verify: the two label checks use one box and one rule` |
| 53 | "59개 중 21개가 빨강" 이 재현되지 않는다 | 어떤 회귀를 넣었는지 커밋이 안 적었다 (커밋 메시지) | — (주장) | 판정불가 — 14R 이 같은 방향으로 재니 2개→4개 · 옮겨 적음(출처: 13R 커밋) | 기록 없음 · 기록 없음 | 없음 |
| 54 | `c391783` 의 "배치하는 **네 곳**과 재는 곳을 같은 상수로 묶었다" | 재는 곳은 절반만 묶였다 (커밋 메시지) | 다음 커밋이 나머지를 묶었다 [없앰] | 과장 — 14R 판정 · 옮겨 적음(출처: 14R 검토자) | 기록 없음 · 미확인 — 15R-D 안 돌림 | 기록 없음 |
| 55 | `f6fbc05` 의 "188장 중 188장" | 그 커밋 시점 값은 185/185 이고 188 은 HEAD 값이다 (커밋 메시지) | — (주장) | 과장 — 14R 판정 · 옮겨 적음(출처: 14R 검토자) | 기록 없음 · 기록 없음 | 없음 |
| 56 | `2e87922` 의 "고치기 전 같은 도구가 **3개**를 잡아냈다" | 그 커밋의 시험은 `AREAS[0]` 한 장만 본다 — 전체도까지 보면 같은 격자에서 200개 (커밋 메시지) | — (주장) | 과장(축소 방향) — 14R 판정 · 옮겨 적음(출처: 14R 검토자) | 기록 없음 · 기록 없음 | 없음 |
| 57 | `2e87922` 의 "남은 것: 영역도의 가로겹침 13" | `thru` 도 남아 있었는데 안 적었다 (커밋 메시지) | — (주장) | 과장(불완전한 고백) — 14R 판정 · 옮겨 적음(출처: 14R 검토자) | 기록 없음 · 기록 없음 | 없음 |

**직전 수정이 만든 것.** 12라운드 커밋이 만든 42개가 어디에도 등록되지 않아 기본
점검에서 한 번도 돌지 않았다(#46). "재는 것을 늘려 놓고 **돌지 않는 자리에 두는 것**"
이 이 문서의 항목이 된 자리다.

**넷 중 하나는 쓰지 않고 멈췄다.** 제 몫의 파일을 다른 하나가 이미 만지고 있는 것을
보고, 덮어쓰는 대신 검증으로 돌아섰다. 그 판단이 이 라운드에서 가장 값나갔다 —
그쪽이 낸 것("경고 0" 을 반증한 퍼저, 아무것도 등록하지 않는 등록 줄)이 이번 라운드
산출 중 제일 쓸모 있었다. **동시에 고칠 때 겹치면 멈추는 편이 낫다.**

**이 라운드의 커밋 메시지 넷이 뒤에 판정된다.** #53~#57 이 그것이고, 넷 다 과장이거나
판정불가였다. 특히 #53("59개 중 21개가 빨강")은 **어떤 회귀를 넣었는지 커밋이 안
적어서** 지금도 재현되지 않는다 — "주장에 숫자를 적을 때는 그 숫자를 낸 조작도 같이
적는다" 가 여기서 규칙이 됐고, 14라운드가 그 규칙을 읽고도 같은 자리에 다시 빠진다.

### 14라운드 — 다섯 관점 동시 (2026-08-04, 미커밋)

**반영 커밋** 없음 (2026-08-04, 워킹 트리) · **붙은 사람** 5 갈래 동시(검토와 수정을 같은 사람이) · **시험** 101 → **141** (옮겨 적음, 출처: 15R 기록의 `all 141 passed`).

다섯 관점을 한 라운드에 붙였다 — 설치·배포 / 산출물 / 설정 / **시험의 반증가능성** /
주장 판정. 발견 42건으로 이 저장소 최다다. 새로운 것은 **다섯 중 하나에게 재는 장치만
맡긴 것**이다. 다른 넷이 제품 코드를 고치는 동안 그 하나는 시험이 반증 가능한지를
고친다 — 고치는 손과 재는 손이 같으면 고친 것을 확인해 주는 자가 고친 사람 것이
되기 때문이다. 그 하나가 census 를 떠 보니 다섯 카운터 중 **넷은 188장 어디서도 0 이
아닌 적이 없었다**: 규칙은 있는데 발동할 일이 없으니 카운터를 상수 0 으로 바꾸는 한
줄이면 그림 품질 보증이 통째로 조용해졌다(#91).

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 58 | 어느 영역에도 안 든 테이블 하나가 GraphML·PNG·SVG·HTML·docx 를 **통째로 0개**로 만든다 | `load_spec` 이 spec 오류 셋만 세고 네 번째 방향을 빠뜨려 `erd.py` 의 `pos[n]` 이 KeyError (`config.load_spec`) | 남은 테이블을 '기타' 영역이 받는다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M58`) | `spec: a table named in no area is placed, not a KeyError` |
| 59 | spec 의 `roles`·`layer_of`·`doc`·`derives` 에 잘못된 타입을 적으면 파이썬 내부가 나온다 | 4R 이 `areas` 에만 검사를 달았다 (`config.load_spec`) | 네 키를 함께 검사한다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M59`) | `spec: a key of the wrong type is explained for every key, not just areas` |
| 60 | 최상위 키를 오타 내면 spec 이 통째로 없는 것과 같아지는데 한 글자도 안 알린다 | 아는 키 일곱만 `.get()` 으로 집고 나머지는 존재조차 안 봤다 (`config.load_spec`) | 모르는 키를 이름으로 말한다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M60`) | `spec: a misspelled top-level key is named, not silently dropped` |
| 61 | `"areas": [["x/y", …]]` 하나면 PIL 이 없는 디렉터리에 쓰려다 죽고 도판·문서가 통째로 사라진다 | 자동 생성 코드만 파일명 안전을 봤고 **손으로 적는 spec 쪽**은 검사 밖이었다 (`config.load_spec`) | spec 쪽 코드도 파일명으로 쓸 수 있는지 본다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M61`) | `spec: an area code that cannot be a file name is refused…` |
| 62 | spec 문자열에 개행·제어문자가 있으면 `build_erd`·`build_docx` 가 죽고 HTML 만 살아남는다 | 4R 이 `schema.json` 쪽에서 고친 것과 같은 결함이 spec 경로에 남았다 (`config.load_spec`) | spec 문자열도 `clean()` 을 지난다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M62`) | `spec: a newline or control character in a spec string does not kill any builder` |
| 63 | `ERD_HTML_SVG=False`·`NO`·`off` 로 꺼도 켜지고, `ERD_HTML_STATS='0 '` 은 공백 하나로 도로 켜진다 | 다섯 자리가 저마다 `not in ('0','false','no')` 를 적었다 (`config.py`) | 규칙을 `config.env_flag` 하나로 모았다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M63`) | `config: every ERD_* boolean reads FALSE / No / off / "0 " the same as 0` |
| 64 | `ERD_MAX_AREAS=abc` 는 아무 말 없이 12 가 되고 `--5` 는 raw ValueError 로 죽는다 | `isdigit()` 한 줄에 셋이 걸려 있었다 (`config.py`) | 숫자가 아니면 이름을 대고 죽는다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M64`) | `config: an ERD_MAX_AREAS that is not a number says so instead of silently meaning 12` |
| 65 | `ERD_WORK=''` 하나로 `schema.json` 과 `out/` 이 부르는 사람의 cwd 에 흩어진다 | `os.environ.get(env, default)` 는 빈 문자열도 값이라 `Path('')` → `Path('.')` (`config._p`) | 빈 값이면 기본값으로 떨어진다 [옮김][^f-blank] | 과장 — 15R 이 `'  '`(공백)은 여전히 통한다고 확인 | 있음: 공백만 있는 값 · 반증됨(16R-D, `M65`) | `config: an empty or blank ERD_WORK falls back to $ERD_PROJ/erd-build, not the cwd` |
| 66 | 경로 환경변수 다섯이 잘못된 종류의 노드를 받으면 어느 변수 탓인지 없는 errno 가 그대로 나온다 | `mkdir`·`read_text` 의 예외를 그대로 흘렸다 (`config.py`) | 변수 이름을 대는 메시지로 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M66`) | `errors: every ERD_* path variable pointed at the wrong kind of node is a message…` |
| 67 | `ERD_EXCLUDE` 정규식 오류와 `ERD_PSQL` 따옴표 짝 오류가 변수 이름 없이 터진다 | 처음 쓰이는 자리까지 검사가 미뤄져 있었다 (`config.excluded`·`config.psql_cmd`) | 읽는 자리에서 검사한다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M67`) | `errors: a bad ERD_EXCLUDE regex and an unbalanced ERD_PSQL quote are messages` |
| 68 | `ERD_SCHEMAS` 가 비면 `where table_schema in ()` 라는 문법 오류 SQL 이 서버로 나간다 | 빈 조각을 걸러 `SCHEMAS=[]` 가 됐다 (`config.py`·`introspect.py`) | 이름을 대고 거절하고, 이름 속 따옴표도 이스케이프한다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M68`) | `introspect: an empty ERD_SCHEMAS is refused by name, not as a SQL syntax error` |
| 69 | `ERD_DEFAULT_PK` 가 DB 없이 그리는 사람에게는 영원히 무효인데 한 글자도 안 나온다 | `if n not in db: continue` 안쪽이라 접속이 없으면 루프가 한 번도 안 돌았다 (`parse_ddl.py`) | DB 없이도 채우거나, 왜 못 채웠는지 말한다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M69`) | `parse: ERD_DEFAULT_PK either fills the PK without a database or says why it did not` |
| 70 | 함께 배포하는 예제 spec 이 HTML 에서만 `ValueError` 로 죽는다 — docx 는 나오니 실패로 보이지도 않는다 | 표지 정보표 행을 docx 는 4칸, HTML 은 2칸으로 언팩했다 (`build_html.py`) | 규칙을 `build_erd.meta_cells` 한 자리에 모았다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 미확인 — 15R-D 안 돌림 | `artifacts: every shipped example spec builds all three outputs` |
| 71 | 한 영역이 두 DB 에 걸치면 요약표가 남의 DB 테이블을 세고, 어떤 DB 는 행 자체가 사라진다 | 영역의 DB 를 첫 테이블로 정하면서 개수는 테이블마다 제 db 로 셌다 (`build_html.py`) | 싣는 것과 세는 것을 같은 자리에서 뽑는다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 미확인 — 15R-D 안 돌림 | `html: the DB summary adds up to what the document actually lists` |
| 72 | 'of 11 tables' 라고 적힌 docx 에 4.x.x 절이 10개만 실린다 | 본문은 `AREAS` 를 돌고 표지·1.2·4장 머리말은 `len(SCHEMA)` 를 셌다 (`build_docx.py`) | `build_erd.doc_tables()` 한 자리에서 뽑는다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 미확인 — 15R-D 안 돌림 | `docx: the table count the document states matches the sections it lists` |
| 73 | HTML 목차와 FK 상호참조가 조용히 남의 테이블을 가리킨다 | `'tb_' + re.sub(r'[^a-zA-Z0-9_]','_',key)` 가 단사가 아니다 (`build_html.py`) | 충돌하면 꼬리를 붙여 갈랐다 [옮김][^f-anchor] | 거짓 — 15R 이 꼬리 번호가 불안정하다고 재현 | 있음: 꼬리 번호가 스키마가 자라면 옮겨 앉음 · 반증됨(16R-D, `M73`) | `html: every table gets its own anchor` |
| 74 | docx 제목이 '4.1.1 users · ' 로 끝난다 | 역할명이 비어도 구분자를 찍었다 (`build_docx.py`) | 비면 구분자도 안 찍는다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M74`) | `docx: a table with no role gets a heading with no dangling separator` |
| 75 | 제출용 docx 에서 `UNIQUE` 라는 스키마 사실이 통째로 사라진다 | 4장 컬럼표의 '구분' 칸이 PK·FK 만 알았다 (`build_docx.py`) | docx 도 UNIQUE 를 적는다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M75`) | `docx: a column made unique says so` |
| 76 | `ERD_HTML_OUT` 이 디렉터리를 가리키면 변수 이름 없는 `IsADirectoryError` 가 그대로 나온다 | `Path(os.environ.get(...))` 를 그대로 열었다 (`build_html.py`) | 변수 이름을 대는 메시지로 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M76`) | `errors: ERD_HTML_OUT pointed at a directory is a message, not a traceback` |
| 77 | 자기참조 팔이 테이블 한가운데에 심긴다 | `loop_room()` 이 완전히 왼쪽·완전히 오른쪽만 보고 x 구간이 걸치는 셋째 갈래를 그냥 무시했다 (`erd.loop_room`) | 걸치는 것도 clamp 한다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M77`) | `render: a self-reference arm clears a table whose x range overlaps its own` |
| 78 | 자기참조 루프가 옆 테이블을 지나가도 선↔테이블 `0` 이 찍힌다 | 루프를 검증 세그먼트에 안 넣었다 — 그 한 줄을 `[]` 로 바꿔도 101개가 통과했다 (`erd.draw_erd`) | 루프를 세그먼트에 넣고, 관통이 불가피한 그림으로 못박았다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M78`) | `render: a self-reference loop is measured, not only drawn` |
| 79 | 방이 없을 때 팔의 경계를 600px 바깥으로 벌려 남의 테이블 속에 자리를 잡는다 | `x1-600`·`x2+600` 으로 창을 억지로 만들었다 (`erd.py`) | 방이 없으면 제 테이블에 붙인다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 미확인 — 16R-D 조작(`arm = want`) 초록 | `render: a self-reference arm with no room hugs its own table` |
| 80 | `ERD_FONT_BOLD` 가 폰트 아닌 파일이면 PIL 안에서 트레이스백, 없는 파일이면 아무 말 없이 regular 로 떨어진다 | regular 은 `exists()`+`_usable()` 을 보는데 볼드는 `exists()` 만 봤다 (`erd._pick_font`) | 볼드도 둘 다 본다 [옮김][^f-half] | 과장 — 15R 이 후보 목록 갈래는 여전히 안 잰다고 확인 | 있음: 후보 목록 갈래 · 반증됨(16R-D, `M80`) | `errors: an unusable ERD_FONT_BOLD is explained the same way ERD_FONT is` |
| 81 | `scripts/` 를 통째로 지워도 `install.sh --check` 가 초록으로 지나간다 | `[ -n "$ST" ]` 가 6번 절을 통째로 지웠다 (`install.sh`) | 없으면 non-zero 로 죽는다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M81`) | `install: --check on a tree with no scripts/ exits non-zero` |
| 82 | README 가 안내하는 `git clone` 흐름에서 `--check` 가 설치본이 아니라 클론을 점검한다 | 점검 대상 경로를 호출 위치로 잡았다 (`install.sh`) | 설치된 트리를 점검한다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M82`) | `install: --check run from the clone reports the installed tree, not the clone` |
| 83 | `--check` 가 frontmatter 없는 `SKILL.md` 를 통과시킨다 | `[ -f "$DEST/SKILL.md" ]` — 존재만 봤다 (2R 이 넣은 그 한 줄) (`install.sh`) | 내용을 본다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M83`) | `install: --check rejects a SKILL.md without frontmatter` |
| 84 | `-` 로 시작하는 메시지가 bash `printf` 의 옵션으로 읽혀 깨진다 | 형식 문자열을 그대로 넘겼다 (`install.sh`) | `--` 로 끊는다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M84`) | `install: every message key renders in all four languages` |
| 85 | 문서 넷이 "아무것도 바꾸지 않는다" 고 적은 `--check` 가 부르는 사람의 cwd 를 바꾼다 | 회귀 시험이 그 말을 안 쟀다 (`install.sh:7`·`INSTALL.md:32`·`INSTALL.ko.md:101`·`README.md:86`) | cwd 가 바이트까지 같은지 잰다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M85`) | `install: --check leaves the caller's cwd byte-identical` |
| 86 | `--check --project` 는 충돌 검사가 없어 마지막 것이 이겨 쓰기 모드로 돈다 | 모드 인자에 충돌 검사가 없었다 (`install.sh`) | 충돌하면 거절한다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M86`) | `install: --check never writes, whatever else is on the command line` |
| 87 | "Permission denied" 바로 다음 줄에 "✓ copied" 가 찍힌다 | `cp -R` 의 결과를 안 봤다 (`install.sh`) | 실패하면 안 찍는다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M87`) | `install: a failed copy never prints "copied"` |
| 88 | `install.sh` 한 개만 든 디렉터리에서 `--here` 가 그냥 지나간다 | `SKILL.md` 확인이 `MODE=check` 안에만 있었다 (`install.sh`) | 모드와 무관하게 본다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M88`) | `install: --here in a non-skill directory exits non-zero` |
| 89 | `--check` 가 `tail -1` 만 읽어 "몇 개는 안 돌렸다" 는 줄을 사용자에게 안 보여 준다 | 집계 윗줄을 안 읽었다 (`install.sh`) | 그 줄도 보여 준다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M89`) | `install: --check reports the cases the regression test did not run` |
| 90 | `python-docx==0.8.11`(선언 하한 미만)에서도 `--check` 가 초록이다 | `import docx` 만 봤다 (`install.sh`) | `requirements.txt` 의 하한을 잰다 [없앰] | 미검증 — 개별 판정 기록 없음 | 확인된 것 없음 · 반증됨(16R-D, `M90`) | `install: --check measures the version floor in requirements.txt` |
| 91 | 카운터를 상수 0 으로 바꿔도 101개가 전부 통과한다 | 다섯 카운터 중 넷이 188장 어디서도 0 이 아닌 적이 없어 규칙이 발동할 일이 없었다 (`selftest.py`) | 세는 함수를 산출물을 통하지 않고 직접 건다 [옮김][^f-feed] | 과장 — 15R 이 굶기기·실제경로·배선 셋으로 우회 | 있음: 먹이와 배선은 여전히 무측정 · 반증됨(15R-D, `V4`·`V18`·`V19`) | `verify: the overlap counter still counts…` 외 3개 |
| 92 | `sweep_verify()` 호출을 통째로 지워도 아무 데서도 안 붉는다 | 재는 장치가 빠진 것과 잴 것이 없는 것을 구분 못했다 (`selftest_kit.main`) | 통과한 케이스 수와 훑은 케이스 수를 맞춘다 [없앰] | 참 — 15R-D 확인 | 없음 · 반증됨(15R-D, `S-sweep`: 호출 삭제 → `the diagram sweep ran on 0 of 147`) | `selftest.py` 집계 앞 대조 한 줄 |
| 93 | 글로브 등록이 어긋나면 39개가 사라지는데 종료코드 0 에 마지막 줄은 초록 `all 62 passed` | '조용히 두 배' 는 막고 '조용히 0' 은 아무도 안 막았다 (`selftest_kit.load_extras`) | 옆에 파일이 없으면·0개를 올리면·신고와 어긋나면 죽는다 [옮김][^f-shrink] | 과장 — 15R 이 '조용히 줄기' 로 우회 | 있음: 조용히 줄기 · 반증됨(15R-D, `V16`) | `selftest: every case file beside the kit is registered and says how many it added` |
| 94 | `verify_recs()` 의 "기록이 없으면 실패" 를 `return []` 로 바꿔도 전부 초록이다 | 재는 장치를 재는 것이 없었다 (`selftest_kit.verify_recs`) | 케이스 하나로 못박았다 [없앰] | 참 — 15R-D 확인 | 없음 · 반증됨(15R-D, `S-logzero`: 4개 빨강) | `selftest: the harness fails where it says it fails` |
| 95 | `case()` 의 이름 중복 검사를 지워도, `eq()` 가 절대 실패하지 않게 해도 전부 초록이다 | 같은 자리 (`selftest_kit.case`·`eq`) | 프로브 케이스 하나로 못박았다 [옮김][^f-probe] | 과장 — 15R 이 목록·사전·긴 바늘로 우회 | 있음: 프로브가 좁음 · 반증됨(15R-D, `S-dup`·`V7`·`V8`) | `selftest: the harness fails where it says it fails` |
| 96 | `if ver < MIN_PG` 관문이 한 번도 지나가지 않는다 | 가짜 psql 이 그 앞 `isdigit()` 분기에서 먼저 걸린다 (`introspect.py`) | 하한을 내려 잡는 쪽도 잰다 [없앰] | 참 — 15R-D 확인 | 없음 · 반증됨(15R-D, `S-minpg`: `MIN_PG` → 0) | `introspect: a server too old to answer is refused, not half-read` |
| 97 | `config._CTRL` 을 통째로 비워도 초록이다 — 표가 실제로 일하는 문자를 fixture 가 안 썼다 | fixture 가 `\n`·`\x1e`·`\x1f`·`\x0b` 만 썼는데 그 넷은 `str.split()` 이 이미 공백으로 친다 (`selftest_history.py`) | `\x00`·`\x07`·`\x7f` 를 fixture 에 넣었다 [없앰] | 참 — 15R-D 확인 | 없음 · 반증됨(15R-D, `S-ctrl`: `_CTRL = {}`) | `errors: a control character in a name does not kill the build` |
| 98 | "graphml carries the ETL flows **the diagram draws**" 가 그림 쪽을 안 본다 — 그림에서 ETL 을 지워도 초록 | 이름이 검사를 대신했다 (`selftest_history.py`) | 그림 쪽도 재게 했다 [없앰] | 참 — 14R 자백 | 없음 · 반증됨(16R-D, `M98`) | `artifacts: graphml carries the ETL flows the diagram draws` |
| 99 | "6개는 안 돌렸다" 는 줄을 지워도 아무 일이 없다 | `NOTES.append(...)` 를 아무도 안 봤다 (`selftest_history.py`) | 그 줄을 확인하는 케이스를 넣었다 [옮김][^f-db] | 거짓 — 15R 이 자기일관 거짓말로 우회 | 있음: 세는 쪽과 확인하는 쪽이 같은 함수 · 반증됨(15R-D, `V24`) | `selftest: the line that says what was skipped is itself checked` |

**직전 수정이 만든 것.** 13라운드가 새로 넣은 넷이 **전부 저를 지키는 케이스 없이**
들어왔다. ① `sweep_verify()` 는 호출을 통째로 지워도 안 붉었다(#92). ② `RENDER_ALLOW`
는 **상한**이라 5→50 으로 벌리고 11라운드 회귀를 함께 넣어도 101개가 전부 통과했다.
③ `verify_recs()` 의 "기록이 없으면 실패" 를 `return []` 로 바꿔도 전부 초록이었다
(#94). ④ 글로브 등록은 어긋나면 39개가 사라지는데 종료코드 0 에 마지막 줄은 초록
`all 62 passed` 였다(#93). 그리고 도구 자신의 도구 — `eq()`·`has()`·`case()` 의 중복
검사 — 도 죽여 보니 아무 말이 없었다(#95).

**이 라운드가 스스로 적은 자백이 맞았다.** "넣은 것마다 뮤턴트를 실제로 넣고 빨강이
뜨는 것을 보고서야 적었다" 고 썼는데, 15라운드 감사자가 그 장치를 **8개로 우회**했다.
거짓말이어서가 아니다. 넣어 본 뮤턴트가 **제가 새로 넣은 케이스를 겨냥한 것들**이었고,
그 케이스들이 재지 않는 방향은 뮤턴트도 만들지 않았다. 자기가 쓴 시험을 자기가 만든
뮤턴트로 재면 이 각도는 원리상 안 나온다.

**그리고 하네스를 안 남겼다.** 뮤테이션 91개·census 188장·"22개 생존" 같은 수치를
적으면서 그것을 낸 도구를 트리에 남기지 않아, 15라운드 검증자가 M 번호를 **하나도**
재현하지 못했다. 13라운드가 이름 붙인 잘못을 14라운드가 읽고 같은 자리에 다시 빠진
셈이다 — **문서에 이름이 붙어 있다는 것으로는 안 막힌다.**

**표에 42행뿐이다.** `## 라운드 요약` 이 이 라운드의 발견을 45건이라 적어 온 근거는
14라운드 검토자의 집계인데, 세 건의 내역이 15·16라운드 어느 쪽에도 없다. **모르는
것을 채우지 않는다**는 규칙에 따라 세 줄을 지어내지 않는다 — `## 남은 버그` 의 `B26`
이 그 자리다.

### 15라운드 — 검증, 주장하는 자와 뮤턴트를 만드는 자를 가른다 (2026-08-04, 미커밋)

**반영 커밋** 없음 (2026-08-04, 워킹 트리) · **붙은 사람** 독립 검증 3 + 수정 4 · **시험** 141 → **161** (실측, 병렬 8회 전부 `all 161 passed`).

**판정만을 관점으로 삼은 첫 라운드**다. 셋이 14라운드 수정자들의 주장을 적대적으로
감사하고, 넷이 그 감사가 낸 것을 다시 고쳤다. 감사 결과가 이 문서에서 가장 값나가는
수치다 — 14라운드가 "이제 잡힌다" 고 33행 표로 적은 재는 장치를 **8개가 그대로
우회**했고, 전부 `all 141 passed` 로 빠져나갔다. 우회 모양이 세 부류인데 셋 다 이
문서에 이미 이름이 있다: **단위 케이스가 먹이를 자기가 넣는다**(#100·#101),
**배선을 이름으로 본다**(#102), **도구를 재는 케이스가 도구가 만든 것을 잰다**(#103).

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 100 | `lab_boxes` 를 `placed[:0]` 로 굶기면 라벨 두 항목이 영구히 0 인데 `all 141 passed` | 단위 케이스가 세는 함수의 **먹이를 자기가 넣는다** (`selftest.py` 의 `_counting_ns` 계열 셋) | 실제 판을 그리며 `draw_erd` 프레임에서 먹이 크기를 읽는다 [없앰] | 참 — 15R-D 가 `mutants.py V4` 로 재현 | 없음 · 반증됨(`V4` → 2개 빨강) | `verify: the counters the drawing path really uses are fed, and still bite` |
| 101 | `lab_hits()` 가 실제 도판 경로에서만 0 을 돌려줘도 단위 시험은 통과한다 | 단독 네임스페이스에 없는 이름(`pos`)으로 갈래를 판다 — 같은 뿌리 (`erd.lab_hits`) | 그 판이 실제로 부른 함수 객체를 자유변수 셀 치환으로 직접 부른다 [없앰] | 참 — `mutants.py V18` 로 재현 | 없음 · 반증됨(`V18` → 1개 빨강) | `verify: the counters the drawing path really uses are fed, and still bite` |
| 102 | `lab_hit = 0 * sum(…)` 로 배선을 상수로 만들어도 배선 케이스가 통과한다 | `names_of()` 가 AST 의 **이름 집합**만 봐서 상수 곱·`and False`·`[:0]` 을 구분 못했다 (`selftest.py`) | 이름 대신 **모양**을 못박는다 [없앰] | 참 — `mutants.py V19` 로 재현 | 없음 · 반증됨(`V19` → 1개 빨강) | `verify: the five numbers in the record come from the counting functions` |
| 103 | `_counting_ns()` 를 손수 구현으로 갈아치우면 단위 셋이 `erd.py` 대신 그 스텁을 채점한다 | `_counting_ns()` 자체를 재는 것이 하나도 없었다 (`selftest.py`) | `co_filename`·`co_firstlineno` 대조 + 손댄 소스를 먹여 동작이 따라 바뀌는지 [없앰] | 참 — `mutants.py V6b` 로 재현 | 없음 · 반증됨(`V6b` → 1개 빨강) | `verify: the counting namespace grades erd.py itself, not a stand-in` |
| 104 | `eq()` 가 list·tuple·dict·set 에 절대 실패하지 않게 해도 전부 초록이다 | 프로브가 `eq(1, 2, 'x')` 하나뿐이라 값의 모양을 못 덮었다 (`selftest.py`) | 목록·튜플·사전·집합·None 과 역방향을 한 줄씩 [없앰] | 참 — `mutants.py V7` 로 재현 | 없음 · 반증됨(`V7`) | `selftest: the harness fails where it says it fails` |
| 105 | `has()` 가 네 글자 이상 바늘에 절대 실패하지 않게 해도 전부 초록이다 | 프로브가 `has('abcdef','zzz','x')` 하나뿐이었다 (`selftest.py`) | 7글자 바늘·5000자 건초더미·빈 건초더미와 역방향 [없앰] | 참 — `mutants.py V8` 로 재현 | 없음 · 반증됨(`V8`) | `selftest: the harness fails where it says it fails` |
| 106 | `@case` 셋을 떼고 `EXPECT_CASES` 를 함께 내리면 `all 138 passed` 로 **조용히 줄어든다** | 신고제는 '조용히 0' 만 막는다 (`selftest_kit.load_extras`) | 신고를 필수로 하고, 입구 파일에 `CASE_FLOOR` 하한을 뒀다 [옮김][^f-floor] | 참 — `mutants.py V16` 로 재현 | 있음: 파일 이름을 바꾸면 하한이 사라짐 · 반증됨(`V16` → `1 of 158 failed`) | `selftest: every case file beside the kit is registered and says how many it added` |
| 107 | `_db_case_count()` 가 0 을 세도 초록이고 사용자에게는 `0 cases need a real server` 가 찍힌다 | 세는 함수와 그것을 확인하는 케이스가 **같은 함수**를 읽어 자기일관이다 (`selftest_history.py`) | 확인하는 쪽은 줄 훑기가 아니라 AST 로 `@case` 데코레이터를 센다 [없앰] | 참 — `mutants.py V24` 로 재현 | 없음 · 반증됨(`V24`) | `selftest: the line that says what was skipped is itself checked` |
| 108 | 시험을 두 벌 동시에 돌리면 i18n 케이스가 간헐로 붉고, 죽으면 `scripts/lang/zz.py` 가 트리에 남는다 | 시험이 검사 대상 트리에 파일을 쓴다 — 시험 벌 전체의 유일한 공유 가변 상태 (`selftest_history.py`) | 카탈로그 트리째 임시 디렉터리에 복사해 거기서 잰다 [없앰] | 참 — 병렬 16회 중 7회(옮겨 적음, 출처: 15R 검증자) · 15R-D 실측 8회 중 2회 | 확인된 것 없음 · 반증됨(되돌린 사본 병렬 8회 → 2회 빨강, 고친 것 8회 → 0회) | `i18n: a catalog that cannot be read says so instead of quietly speaking English` |
| 109 | 이 문서에 날짜가 한 줄도 없어 "지금 131개" 가 언제의 131 인지 아무 데도 없다 | 표에 날짜 열이 없었다 (`REVIEW-LOG.md`) | 반영 커밋의 author date 를 시각까지 넣고 규칙으로 못박았다 [없앰] | 참 — `grep -nE "20[0-9]{2}[-./년]" REVIEW-LOG.md` 가 0건 | 없음 · 반증 안 됨 | 없음 (문서는 시험이 없다) |
| 110 | `지금 있는 것` 의 "131개" 가 두 라운드째 틀린 값이었다 (실제 161) | 라운드가 끝나기 전에 재고 적었다 (`REVIEW-LOG.md`) | 라운드가 끝난 뒤 실측해 161 로 고치고 측정 날짜를 붙였다 [없앰] | 참 — `python3 scripts/selftest.py` → `all 161 passed` | 없음 · 반증 안 됨 | 없음 |
| 111 | 14라운드 기록의 세 자리가 **확인 필요**인 채로 남았다 | 라운드가 끝나기 전에 문서를 써서 (문서 자신의 규칙 위반) (`REVIEW-LOG.md`) | 셋을 채우고, "확인 필요는 라운드가 끝난 뒤 남으면 안 된다" 를 규칙으로 넣었다 [없앰] | 참 — `grep 확인 필요` 가 규칙 문장 말고는 0건 | 없음 · 반증 안 됨 | 없음 |
| 112 | 라운드별 표가 표의 모양만 하고 있다 — 한 칸에 문단이 통째로 들어간다 | 발견 단위가 아니라 라운드 단위로 적었다 (`REVIEW-LOG.md`) | 주 표를 발견 단위로 갈랐다 — 16R 정리가 그것을 라운드별 절로 다시 나눴다 [없앰] | 참 — 사용자 지적 (한 줄 439자, 15R 줄은 수천 자) | 없음 · 반증 안 됨 | 없음 |
| 113 | spec 의 테이블 자리에 dict 를 적으면 `TypeError: unhashable type` 이 그대로 나온다 | 14R 이 검사를 달았는데 지켜 주는 항목이 하나도 없어 지워도 141개가 초록이었다 (`config.load_spec`) | 케이스를 붙였다 [없앰] | 참 — 그 파일이 조작과 결과를 적음 · 옮겨 적음(출처: 수정자 A) | 확인된 것 없음 · 반증됨(16R-D, `M113`) | `spec: a table name that is not a string is a message, not an unhashable TypeError` |
| 114 | 같은 파일 이름이 될 영역 코드 둘이 그림 한 장을 덮어쓴다 (`A`/`a`, `Ä`/`Ä`) | 14R 이 '뿌리는 spec 로더' 라고 보고만 하고 넘어갔다 (`config.load_spec`) | 파일 이름이 같아질 코드를 앞에서 거절한다 [없앰] | 참 · 옮겨 적음(출처: 수정자 A) | 확인된 것 없음 · 반증됨(16R-D, `M114`) | `spec: two area codes that would write the same file name are refused` |
| 115 | spec 이 영역 코드를 소문자로 적으면 '기타' 영역이 대문자 코드를 받아 가 **문서가 남의 그림을 싣는다** | `_free_code` 가 언제나 대문자인 `_code(i)` 만 `used` 와 비교했다 — **14R 이 만든 것** (`erd.py`) | 대소문자를 접어서 비교한다 [없앰] | 참 · 옮겨 적음(출처: 수정자 A) | 확인된 것 없음 · 반증됨(16R-D, `M115`) | `spec: an area code written in lower case keeps a diagram of its own` |
| 116 | `ERD_MAX_AREAS=1` 인데 영역이 3개 나오고 화면에는 한 글자도 안 나온다 | 상한이 자동 분류 가지에만 걸려 있었다 (`config.py`) | 다 그리되 넘었다고 말한다 [없앰] | 참 · 옮겨 적음(출처: 수정자 A) | 확인된 것 없음 · 반증됨(16R-D, `M116`) | `config: ERD_MAX_AREAS says so when a spec asks for more areas than the cap` |
| 117 | `import` 하나로 부르는 사람의 cwd 에 `erd-build/out` 이 생긴다 | `config.py:92` → `config.py:76` 의 `path.mkdir(parents=True)` 가 import 시점에 돈다 | import 만으로는 아무것도 안 만든다 [없앰] | 참 — 검증자가 `os.mkdir` 후킹으로 확인 · 옮겨 적음(출처: 15R 검증자) | 확인된 것 없음 · 반증됨(16R-D, `M117`) | `config: an import alone creates nothing in the caller's directory` |
| 118 | `ERD_PSQL='psql "unclosed'` 하나로 DB 를 안 쓰는 네 스크립트가 전부 rc 1 이 된다 | `ERD_PSQL` 이 `config` import 시점에 분해됐다 (`config.py`) | 묻는 스크립트만 멈춘다 [없앰] | 참 — 14R 수정자 자신이 신고, 검증자 확정 · 옮겨 적음(출처: 15R 검증자) | 확인된 것 없음 · 반증됨(16R-D, `M118`) | `config: an unusable ERD_PSQL stops the scripts that ask the database, not the ones that never do` |
| 119 | `ERD_REF_SCHEMA`/`ERD_REF_TABLES` 의 따옴표가 **살아 있는 서버로 나가는 SQL** 을 연다 | `introspect` 쪽에는 케이스가 있는데 `parse_ddl` 쪽은 0건이었다 (`parse_ddl._lit`) | 케이스를 붙였다 [없앰] | 참 — 141개가 전부 초록이었다 · 옮겨 적음(출처: 수정자 A) | 확인된 것 없음 · 반증됨(16R-D, `M119`) | `parse: a quote in ERD_REF_SCHEMA or ERD_REF_TABLES is escaped…` |
| 120 | `ERD_REF_TABLES` 만 주면 조용히 무시된다 — 문구까지 있는데 아무도 안 부른다 | `grep -rn 'ref_tables_ignored' scripts/selftest*.py` 가 0건 (`parse_ddl.py`) | 케이스를 붙였다 [없앰] | 참 · 옮겨 적음(출처: 수정자 A) | 확인된 것 없음 · 반증됨(16R-D, `M120`) | `parse: ERD_REF_TABLES without ERD_REF_SCHEMA is named, not silently ignored` |
| 121 | `HTML tables 3` 이라고 찍히는데 파일에 실린 절은 2개다 | `log.html_done` 의 `tables=` 가 `len(SCHEMA)` 를 셌다 (`build_html.py`) | 찍힌 수와 실린 절을 맞췄다 [없앰] | 참 — 뮤턴트 L · 옮겨 적음(출처: 수정자 B) | 확인된 것 없음 · 반증됨(16R-D, `M121`) | `html: the line the run prints counts the sections the file carries` |
| 122 | 표지가 `Columns 9`·`FKs 1` 이라 적는데 문서가 싣는 것은 5컬럼·0관계다 | 14R 의 docx 케이스가 표지 `Tables` 와 4장 절 수만 봤다 (`build_docx.py`) | 문서가 수를 말하는 네 자리를 전부 같은 집합에 건다 [없앰] | 참 — 뮤턴트 DD·FF · 옮겨 적음(출처: 수정자 B) | 확인된 것 없음 · 미확인 — 15R-D 안 돌림 | `docx: every count the document states is the count of what it lists` |
| 123 | 절이 없는 테이블로 가는 죽은 링크가 HTML 에 실린다 | 14R 의 앵커 케이스가 **모든 테이블이 실리는** 스키마를 써서 그 자리를 안 밟았다 (`build_html.col_flags`) | 부모를 영역에서 빼서 죽은 링크가 생길 수 있게 잰다 [없앰] | 참 — 뮤턴트 U · 옮겨 적음(출처: 수정자 B) | 확인된 것 없음 · 반증됨(16R-D, `M123`) | `html: a reference link always lands on a section this file has` |
| 124 | `#tb_x_y` 가 스키마가 자라면 어제와 다른 테이블로 간다 | 앵커 꼬리 번호가 **자리를 세는 값**이다 — **14R 이 만든 것** (`build_html.py`) | 앵커를 테이블 키만으로 정한다 [없앰] | 참 · 옮겨 적음(출처: 수정자 B) | 확인된 것 없음 · 미확인 — 15R-D 안 돌림 | `html: an anchor is decided by the table key alone` |
| 125 | `doc.mapping`·`doc.open_items` 로 손으로 쓴 장이 docx 에는 있고 HTML 에는 없다 | 14R 이 셋을 하나의 근거('매체 차이')로 함께 넘겼는데 그 근거는 `derives` 에만 참이다 (`build_html.py`) | 표로만 실리는 둘은 HTML 에도 싣는다 [없앰] | 참 · 옮겨 적음(출처: 수정자 B) | 확인된 것 없음 · 반증됨(16R-D, `M125`) | `html: the chapters a spec writes by hand reach both documents` |
| 126 | `as_file` 폴백 경로가 카탈로그를 안 거친 영어를 뱉는다 — 다국어 원칙의 유일한 구멍 | `build_html.py` 의 `except ImportError:` 사본 — **14R 이 만든 것**, 이 트리에서는 도달 불가능한 죽은 코드 | 폴백을 지우고 소스에서 규칙 사본을 센다 [없앰] | 참 · 옮겨 적음(출처: 수정자 B) | 확인된 것 없음 · 반증됨(16R-D, `M126`) | `build: every line the builders put in front of a person goes through the catalog` |
| 127 | `ERD_STALE=true`·`on`·`y` 가 조용히 안 먹어 문서가 안 나오고 멈춘다 | 14R 이 다섯 자리를 `env_flag` 로 묶을 때 여기만 빠졌다 (`build_erd.require_fresh`) | `env_flag` 에 얹고 모드 낱말 'warn' 만 따로 둔다 [없앰] | 참 · 옮겨 적음(출처: 수정자 B) | 확인된 것 없음 · 반증됨(16R-D, `M127`) | `errors: ERD_STALE follows the one yes/no rule, plus its own mode word` |
| 128 | 자기참조 팔의 띠를 좁혀 보는 반복문이 **살아 있는데 지켜지지 않는다** — 통째로 죽여도 초록 | 14R 이 '빈 range 라 안 돈다' 고 적었으나 계측하면 시험 한 벌에서 44번 돈다 (`erd.py`) | 그 반복문을 지키는 케이스를 넣었다 [없앰] | **거짓** — 14R 의 "빈 range" 주장을 15R 이 계측으로 반증 · 옮겨 적음(출처: 수정자 C) | 확인된 것 없음 · 반증됨(16R-D, `M128`) | `render: a self-reference arm with no room in its own band narrows the band` |
| 129 | `if hi - lo < 1:` 보호선이 selftest 184 프로세스 + 퍼저 120 프로세스에서 **한 번도 실행되지 않았다** | 결과에 흔적이 없는 줄이라 결과만 보는 시험으로는 반증할 수 없다 (`erd.py`) | `sys.settrace` 로 그 줄의 지역 변수를 본다 [없앰] | 참 · 옮겨 적음(출처: 수정자 C) | 확인된 것 없음 · 반증됨(16R-D, `M129`) | `render: the bounds handed to slot() for a self-reference arm are never empty` |
| 130 | 후보 목록 갈래에서 볼드 폰트의 `and _usable(bp, bi)` 를 지워도 전부 초록이다 | 14R 이 그렇게 고쳤는데 지키는 케이스가 없었다 — **고친 것의 절반이 반증 불가능했다** (`erd._pick_font`) | 후보 목록 갈래도 잰다 [없앰] | 참 · 옮겨 적음(출처: 수정자 C) | 확인된 것 없음 · 반증됨(16R-D, `M130`) | `errors: a bold font from the candidate list is opened before it is picked` |
| 131 | `ERD_WORK='  '`(공백만) 이 값으로 통해 cwd 에 이름이 공백인 디렉터리가 생긴다 | 14R 판이 빈 문자열만 재고 `_p` 의 `raw.strip()` 은 안 쟀다 (`config._p`) | 공백만 있는 값도 기본값으로 [없앰] | 참 · 옮겨 적음(출처: 수정자 A) | 확인된 것 없음 · 반증됨(16R-D, `M65`) | `config: an empty or blank ERD_WORK falls back to $ERD_PROJ/erd-build, not the cwd` |
| 132 | `env_flag` 의 `return default` 를 `return True` 로 바꿔도 초록이다 — **모르는 값**을 재는 자리가 없었다 | 14R 이 규칙은 모았는데 모르는 값 방향을 안 쟀다 (`config.env_flag`) | 모르는 값도 케이스에 넣었다 [없앰] | 참 · 옮겨 적음(출처: 수정자 A) | 확인된 것 없음 · 반증됨(16R-D, `M63`) | `config: every ERD_* boolean reads FALSE / No / off / "0 " the same as 0` |

**직전 수정이 만든 것.** 14라운드가 새로 만든 것이 넷이다. ① 영역 코드 대소문자
충돌로 **문서가 남의 그림을 실었다**(#115 — `_free_code` 가 언제나 대문자인 `_code(i)`
만 `used` 와 비교했다). ② 앵커 충돌을 **꼬리 번호**로 풀어서 스키마가 자라면
`#tb_x_y` 가 어제와 다른 테이블로 갔다(#124). ③ `as_file` 폴백에 카탈로그를 안 거친
영어가 하드코딩됐다(#126). ④ `_db_case_count` 가 **자기일관적 거짓말**을 하게 됐다 —
세는 함수와 그것을 확인하는 케이스가 같은 함수를 읽어, `0 cases need a real server`
가 찍히는데도 초록이었다(#107). 그리고 `ERD_WORK` 빈 값 수정은 **빈 문자열만** 재고
공백만 있는 값은 안 쟀다(#131).

**같은 자리에 15라운드도 서 있다.** 표에서 15라운드 행이 `반증됨` 이라고 말하는
여덟은 15라운드 수정자가 *스스로 넣어 보고 빨강을 확인한* 것이고, 그 여덟 밖에 무엇이
있는지는 다음 감사자가 판정할 몫이다. 하네스(`scratchpad/r15-testkit/mutants.py`)를
남긴 것은 그래서다. 같은 이유로 다른 갈래가 고친 것은 `미확인 — 15R-D 안 돌림` 으로
적었다 — 그 수정자들이 제 파일에서 뮤턴트를 돌렸다고 적어 두었지만, **내가 돌린 것이
아니다.** 그 56행을 16라운드가 직접 돌린다.

**새 모양 하나가 여기서 이름을 얻었다.** *시험이 검사 대상 트리에 쓰면 시험은 저희끼리
싸운다*(#108). i18n 케이스 하나가 `scripts/lang/zz.py` 를 트리에 쓰고 지웠는데, 시험
벌 전체에서 **공유 가변 상태를 쓰는 유일한 자리**였다. 순차로는 아무 일이 없고 동시에
돌리면 그 케이스만 간헐로 붉었다(병렬 16회 중 7회). 규칙 한 줄로 둔다 — **시험은
검사 대상 트리에 아무것도 쓰지 않는다.**

### 16라운드 — 다섯 갈래, 그리고 `미확인` 56행을 직접 되돌린다 (2026-08-04, 미커밋)

**반영 커밋** 없음 (2026-08-04, 워킹 트리) · **붙은 사람** 수정 5 갈래(A~E) · **독립 검증 0** · **시험** 161 → **181** (2026-08-04 실측).

다섯이 서로 다른 파일을 동시에 고쳤다 — A 는 재는 쪽(`selftest*.py`), B 는
산출물(`build_*.py`·`merge_schemas.py`), C 는 설정(`config.py`·`parse_ddl.py`·`lang/*`),
D 는 문서(`README*`·`SKILL*`·`INSTALL*`·이 파일), E 는 렌더(`erd.py`). D 갈래의 관점은
**문서-코드 대조**였다: 15라운드 수정이 문서에 반영됐는지 열 문서를 코드와 맞춰 읽고,
동시에 `회귀 여부` 의 `미확인 — 15R-D 안 돌림` 56행을 **제 손으로 조작을 만들어**
되돌려 봤다. 규칙은 하나다 — 트리를 통째로 복사하고, 그 사본에 **치환 한 건**을 넣고,
그 행이 이름 붙인 케이스만 골라 돌린다(`python3 scripts/selftest.py '<케이스 이름 일부>'`).
**조작 48건 중 47건이 빨강, 1건이 초록**이었고 그 47이 49행을 덮는다 — #131·#132 는
각각 #65·#63 과 **같은 케이스**가 지키므로 같은 조작이 함께 빨갛게 한다. 조작 표와
실행 기록은 워크트리 밖(scratchpad `r16-docs/mutants.py`)에 있고, 조작 이름 `M<행번호>`
는 그 표의 키다.

**아래 표의 11행은 D 갈래 발견만이다.** 다른 넷의 발견은 그 사람들의 말이라 옮겨
적으면 이 문서가 네 번 틀렸던 바로 그 모양('좁게 재고 넓게 쓴다')이 된다. 그래서
**번호를 새로 세우지 않고**, 넷이 무엇을 했는지는 바로 아래 갈래별 문단에 `옮겨
적음(출처: 16R 수정자 X 보고)` 으로 적고, 넷이 **못 고쳤거나 안 고쳤다고 스스로 신고한
것**은 전부 `## 남은 버그` 로 내렸다. 그 절이 이 라운드의 실질적인 산출물이다.

| # | 발견 (증상 한 줄) | 원인 (파일:줄) | 수정 | 검증 결과 | 회귀 여부 | 회귀 시험 |
|---|---|---|---|---|---|---|
| 133 | `INSTALL.md`·`.ko`·`.ja`·`.es` 의 예시 출력이 `✓ all 131 passed` — 두 라운드 낡은 값이다 | 같은 숫자를 다섯 자리에 손으로 적어 두고, 15R 이 `REVIEW-LOG` 하나만 고쳤다 (`INSTALL*.md`) | 다섯 자리를 실측값으로 맞추고, 문서가 적은 수를 등록 수와 대조하는 케이스를 넣었다 [없앰] | 참 — 16R-D 가 `grep` 으로 네 파일 모두 확인 | 있음: 라운드가 케이스를 늘리면 다섯 자리를 함께 고쳐야 한다 · 반증됨(16R-D, `D2-131`·`D2-inst`) | `docs: the case counts the documents state are the counts a run produces` |
| 134 | `ERD_MAX_AREAS` 를 적어도 spec 이 영역을 적으면 그 수가 안 지켜지는데 SKILL 은 '문서 전체 기준' 이라고만 적는다 | 13R 이 자동 분류 가지만 고치고 문서는 두 가지를 구분하지 않았다 (`SKILL.md`·`SKILL.ko.md`) | 두 언어 SKILL 에 spec 가지의 동작을 절로 적었다 [없앰] | 참 — 16R-D 가 `config.py:651` 갈래를 읽어 확인 (15영역 실측은 옮겨 적음) | 없음 · 반증 안 됨 (문서 문장을 재는 것은 없다) | 없음 |
| 135 | SKILL 이 `tolerate` 를 **과거형**으로 적어 이미 고친 것처럼 읽힌다 | 문서가 '고쳤다' 고 적은 상태가 현재 상태다 (`SKILL.md:252`·`SKILL.ko.md:234`) | 현재형으로 고치고 두 인자를 떼는 것이 남은 일이라고 적었다 [옮김] | 참 — 16R-D 가 `build_erd.py:178`·`:191` 에서 확인 | 없음 · 반증 안 됨 | 없음 |
| 136 | SKILL 이 HTML 차례를 다섯 장으로 못박는데 문서는 ④와 ⑤ 사이에 6·7장을 싣는다 | 15R 이 장을 넣고 문서 차례는 안 고쳤다 (`SKILL.md`·`SKILL.ko.md`) | 차례를 여섯 단계로 고치고 두 문서가 같은 장을 쓴다는 것도 적었다 [없앰] | 참 — 16R-D 가 `build_html.py:414-445` 에서 확인 | 없음 · 반증 안 됨 | 없음 |
| 137 | `ERD_STALE` 이 문서 생성을 막는 게이트인데 네 언어 문서에 한 글자도 없다 | 이 변수를 문서에 실은 라운드가 없었다 (`README*`·`SKILL*`·`INSTALL*`) | SKILL 환경변수 표·INSTALL '자주 걸리는 것'·README 실행 절차에 넣었다 [없앰] | 참 — 16R-D 가 `grep ERD_STALE` 로 열 문서에서 0건 확인 | 없음 · 반증 안 됨 | 없음 |
| 138 | `ERD_LABEL` 이 SKILL 의 **범용 표**에 있는데 `parse_ddl.py` 는 그것을 안 봤다 | 문서가 두 경로 모두를 뜻하는데 코드는 한 경로만 따랐다 (`parse_ddl.py`) | 16R 수정자 C 가 코드를 문서 쪽으로 맞췄고, 문서는 두 경로를 이름으로 적었다 [없앰] | 참 — 16R-D 가 `parse_ddl.py:33-67` 에서 C 의 수정을 확인 | 없음 · 미확인 — 16R-D 안 돌림 (C 의 파일) | `parse: ERD_LABEL labels the tables parse_ddl writes, like introspect` (수정자 C) |
| 139 | spec 의 영역 코드 중복이 15R 부터 하드 에러인데 문서에 없다 — 리눅스에서 되던 spec 이 이제 거부된다 | 15R 이 검사를 넣고 문서에 적지 않았다 (`config.py:583`) | 두 언어 SKILL 에 규칙과 **호환이 깨진다는 사실**을 함께 적었다 [없앰] | 참 — 16R-D 가 `config.py:583` 에서 확인 | 없음 · 반증 안 됨 | 없음 |
| 140 | 빈 값(`ERD_X=`)의 뜻이 갈래마다 다른데 그 세 규칙이 `config.py` 헤더에만 있다 | 15R 이 코드 쪽에만 모았다 (`config.py:23-40`) | 두 언어 SKILL 에 세 규칙 표와 켜짐/꺼짐 한 규칙을 옮겼다 [없앰] | 참 — 16R-D 가 `grep` 으로 SKILL 에서 0건 확인 | 없음 · 반증 안 됨 | 없음 |
| 141 | `회귀 여부` 의 `미확인 — 15R-D 안 돌림` 이 56행 — 문서가 제 작업 목록을 못 줄이고 있었다 | 15R-D 가 남의 갈래를 되돌려 보지 않았다 (`REVIEW-LOG.md`) | 16R-D 가 조작 50건을 새로 만들어 돌리고 49행을 `반증됨` 으로 확정했다 [없앰] | 참 — 조작마다 그 행이 이름 붙인 케이스만 돌려 빨강을 봤다 | 없음 · 해당 없음 (문서 행의 상태값) | 없음 |
| 142 | 요약표는 14R 발견을 45건이라 적는데 표에 선 14R 행은 42개다 | 45 의 내역이 15R·16R 어느 쪽에도 없다 (`REVIEW-LOG.md`) | 세 줄을 지어내지 않고 어긋난다는 사실만 남겼다 — 지금은 `B26` 이다 [옮김] | 참 — 16R-D 가 표의 14R 행을 세어 42 확인 | 없음 · 해당 없음 (채우지 않기로 한 자리) | 없음 |
| 143 | `반증 안 됨` 4행(#109~#112)이 전부 문서 행이다 — **문서를 재는 것이 하나도 없었다** | 회귀 시험이 `*.md` 를 한 번도 열지 않았다 (`scripts/selftest_*.py`) | `docs:` 케이스 셋을 넣었다 — 표의 모양·문서가 적은 수·`확인 필요` 의 수명 [없앰] | 참 — 16R-D 가 조작 7건을 넣어 전부 빨강 확인 | 있음: 케이스 수가 바뀌면 문서 다섯 자리를 함께 고쳐야 한다 · 반증됨(16R-D, `D1`·`D2`·`D3`) | `docs: every row of the REVIEW-LOG tables has the shape its header declares` 외 2 |

**직전 수정이 만든 것.** 15라운드가 만든 것이 셋이다. ① **앵커 체계를 통째로
교체**해서(꼬리 번호 → 테이블 키) 링크가 한 번 끊겼다 — 15라운드 이전 판 HTML 을
북마크해 둔 사람의 `#tb_x_y` 는 이제 다른 곳을 가리키거나 아무 데도 안 간다. 안정성을
얻는 대가로 **한 번은 반드시 깨지는** 교체였고, 그 사실이 문서 어디에도 없었다.
② `err.spec_dup_code` 를 **하드 에러로 신설**해서(#114) 리눅스에서 잘 돌던 spec 이
이제 거부된다 — 파일 이름이 같아질 영역 코드 둘(`A`/`a`)은 대소문자를 구분하는
파일시스템에서는 실제로 두 파일이었기 때문이다. 16라운드가 두 언어 SKILL 에 규칙과
**호환이 깨진다는 사실**을 함께 적었다(#139). ③ `build: every line the builders put in
front of a person goes through the catalog` 의 **소스 스캔 정규식이 세 갈래로 뚫렸다**
— 16R 수정자 B 가 그것을 AST 로 다시 쓰고 케이스를 하나 더 세웠다
(`build: the AST scan reads the shape, not one spelling of it`).

#### 16라운드 — 갈래별로 무엇을 했나

아래는 **다섯 수정자 자신의 보고를 옮겨 적은 것**이다(출처: 16R 수정자 A~E 보고).
16라운드는 독립 검증 단계를 거치지 않았으므로 **판정이 아니라 신고**로 읽어야 한다.
각 갈래가 스스로 신고한 '못 고친 것·안 고친 것·만들었을 수 있는 것' 은 전부
`## 남은 버그` 에 번호가 붙어 있다.

**수정자 A — 재는 쪽** (`selftest.py`·`selftest_kit.py`·`selftest_history.py`).
세는 함수에 걸린 문턱 조작(`if len(node_rects) > 12: return 0` 류)을 **크기 사다리**로
밀어냈다 — `SCALES = (2, 3, 5, 9, 17, 33, 65, 129, 257)` 로 판을 배로 키우며 같은
불변식을 되풀이한다(`selftest.py:987`). 그리고 입구 파일에도 다른 다섯 파일과 같은
규율을 걸었다(`EXPECT_CASES = 70` 신고 + `kit.ENTRY_FLOOR['selftest']`), 이름을 하나도
안 보는 `TOTAL_FLOOR = 181` 을 새로 뒀다. 남긴 것: `B1`~`B8`.

**수정자 B — 산출물** (`build_*.py`·`merge_schemas.py`). `merge_schemas.py` 가 라벨을
안 붙이고 `merged.update(part)` 만 하던 것을 문서가 약속하는 `<라벨>.<테이블명>` 키로
고쳤다(같은 이름 테이블이 말없이 삼켜지고 합계까지 1+1=1 로 찍히던 자리). 카탈로그
스캔을 정규식에서 AST 로 옮겼고, 세 builder 의 cwd 오염을 재는 케이스를 세웠다.
남긴 것: `B9`~`B16`.

**수정자 C — 설정** (`config.py`·`parse_ddl.py`·`lang/*`). `ERD_LABEL` 을 `parse_ddl`
쪽에서도 붙이게 해 문서와 코드를 맞췄고(`parse_ddl.py:33-67`, #138), `_get` 캐시의
근거 문장을 **코드가 하는 일에 맞게 낮춰** 다시 적었다(`config.py:228` 부근 — 예전
문장은 있지도 않은 피해를 근거로 세우고 있었다). 카탈로그 넷에 `col.null` 키를
넣었다. 남긴 것: `B17`~`B22`.

**수정자 D — 문서** (`README*`·`SKILL*`·`INSTALL*`·`install.sh`·이 파일). 아래 표의
11행이 D 의 발견이고, `미확인` 56행을 되돌리는 조작 48건도 D 가 돌렸다. 남긴 것:
`B23`~`B32`.

**수정자 E — 렌더** (`erd.py`). 자기참조 팔이 x 구간이 겹치는 테이블을 뚫던 것과,
라벨이 갈 곳을 잃었을 때 가는 여백 폴백이 테이블 위에 앉던 것을 고쳤다. `import erd`
가 만들던 `erd-build/out` 중 `out/` 만 멎게 했다(PEP 562 모듈 `__getattr__`). 그리고
**14라운드가 안 남긴 퍼저를 파일 안에 두었다** — `selftest_r14_render.py` 를 직접
실행하면 `fuzz`·`board` 로 돌고, `--scripts` 로 다른 트리를 가리켜 수정 전/후를
나란히 잴 수 있다. 남긴 것: `B33`~`B40`.

### 각주

라운드 절의 표가 `[^…]` 로 가리키는 자리다. 표의 칸에 들어가면 표가 아니게 되는
길이의 설명만 여기 둔다.

[^f-check]: 2라운드가 "`--check` 가 `SKILL.md` 를 본다" 고 커밋 제목에 적고 넣은 것이
    `[ -f "$DEST/SKILL.md" ]` 한 줄, 곧 **파일이 있느냐**였다. 문서가 이름 붙인 검사를
    도구가 하지 않던 자리이고, 열두 라운드 뒤인 14라운드가 같은 자리를 다시 발견했다.

[^f-half]: 이 문서의 `되풀이된 것` 에서 가장 자주 나온 모양 — **같은 버그를 반만
    고쳤다.** 한 부류를 고치면서 같은 부류의 나머지 절반을 남기고, 남긴 절반이 다음
    라운드의 발견이 된다.

[^f-sep]: 구분자를 바꾸는 방식이라 **다음 바이트가 똑같이 남아 있었다** —
    `|` → 개행 → `\x1e` 로 세 번 되풀이됐고, 전송을 `row_to_json` 으로 바꿔 원리적으로
    막고서야 끝났다. `[옮김]` 이 무엇을 뜻하는지 보여 주는 표본이다. **다만 그
    `row_to_json` 은 `introspect.py` 에만 들어갔다** — `parse_ddl.py` 의 라이브 조회
    경로는 지금도 구분자를 쓴다(`## 남은 버그` 의 `B42`).

[^f-self]: 겹침을 줄이려 만든 두 단계 경로 계산을 통로 lane 에만 적용하고 자기참조에는
    안 했다. 그런데 "무작위 200개 스키마에서 경고 0" 은 바로 그 수정이 내건 자랑이었다.

[^f-self3]: 13라운드가 두 자리(팔 높이 순서·팔의 x 검사)를 고쳤고, 14라운드가 셋째
    갈래(x 구간이 걸치는 테이블)를 찾았고, 15라운드가 넷째·다섯째(띠 좁히기 반복문,
    `hi - lo < 1` 보호선)를 찾았다. 같은 함수에서 **세 라운드 연속**이다.

[^f-reg]: 다만 순진한 등록(`import selftest_history` 한 줄)은 아무 말 없이 아무것도
    등록하지 않는다 — `selftest.py` 를 직접 돌리면 그 파일은 이미 `__main__` 으로 올라와
    있고 `from selftest import CASES` 는 같은 파일을 두 번째 모듈로 다시 올린다.
    그래서 목록과 도우미를 제3의 모듈(`selftest_kit.py`)로 빼고 글로브로 찾는다.

[^f-cap]: 상한을 자동 분류 가지에만 걸어 두고 spec 가지는 그 값을 아예 안 봤다.
    거기에 '어느 영역에도 없는 테이블' 을 받는 '기타' 영역까지 얹혀서,
    `ERD_MAX_AREAS=1` + 영역 하나짜리 spec 이 영역 3개가 됐다.

[^f-anchor]: 충돌을 꼬리 번호로 푼 것이 문제였다. 꼬리 번호는 **자리를 세는 값**이라
    스키마가 자라면 옮겨 앉는다: `x-y`·`x.y` 에 `x y` 를 더하면 `#tb_x_y` 가 가리키는
    테이블이 바뀐다. 코드가 스스로 "읽는 사람이 URL 로 쓰는 값" 이라고 적어 둔 값이다.

[^f-blank]: 14라운드 판은 **빈 문자열만** 재고 `_p` 의 `raw.strip()` 은 안 쟀다.
    전형적인 반쪽 수정이고, 15라운드가 `ERD_WORK='  '` 로 나머지 절반을 재현했다.

[^f-feed]: 세는 함수를 직접 거는 것은 **자가 살아 있는지**를 지킨다. 그런데 그 자에
    무엇이 닿는지(먹이)와, 실제 도판 경로에서도 같은 답을 내는지는 여전히 아무도 안
    봤다. 15라운드가 그 두 방향으로 우회했다.

[^f-shrink]: 신고제(`EXPECT_CASES`)는 '조용히 0' 을 막지만 '조용히 줄기' 는 못 막는다 —
    신고와 실제를 같은 손으로 함께 낮추면 아무 데서도 안 붉는다. 15라운드가 입구 파일에
    `CASE_FLOOR` 하한을 뒀고, 16라운드가 이름을 안 보는 `TOTAL_FLOOR` 를 하나 더 뒀다.
    한계는 `## 남은 버그` 의 `B8` 이다.

[^f-probe]: 프로브가 `eq(1, 2, 'x')` 와 `has('abcdef', 'zzz', 'x')` 딱 둘이라 **그 두
    값에서만** 정직한 뮤턴트가 그대로 빠져나갔다. 재는 쪽의 fixture 가 좁으면 재는 쪽도
    좁게 정직하다.

[^f-db]: 손으로 적은 `6` 을 세는 함수로 바꾼 것까지는 맞는데, 그 수를 확인하는 케이스가
    **같은 함수**를 읽었다. 손으로 적은 수의 문제를 없앤 것이 아니라 한 겹 안으로 옮겼다.

[^f-floor]: `RENDER_ALLOW` 를 '코드를 고쳐서는 늘릴 수 없는 자리' 에 둔 것과 같은
    이유다. 케이스를 더하는 것은 자유롭고(하한이므로), 줄이려면 **그 파일 바깥**의
    숫자를 함께 내려야 한다 — 그러면 그 삭제가 diff 에 남는다. 다만 `RENDER_ALLOW`
    쪽은 **새 항목을 더하는 것**을 아무것도 막지 않는다(`## 남은 버그` 의 `B43`).

---

## 남은 버그

**이 절이 다음 라운드가 여는 첫 절이다.** 아래는 16라운드가 끝나는 자리에서 아직
안 고쳐진 것 전부다. 대부분은 **수정자 다섯이 스스로 신고한 것**이고, 그 사실을
`왜 남았나` 칸에 적었다. 앞 라운드에서 넘어온 것 셋(`B41`·`B42`·`B43`)은 이 문서를
정리하면서 실제로 열어 확인했다.

**⚠ 최우선 — 16라운드는 독립 검증을 안 받았다.** 14·15라운드는 수정 뒤 독립 검증자
셋이 붙어 **과장·거짓을 찾아냈다**(14R: 과장 6·거짓 2, 독립 뮤테이션 19건 중 12건
생존 / 15R: 우회 9건). **16라운드는 그 단계 없이 끝났다.** `## 라운드별 상세` 의
16라운드 절에서 "고쳤다" 고 적힌 것은 전부 **수정자 자신의 말**이고, 재현은 그들의
하네스로만 확인됐다. 다음 라운드의 첫 일은 16라운드 검증이다.

| # | 무엇 | 왜 남았나 | 재현/근거 | 어느 파일 | 심각도 | 다음 라운드가 할 일 |
|---|---|---|---|---|---|---|
| B0 | 16라운드 수정 전체가 독립 검증을 안 받았다 | 다섯이 고치고 라운드가 끝났다 — 검증 단계를 아무도 열지 않았다 | 14R 은 과장 6·거짓 2, 15R 은 우회 9건을 바로 그 단계에서 찾아냈다 | 16R 수정분 전부 | 치명 | 주장하는 자와 뮤턴트를 만드는 자를 갈라 16R 을 판정한다 |
| B1 | 크기 사다리 최댓값 257 **위**에 문턱을 두는 조작은 여전히 통한다 | 부류를 없앤 것이 아니라 실제 도판 규모 너머로 밀어냈다 `[옮김]` | `if len(node_rects) > 500: return 0` 은 사다리를 지나간다 (신고: 16R 수정자 A) | `scripts/selftest.py:987` (`SCALES`) | 높음 | 문턱 자체를 금지하는 소스 검사로 바꿀지 판단한다 |
| B2 | `_counting_ns` **밖**의 이름에는 크기 사다리를 안 걸었다 | 사다리를 세는 함수 셋에만 걸었다 | `label_ink_box`·`on_a_table`·`overlaps` 의 호출자 쪽은 사다리 밖이다 (신고: A) | `scripts/selftest.py` | 중간 | 호출자 쪽 이름에도 같은 사다리를 건다 |
| B3 | `ENTRY_FLOOR['selftest_history']` 는 오늘 한 번도 안 걸린다 | 그 검사를 하는 케이스가 `selftest.py` 안에 있다 | `selftest_history.py` 를 직접 돌리면 `selftest.py` 는 글로브에 안 걸려 안 올라온다 | `scripts/selftest_kit.py:51` | 중간 | 입구 검사를 kit 로 옮기거나 그 바닥을 지운다 |
| B4 | 손으로 적는 상수가 넷에서 **여덟**로 늘었다 | 하한을 이름에 걸다 보니 자리가 늘었다 `[옮김]` | `CASE_FLOOR` 5 + `TOTAL_FLOOR` + `ENTRY_FLOOR` 2 + `EXPECT_CASES` (신고: A) | `selftest.py:1733`·`:1747`·`:1752`, `selftest_kit.py:51` | 높음 | 여덟을 줄일 방법을 찾거나, 못 줄이면 그 사실을 규칙으로 적는다 |
| B5 | `EXPECT_CASES = 70` 은 하한이 아니라 **정확값**이다 | 신고 대조가 `eq()` 라서 그렇다 | `selftest.py` 에 케이스를 하나만 더해도 벌 전체가 빨강이 된다 | `scripts/selftest.py:1752` | 높음 | 케이스를 더할 때 이 수를 함께 올릴 것 — 규칙으로 못박는다 |
| B6 | `top_assign` 의 `one()` 이 `erd.py` 의 **모양**을 강제한다 | 배선을 이름이 아니라 모양으로 못박은 대가다 | `checks`·`lab_hit`·`lab_boxes`·`node_rects` 는 `draw_erd` 안에서 정확히 한 번만 대입돼야 한다 | `scripts/selftest.py:1400`·`:1471` | 중간 | 정당한 리팩터가 빨강을 부를 때 이 케이스를 먼저 읽게 한다 |
| B7 | 크기 사다리가 시험 시간을 늘렸다 | `lab_hits` 가 O(n²) 다 | 다음 라운드가 사다리를 키우면 비용이 제곱으로 는다 (신고: A) | `scripts/selftest.py` (`SCALES`) | 낮음 | 사다리를 키우기 전에 시간을 잰다 |
| B8 | 파일을 지우면서 **같은 수의 빈 케이스를 다른 데 채우면** `TOTAL_FLOOR` 를 통과한다 | 총계 바닥은 개수만 본다 | 총계가 안 내려가므로 아무 데서도 안 붉는다 (신고: A) | `scripts/selftest.py:1747` | 중간 | 케이스가 실제로 무언가를 재는지 보는 자를 둘지 판단한다 |
| B9 | `build_html.py:275` 의 `Null` 머리칸이 아직 카탈로그를 안 거친다 | B 가 고치려 했을 때 카탈로그에 `col.null` 이 없었다 | 확인함 — 키는 네 카탈로그 `:51` 에 **들어왔고** `build_html.py:275` 가 아직 안 쓴다 | `scripts/build_html.py:275` | 중간 | `T('col.null')` 로 바꾸고 봐주기 문구를 케이스에서 뺀다 |
| B10 | `import build_erd` 만 해도 `./erd-build` 가 생긴다 | `erd.py:49` 가 모듈 최상단에서 `SCHEMA_JSON.read_text()` 를 한다 | 세 builder(`build_erd`·`build_html`·`build_docx`)의 남은 cwd 오염 **유일한 원인**이다 | `scripts/erd.py:49` | 높음 | `SCHEMA`·`SPEC`·`AREAS` 를 지연으로 돌릴지 결정한다 (`B34` 와 한 몸) |
| B11 | `merge_desc.py` 의 '내용이 같으면 안 쓴다' 가 신선도 게이트 동작을 정한다 | 경계에 케이스는 넣었으나 코드는 C 소유라 B 가 안 봤다 | `build_erd.require_fresh` 는 mtime 으로 판정한다 | `scripts/merge_desc.py:197-200` | 낮음 | 두 쪽 소유자가 같은 라운드에 함께 읽는다 |
| B12 | docx 는 zip 껍데기에 **벽시계**를 박는다 | python-docx 가 zip 항목마다 `datetime.now()` 를 쓴다 | 두 판이 1초를 걸치면 내용이 한 바이트도 안 달라도 파일 바이트가 다르다 (실측: 16R 수정자 B) | `scripts/build_docx.py` (python-docx) | 낮음 | 결정성은 zip 속(항목 이름+바이트)으로만 잰다 — 지금 그렇게 하고 있다 |
| B13 | `_speaks()` 가 **부름을 이름에 담는** 모양을 못 잡는다 | AST 스캔이 호출 노드를 본다 | `w = sys.stdout.write` 처럼 함수를 변수에 담아 부르면 안 걸린다 (신고: B) | `scripts/selftest_r14_build.py:597` | 중간 | 이름 대입까지 따라가게 하거나 못 잡는다는 것을 케이스 주석에 적는다 |
| B14 | 수정자 B 가 **자기 소유가 아닌 파일**을 한 줄 고쳤다 | merge_schemas 라벨링이 그 케이스 기대값을 바꿨다 | `selftest_history.py:719-724` 에 5줄 + 이유 주석이 남아 있다 (확인함) | `scripts/selftest_history.py:719-724` | 낮음 | 갈래가 겹칠 때 멈추는 규칙(13R)을 다시 읽는다 |
| B15 | `merge_schemas.py` 의 **출력 키가 바뀌었다** — 기존 spec 이 안 맞는다 | 문서가 약속하던 `<라벨>.<테이블>` 쪽으로 코드를 맞췄다 | 라벨 없이 뽑아 둔 파일을 합치던 사람의 `erd.spec.json` 이 `orders` 면 `shop.orders` 를 못 찾는다 | `scripts/merge_schemas.py` | 높음 | 마이그레이션 문구를 네 언어 문서에 적는다 |
| B16 | `"mapping_intro": ""` 를 **일부러** 비워 두던 사람에게 카탈로그 문구가 나온다 | 빈 값을 '안 적음' 과 구분하지 않는다 | 15R 이 빈 손글 장을 카탈로그 폴백에 태우면서 생겼다 (신고: B) | `scripts/build_html.py`·`build_docx.py` | 낮음 | 빈 문자열과 키 없음을 가를지 판단한다 |
| B17 | `_get` 캐시(`M6`)는 여전히 **반증 불가능**하다 | 시험을 못 만든 게 아니라 **안 만들었다** | 캐시를 지운 결과를 밖에서 관측할 길이 없다 — mkdir 은 멱등, `shlex.split` 은 순수 | `scripts/config.py:228` 부근 | 중간 | 관측 가능한 지연 값이 생기면 그때 잰다 |
| B18 | `parse_ddl` 의 `ERD_LABEL` 을 `ERD_EXCLUDE` 처럼 **라벨 전에** 거는지 안 봤다 | C 가 라벨 붙이기만 하고 순서는 확인 안 했다 | 신고: 16R 수정자 C | `scripts/parse_ddl.py` | 중간 | 제외와 라벨의 순서를 케이스로 못박는다 |
| B19 | `_relabel` 이 FK 를 통째로 접두어 붙이는 것이 **깨지면 관계가 조용히 사라진다** | "`parse_ddl` 의 모든 `ref_table` 은 `tables` 의 키다" 라는 성질에 기댄다 | 그 성질이 깨져도 경고가 없다 — 관계만 없어진다 (신고: C) | `scripts/parse_ddl.py` (`_relabel`) | 높음 | 그 성질을 단정으로 세워 깨지면 소리 내게 한다 |
| B20 | `RESERVED` 봐주기 목록이 자랄수록 케이스가 무뎌진다 | 예외를 목록으로 풀었다 | 목록이 자라기만 하면 그 자체가 신호다 (신고: C) | `scripts/parse_ddl.py` (`RESERVED`) | 낮음 | 목록 길이를 케이스가 상한으로 잡을지 판단한다 |
| B21 | `i18n: a key nobody calls…` 가 **남의 파일 상태에 딸려** 빨개질 수 있다 | 죽은 키 판정이 호출 쪽 소스를 읽는다 | 누가 `T('…')` 호출을 지우면 그 키가 죽은 키가 된다 (신고: C) | `scripts/selftest_history.py` (i18n 케이스) | 낮음 | 케이스가 왜 빨간지 메시지로 말하게 한다 |
| B22 | `docx.ch6`·`ch6_intro`·`ch7`·`ch7_intro` → `doc.*` 이관을 **안 했다** | 옮기는 순간 두 builder 의 호출이 함께 바뀌어야 하는데 소유자가 달랐다 | 확인함 — HTML 도 같은 항목을 싣는데 이름이 `docx.` 라 실제와 어긋난다 | `scripts/lang/*.py:159` 부근·`build_html.py:447`·`build_docx.py:402` | 중간 | 카탈로그와 호출을 **한 라운드에 함께** 옮긴다 |
| B23 | `tolerate` 는 **코드를 안 고쳤다** — 문서만 현재형으로 맞췄다 | 코드 소유자가 달라 문서 갈래가 손대지 않았다 | 확인함 — `build_erd.py:178`·`:191` 이 개요도·전체도에만 주고 영역도에는 안 준다 | `scripts/build_erd.py:178`·`:191` | 높음 | 두 인자를 떼고 영역도와 같은 규칙으로 맞춘다 |
| B24 | `회귀 여부` 의 `미확인` 이 **7행** 남았다 | 조작을 못 만들었거나(#54) 만든 조작이 초록이었다(#79) | 확인함 — #54·#70·#71·#72·#79·#122·#124 | `REVIEW-LOG.md` 라운드 절 표 | 높음 | 일곱에 조작을 만들어 돌린다 — #79 는 조작이 옳은지부터 본다 |
| B25 | `## 라운드 요약` 16행의 `발견 수` 11 은 **D 갈래 것만**이다 | 다른 넷의 발견을 옮겨 적으면 '좁게 재고 넓게 쓴다' 가 된다 | 확인함 — 다른 넷의 발견은 표에 한 행도 안 세워졌다 | `REVIEW-LOG.md` 16라운드 절 | 중간 | 넷에게 직접 물어 번호를 세운다 |
| B26 | 14라운드 확정 발견 **45건 중 42행**만 세워져 있다 | 45 의 출처가 14R 검토자 집계인데 내역이 없다 | 확인함 — 표의 14R 행을 세면 42다 | `REVIEW-LOG.md` 14라운드 절 | 중간 | 45 의 출처를 찾거나, 못 찾으면 요약표를 42로 내린다 |
| B27 | `ERD_STALE`·빈 값 규칙이 **SKILL 두 판에만** 들어갔다 | `SKILL.ja.md`·`SKILL.es.md` 는 **존재하지 않는다** | 확인함 — SKILL 은 en·ko 두 판뿐이고 README·INSTALL 은 4언어다 | `SKILL.md`·`SKILL.ko.md` | 중간 | SKILL 을 4언어로 낼지, 두 판으로 못박을지 결정한다 |
| B28 | 새 `docs:` 케이스는 문서 4언어를 열지만 **문장 내용은 안 잰다** | 문장을 재려면 카탈로그 대조가 필요하다 | 확인함 — 숫자와 표 모양만 본다 | `scripts/selftest_r14_install.py:525`~`:637` | 중간 | 문장까지 잴 자를 만들지 판단한다 |
| B29 | `docs: the case counts …` 는 라운드가 케이스를 늘리면 **빨개진다** | 문서가 적은 수를 등록 수와 대조하기 때문이다 | 설계된 마찰이다 — 그때 다섯 자리(`REVIEW-LOG` 1 + `INSTALL*` 4)를 함께 고쳐야 한다 | `REVIEW-LOG.md`·`INSTALL*.md` | 중간 | 케이스를 늘리는 라운드가 다섯 자리를 함께 고친다 |
| B30 | `CELL_MAX = 140` 은 수정자 D 가 고른 숫자다 | 근거가 '표로 읽히는 길이' 라는 판단뿐이다 | 확인함 — `selftest_r14_install.py:49` | `scripts/selftest_r14_install.py:49` | 낮음 | 값을 유지할지 다시 정한다 |
| B31 | `docs:` 케이스가 `selftest_r14_install.py` 에 있다 | 더 나은 자리를 만들지 않았다 | 확인함 — 이름과 내용이 어긋난다 | `scripts/selftest_r14_install.py` | 낮음 | `selftest_r14_docs.py` 로 옮길지 판단한다 (`CASE_FLOOR` 도 함께) |
| B32 | 문서 5곳의 `ERD_STALE` 설명이 **손으로 적은 중복**이고 재는 것이 없다 | 문장을 재는 자가 없다 (`B28` 과 한 몸) | 확인함 — SKILL 2 + INSTALL 4 + README 4 에 손으로 적혀 있다 | `SKILL*.md`·`INSTALL*.md`·`README*.md` | 중간 | 한 자리에서 뽑거나, 어긋남을 재는 케이스를 만든다 |
| B33 | `n3`(label_x 크기 조건부 거짓말)를 **E 의 파일은 못 잡는다** | 라벨이 실제로 겹치는 판이 480라벨/30초라 회귀 시험에 못 넣었다 | A 의 단위 케이스 둘이 잡는 것만 확인됐다 (신고: 16R 수정자 E) | `scripts/selftest_r14_render.py` | 중간 | 30초 판을 벌 밖 퍼저로 돌릴지, 벌 안에 넣을지 정한다 |
| B34 | `import erd` 가 `./erd-build` 를 만드는 것은 **그대로다** | `out/` 만 멎었다 — 나머지는 PEP 562 로 못 막는다 | 모듈 **안**의 전역 조회는 `__getattr__` 를 안 거쳐 파일 전체를 고쳐야 한다 | `scripts/erd.py:25-49` | 높음 | 규칙 문장을 "읽어야 하는 자리 말고는 아무것도 안 만든다" 로 좁힌다 |
| B35 | `schema.json` 이 없는 곳에서 `import erd` 는 **raw 트레이스백**이다 | `err.no_schema_file` 키가 필요한데 `lang/*` 는 C 소유라 E 가 안 만들었다 | 사람 말이 아니라 `FileNotFoundError` 가 그대로 나온다 (신고: E) | `scripts/erd.py:49`·`scripts/lang/*.py` | 중간 | 키를 만들고 `erd.py` 가 그 말로 죽게 한다 |
| B36 | 뮤턴트 셋이 **원리적으로 반증 불가능**하다 | 셋 다 *일어나지 않는 상태*를 막는 검사다 | `fb_row_nocheck`·`fb_last_nolift`·`bounds_no_min` — 단일 뮤턴트로는 결과가 안 달라진다 | `scripts/erd.py`, 표는 `selftest_r14_render.py:1055-1080` | 중간 | 짝 뮤턴트로 살아 있음을 보이는 방법을 케이스로 옮길지 판단한다 |
| B37 | `R7` 은 **폰트 의존적**이다 | 여백 폴백을 재려면 라벨이 실제로 밀려나야 한다 | 글꼴이 다른 기계에서는 `nothing to measure` 로 빨강이 날 수 있다 | `scripts/selftest_r14_render.py:750` | 중간 | 폰트를 고정하거나 판을 폰트 무관하게 만든다 |
| B38 | `R8` 의 등식(자 == 카운터)은 **노드 사각형 합치기**에 기댄다 | 머리+몸통을 하나로 합쳐 세기 때문이다 | 같은 열에 x 구간이 똑같고 세로로 맞닿은 테이블 둘이 생기면 셈이 어긋난다 (신고: E) | `scripts/selftest_r14_render.py:798` | 낮음 | 그런 판이 나오면 합치기 규칙을 다시 본다 |
| B39 | `while on_a_table(box)` 는 **원리적으로 무한 반복이 가능**하다 | 끝난다는 증명이 `on_a_table` 의 뜻에 기댄다 | 항상 참인 대역을 세우면 안 멈춘다 (신고: E) | `scripts/erd.py` (라벨 여백 폴백) | 중간 | 반복 상한을 두고 넘으면 소리 내게 한다 |
| B40 | 파일 안 퍼저가 `erd` 의 **시그니처에 기댄다** | 퍼저가 `layout_overview()`·`layout_global()`·`layout_area()` 를 직접 부른다 | 시그니처가 바뀌면 시험이 아니라 도구가 죽는다 (신고: E) | `scripts/selftest_r14_render.py:1092` 부근 | 낮음 | 도구가 죽으면 왜 죽었는지 말하게 한다 |
| B41 | `svg_canvas.py`·`i18n.py`·`lang/__init__.py` 를 **세 라운드째 아무도 안 봤다** | 갈래를 나눌 때마다 이 셋이 어느 갈래에도 안 들었다 | 확인함 — 14·15·16R 변경 파일 목록(`git status`)에 셋 다 없다 | `scripts/svg_canvas.py`·`i18n.py`·`lang/__init__.py` | 높음 | 다음 라운드의 한 갈래를 이 셋에 통째로 준다 |
| B42 | `parse_ddl` 의 **라이브 조회 경로는 아직 구분자로 행을 가른다** | 10R 이 `row_to_json` 으로 바꾼 것은 `introspect.py` 뿐이다 | 확인함 — `parse_ddl.py:600` 이 `psql(query, rs=RS).split(RS)` 다 (`RS='\x1e'`·`SEP='\x1f'`) | `scripts/parse_ddl.py:593-602` | 높음 | `config.psql_rows` 로 갈아 끼우고 #33·#43 의 `[없앰]` 표기를 고친다 |
| B43 | `RENDER_ALLOW` 에 **항목을 새로 더하는 것**을 막는 것이 없다 | 봐주기 값이 '정확값' 이라는 규율은 값만 보고 **키 집합은 안 본다** | 실측함 — 회귀로 `h_overlap` 이 9가 된 사본에서 허용치를 9로 올리니 초록이 됐다 | `scripts/selftest_kit.py:341` | 중간 | 키 집합 자체를 못박을지, diff 심사에 맡길지 정한다 |

### `B0` — 16라운드가 독립 검증을 안 받았다

이 항목이 나머지 43개보다 무겁다. 14·15라운드에서 실제로 일어난 일이 그 근거다.
14라운드는 45건을 고쳤다고 적었고 검증자 셋이 붙어 **과장 6·거짓 2**를 판정했으며,
독립 뮤테이션 19건 중 12건이 살아남았다. 15라운드는 14라운드의 재는 장치를 **9개로
우회**했다. 두 번 다 *수정자 자신은 성실했다* — 문제는 자기가 쓴 시험을 자기가 만든
뮤턴트로 재면 안 나오는 각도가 있다는 것이고, 그것이 이 문서가 여섯 라운드째 적어 온
**"재는 쪽은 아무도 재 주지 않는다"** 다. 16라운드에는 그 단계가 없다. 그러니 이
문서의 16라운드 절과 아래 43개 항목은 **신고서**로 읽어야 하고, 다음 라운드의 첫 일은
버그 찾기가 아니라 16라운드 판정이다.

### 수정자 A 가 남긴 것 — 재는 쪽 (`B1`~`B8`)

**`B1`.** A 는 "세는 함수 첫 줄에 `if len(...) > N: return 0` 을 넣으면 안 잡힌다" 는
부류를 **크기 사다리**로 대응했다 — 판을 2에서 257까지 배로 키우며 같은 불변식을
되풀이하므로, 문턱을 257 아래에 두면 어느 칸에서는 반드시 걸린다. 그런데 이것은
**부류를 없앤 것이 아니라 실제 도판 규모 너머로 밀어낸 것**이다(`[옮김]`).
`if len(node_rects) > 500: return 0` 은 지금도 통한다. A 가 스스로 그렇게 신고했다.

**`B2`.** 사다리는 `_counting_ns` 가 떼어 내는 세는 함수들에만 걸렸다.
`label_ink_box`·`on_a_table`·`overlaps` 를 **부르는 쪽**의 이름들은 사다리 밖이라
같은 조작이 그쪽에서는 통한다.

**`B3`.** `ENTRY_FLOOR` 는 '입구 파일도 다른 다섯 파일과 같은 바닥을 갖는다' 는
장치인데, 그 검사를 하는 케이스가 `selftest.py` 안에 있다. `selftest_history.py` 를
직접 실행하면 입구는 `selftest_history` 가 되고, 그때 `selftest.py` 는 글로브 대상이
아니어서 아예 안 올라온다 — 곧 `ENTRY_FLOOR['selftest_history']` 는 **오늘 어떤
실행에서도 안 걸린다.** 죽은 값이 목록에 앉아 있는 것이므로, 지우거나 살리거나
둘 중 하나다.

**`B4`·`B5`.** 하한을 파일 이름에 걸다 보니 손으로 적는 상수가 넷에서 여덟로 늘었다:
`CASE_FLOOR` 다섯 항목 + `TOTAL_FLOOR` + `ENTRY_FLOOR` 두 항목 + `selftest.py` 의
`EXPECT_CASES`. 그중 `EXPECT_CASES = 70` 은 **하한이 아니라 정확값**이다(신고 대조가
`eq()` 다). 누가 `selftest.py` 에 케이스를 하나라도 더하면 벌 전체가 빨강이 된다 —
의도된 마찰이지만, 그 마찰을 모르는 사람에게는 그냥 고장으로 보인다.

**`B6`.** 15라운드가 "배선은 이름이 아니라 모양으로 못박는다" 를 넣으면서 `one()` 이
`checks`·`lab_hit`·`lab_boxes`·`node_rects` 가 `draw_erd` 안에서 **정확히 한 번만**
대입될 것을 요구하게 됐다. 정당한 리팩터(예: 조건에 따라 두 번 대입)가 빨강을 부른다.
그때 이 케이스를 먼저 읽게 하는 것 말고 지금 답이 없다.

**`B7`.** `lab_hits` 는 O(n²) 다. 사다리가 판을 257까지 키우므로 시험 시간이 늘었고,
다음 라운드가 사다리를 더 키우면 비용은 제곱으로 는다.

**`B8`.** `TOTAL_FLOOR` 는 개수만 본다. 파일을 지우면서 **같은 수의 빈 케이스를 다른
데 채워 넣는** 조작은 총계를 안 내리므로 통과한다. 케이스가 실제로 무언가를 재는지를
보는 자는 아직 없다.

**한 줄 더.** A 는 `n11` 수정이 `\s*(\d+) cases need a real server` 라는 **문장 첫머리
서식**을 못박았다고 신고했다(`selftest_history.py:1727`). 이 안내를 다국어로 옮기는
순간 그 케이스가 소음이 된다 — `B29`·`B32` 와 같은 부류다.

### 수정자 B 가 남긴 것 — 산출물 (`B9`~`B16`)

**`B9`(확인함).** B 는 `build_html.py:275` 의 `Null` 머리칸을 **못 고쳤다**고 신고했다
— 그 시점 카탈로그에 `col.null` 이 없었기 때문이다. 그 뒤 수정자 C 가 키를 넣었다고
보고했으므로 이 문서를 정리하며 직접 확인했다: **키는 네 카탈로그 모두에 들어와 있고
(`lang/en.py:51`·`ko`·`ja`·`es`), `build_html.py:275` 는 아직 그 키를 안 쓴다.** 즉
남은 일은 한 줄 치환이고, 지금은 `html: every label the document prints comes out of
the catalog` 케이스가 그 자리를 **이름을 대고 봐주고** 있다 — 다른 낱말로 바꾸면 그
케이스가 빨강이다.

**`B10`.** 세 builder 의 cwd 오염은 이제 원인이 하나로 좁혀졌다 — `erd.py:49` 가 모듈
최상단에서 `SCHEMA_JSON.read_text()` 를 한다. `import build_erd` 만 해도 `./erd-build`
가 생긴다. `B34` 와 한 몸이므로 함께 판단할 것.

**`B11`.** `merge_desc.py:197-200`(내용이 같으면 안 쓴다)이 `build_erd.require_fresh`
의 mtime 판정 동작을 정한다. 경계에 케이스는 넣었으나(`errors: a re-run that changes
nothing does not turn the diagrams stale`) 코드는 C 소유라 B 가 안 봤다.

**`B12`.** docx 는 zip 껍데기에 **벽시계**를 박는다 — python-docx 가 zip 항목마다
`datetime.now()` 를 쓴다. 두 판이 1초를 걸치면 내용이 한 바이트도 안 달라도 파일
바이트가 다르다(B 의 실측). 그래서 결정성 검사는 docx 를 **속**(항목 이름 + 항목
바이트)으로 잰다. 이것은 고칠 수 없는 것을 우회한 자리이므로 다음 사람이 "왜 겉을 안
재나" 를 다시 묻지 않게 여기 적어 둔다.

**`B13`.** 카탈로그 스캔을 AST 로 옮겼는데, `_speaks()` 가 못 잡는 모양이 남았다 —
`w = sys.stdout.write` 처럼 **부름을 이름에 담는 경우**다.

**`B14`.** B 가 `scripts/selftest_history.py` 를 다섯 줄 고쳤다 — **자기 소유 파일이
아니다**(`:719-724`, 이유 주석 포함). merge_schemas 라벨링이 그 케이스의 기대값을
바꿨기 때문인데, 13라운드가 "동시에 고칠 때 겹치면 멈춘다" 를 규칙으로 적은 자리다.
이번에는 멈추는 대신 이유를 적고 고쳤다. 어느 쪽이 옳은지는 다음 라운드가 정할 것.

**`B15`.** `merge_schemas.py` 의 **출력 키가 바뀐다.** `parse_ddl` 로 만든 파일이나
라벨 없이 뽑아 둔 파일을 합치던 사람의 `erd.spec.json` 이 `orders` 라 적혀 있으면 이제
`shop.orders` 를 못 찾는다. **조용하지는 않다** — `log.spec_missing`·`log.spec_orphan`
이 이름을 대고 그 테이블은 '기타' 영역으로 그려진다. 그래도 **깨진 것은 깨진 것**이고
문서에 마이그레이션 문구가 없다.

**`B16`.** `"mapping_intro": ""` 를 **일부러** 빈 문단으로 쓰던 사람이 있으면 이제
카탈로그 문구가 나온다. 빈 문자열과 '키 없음' 을 가르지 않은 대가다.

### 수정자 C 가 남긴 것 — 설정 (`B17`~`B22`)

**`B17`.** `_get` 캐시(뮤턴트 `M6`)는 여전히 반증 불가능하다. C 는 **시험을 못 만든 게
아니라 안 만들었다**고 적었다 — 캐시를 지운 결과를 밖에서 관측할 방법이 지금 코드에
없기 때문이다(mkdir 은 `exist_ok=True` 라 멱등, `shlex.split` 은 순수, 경고를 찍는
지연 값이 하나도 없다). 관측할 수 없는 것을 재는 시험은 동작이 아니라 **구현을 베껴
적는 것**이고 그러면 다음 사람이 그 자리를 못 고친다. 대신 15라운드가 `config.py` 에
적어 둔 근거 문장이 있지도 않은 피해("경고도 두 번 나지 않는다")를 대고 있었으므로
그것을 사실대로 고쳐 적었다.

**`B18`.** `parse_ddl` 의 `ERD_LABEL` 을 `ERD_EXCLUDE` 처럼 **라벨 전에 거는지** 안
봤다. 제외와 라벨의 순서가 어긋나면 제외 패턴이 라벨 붙은 키에 안 맞는다.

**`B19`.** `_relabel` 이 FK 를 통째로 접두어 붙이는 것은 **"`parse_ddl` 의 모든
`ref_table` 은 반드시 `tables` 의 키다"** 라는 성질에 기댄다. 그 성질이 깨지면 관계가
**조용히** 사라진다 — 경고 한 줄이 없다. 이 절에서 가장 위험한 항목이다.

**`B20`.** `RESERVED` 봐주기 목록이 그만큼 케이스를 무디게 한다. 목록이 자라기만
하면 그 자체가 신호다.

**`B21`.** `i18n: a key nobody calls…` 는 **남의 파일 상태에 딸려** 빨개질 수 있다 —
누가 `T('…')` 호출을 지우면 그 키가 죽은 키가 된다.

**`B22`(확인함).** `docx.ch6`·`ch6_intro`·`ch7`·`ch7_intro` → `doc.*` 이관은 **안
했다.** 15라운드에 HTML 도 같은 항목을 싣게 되어 지금은 두 문서가 함께 쓰는 말인데
이름만 `docx.` 다. 옮기는 순간 `build_html.py:447`·`build_docx.py:402` 의 호출이 함께
바뀌어야 하는데 그 둘을 다른 사람이 동시에 고치고 있었다. `lang/en.py:159` 에 그
메모가 남아 있다 — **카탈로그와 호출을 한 라운드에 함께** 옮길 것.

### 수정자 D 가 남긴 것 — 문서 (`B23`~`B32`)

**`B23`(확인함).** `tolerate` 는 **코드를 안 고쳤다.** `build_erd.py:178`·`:191` 이
지금도 개요도·전체도에만 `tolerate=('h_overlap',)` 을 주고 영역도(`:~204`)에는 안
준다. D 는 문서만 현재형으로 맞췄다(#135). **두 인자를 떼는 것이 남은 일이다.**

**`B24`(확인함).** `미확인` 7행이 남았다 — #54·#70·#71·#72·#79·#122·#124. #54 는
커밋 메시지에 대한 것이라 코드에 되돌릴 자리가 없고, #79 는 만든 조작(`arm = want`)이
초록이었다. 초록을 `반증 안 됨` 이 아니라 `미확인` 으로 남긴 이유는 **그 조작이 그
수정을 되돌린 것이 맞다는 근거가 없기 때문**이다 — 되돌리지 못한 것과 되돌려도 안
잡히는 것은 다른 말이다.

**`B25`·`B26`(확인함).** 16라운드 요약행의 `발견 수` 11 은 D 자기 갈래 것만이고,
14라운드는 확정 발견 45건 중 42행만 세워져 있다. 둘 다 **모르는 것을 채우지 않는다**
는 규칙에 따라 비워 둔 자리이지, 확인된 값이 아니다.

**`B27`(확인함).** `SKILL.ja.md`·`SKILL.es.md` 는 **존재하지 않는다** — SKILL 은 en·ko
두 판뿐이라 `ERD_STALE`·빈 값 규칙이 그 둘에만 들어갔다(README·INSTALL 은 4언어다).
언어 수가 문서 종류마다 다르다는 사실 자체가 문서에 없다.

**`B28`·`B32`.** 새 `docs:` 케이스는 문서 4언어를 열지만 **문장 내용은 안 잰다** —
숫자와 표 모양만 본다. 그래서 문서 5곳의 `ERD_STALE` 설명이 손으로 적은 중복인 채로
남아 있고 재는 것이 없다. `all N passed` 가 두 라운드째 틀렸던 것과 같은 자리이고,
이번에는 **숫자만** 막았다.

**`B29`.** `docs: the case counts …` 는 라운드가 케이스를 늘리면 반드시 빨개진다.
설계된 마찰이다 — 그때 다섯 자리(`REVIEW-LOG` 1 + `INSTALL*` 4)를 함께 고치면 된다.
이 문서를 정리하면서도 그 규칙을 지켰다.

**`B30`·`B31`.** `CELL_MAX = 140` 은 D 가 고른 숫자이고, `docs:` 케이스 셋이
`selftest_r14_install.py` 에 들어 있어 파일 이름과 내용이 어긋난다. 옮기려면
`CASE_FLOOR` 도 함께 고쳐야 한다(`B4`).

### 수정자 E 가 남긴 것 — 렌더 (`B33`~`B40`)

**`B33`.** `n3`(`lab_hits()` 첫 줄에 `if len(lab_boxes) > 3: return 0`)를 **E 의 파일은
못 잡는다.** 라벨이 실제로 겹치는 판이 480라벨/30초라 회귀 시험에 못 넣었다. A 의 단위
케이스 둘이 잡는 것만 확인됐다.

**`B34`.** `import erd` 가 `./erd-build` 를 만드는 것은 **그대로다** — `out/` 만 멎었다.
없애려면 `SCHEMA`·`SPEC`·`AREAS` 를 전부 지연으로 돌려야 하는데, 모듈 **안**의 전역
이름 조회는 PEP 562 를 안 거치므로 파일 전체를 고쳐야 한다. 그래서 규칙 문장을
**"import 는 아무것도 안 만든다" 가 아니라 "읽어야 하는 자리 말고는 아무것도 안
만든다" 로 좁혀야** `B10` 과 어긋나지 않는다. `erd.py:25-49` 의 `__getattr__`
docstring 에 그 사정이 적혀 있다.

**`B35`.** `schema.json` 이 없는 곳에서 `import erd` 는 raw `FileNotFoundError`
트레이스백이다(사람 말이 아니다). `err.no_schema_file` 키가 필요한데 `lang/*` 가 C
소유라 E 가 안 만들었다.

**`B36`.** 뮤턴트 셋이 **원리적으로 반증 불가능**하다 — `fb_row_nocheck`(여백 폴백의
`on_a_table` 검사 삭제)·`fb_last_nolift`(마지막 상자의 `while on_a_table` 삭제)·
`bounds_no_min`(`min(lo_l, x1-13)`·`max(hi_r, x2+13)` 되돌리기). 셋 다 *일어나지 않는
상태*를 막는 검사라 단일 뮤턴트로는 결과가 안 달라진다. **짝 뮤턴트로는 살아 있음이
보인다**(`fb_y_down_nocheck` 는 빨강). 뮤턴트 표는 `selftest_r14_render.py:1055-1080`
에 있고 `(없음)` 이 그 뜻이다.

**`B37`·`B38`.** `R7`(여백 폴백)은 **폰트 의존적**이다 — 글꼴이 다른 기계에서는
`nothing to measure` 로 빨강이 날 수 있다. `R8`(자 == 카운터)의 등식은 **노드 사각형
합치기**(머리+몸통)에 기댄다 — 같은 열에 x 구간이 똑같고 세로로 맞닿은 테이블 둘이
생기면 셈이 어긋난다.

**`B39`.** `while on_a_table(box)` 는 **원리적으로 무한 반복이 가능하다.** 끝난다는
증명이 `on_a_table` 의 뜻에 기대므로, 항상 참인 대역을 세우면 안 멈춘다.

**`B40`.** 파일 안 퍼저가 `erd.layout_overview()`/`layout_global()`/`layout_area()`
**시그니처에 기댄다** — 바뀌면 시험이 아니라 도구가 죽는다.

### 앞 라운드에서 넘어온 것 (`B41`~`B43`) — 이 문서를 쓰며 확인했다

**`B41`(확인함 — 아직 남았다).** `svg_canvas.py`·`i18n.py`·`lang/__init__.py` 를
**세 라운드째 아무도 안 봤다.** 16라운드에 C 가 `i18n.py` 를 소유했으나 손대지
않았다 — `git status` 의 변경 파일 목록(14·15·16라운드분이 전부 미커밋이라 한 덩어리로
보인다)에 이 셋이 하나도 없다. `svg_canvas.py` 의 마지막 손질은 `57c132a`(5라운드),
`i18n.py` 는 `7ff2faa`(4라운드), `lang/__init__.py` 는 `7fc6a33`(다국어화 배치)이다.
**다음 라운드의 한 갈래를 이 셋에 통째로 주는 것이 맞다.**

**`B42`(확인함 — 아직 남았다. 그리고 표기가 틀렸다).** 12라운드가 판정한 과장 —
"값이 담을 수 있는 어떤 바이트도 구분자로 읽히지 않는다" 가 `parse_ddl.py` 의 라이브
조회 경로에는 거짓이라는 것 — 은 **고쳐지지 않았다.** `parse_ddl._rows()`
(`parse_ddl.py:593-602`)는 지금도 `psql(query, rs=RS).removesuffix('\n').split(RS)`
로 행을 가르고 `line.split(SEP)` 으로 필드를 가른다(`RS='\x1e'`·`SEP='\x1f'`,
`config.py:276-277`). `row_to_json` 으로 원리적으로 막은 것은 `config.psql_rows()` 뿐이고
그것을 쓰는 것은 `introspect.py` 다. 그러니 `## 라운드별 상세` 의 #33·#43 이 수정 칸에
붙인 **`[없앰]` 은 사실이 아니다** — 그 자리는 `[옮김]` 이고, 회귀 시험으로 적힌
`parse: a newline inside a fetched value does not kill the parser` 는 이름 그대로
**개행만** 잰다(개행은 `RS` 가 아니므로 통과한다). 표를 고치는 것과 코드를 고치는 것
중 어느 쪽이 먼저인지는 다음 라운드가 정할 일이라 여기 적어만 둔다.

**`B43`(실측함 — 신고자 쪽이 맞다).** 15라운드가 "`RENDER_ALLOW` 항목을 **새로 더하는**
것은 막는 것이 없다" 고 신고했고 검증자가 "실제로는 잡힌다" 고 반박했다. 둘 다 반쪽만
맞다. 실측은 이렇다 — 트리를 복사해 `erd.overlaps` 의 합류 예외를 죽여(`if False:`)
`render: a hub with many children keeps lines out of the tables` 의 `h_overlap` 을
5에서 **9**로 올리니 그 케이스가 빨강이 됐고(`this case allows at most 5`), 같은
사본에서 `RENDER_ALLOW` 의 그 항목을 **9로 올리자 초록**이 됐다. 아무것도 안 그리는
쪽으로도 재 봤다 — 다른 케이스에 `{'h_overlap': 0}` 짜리 항목을 **새로 더하는 것**도
그대로 통과한다. 곧 **값이 실제 최댓값과 다르면 빨강이라는 점에서 검증자가 맞고(그
점에서 '잡힌다'), 키 집합 자체를 못박는 것은 하나도 없다는 점에서 신고자가 맞다.**
회귀를 넣고 그 회귀의 정확한 수를 봐주기에 적으면 조용히 초록이 된다. 지금 이것을
막는 것은 diff 심사뿐이다.

## 되풀이된 것

**수정이 다음 버그를 만들었다.** 2·4·7·8·9·10·11·12 라운드의 심각한 발견 중 상당수가
직전 라운드의 수정에서 나왔다. 7→8→9→10 은 네 번 연속이다. 부분 패치가 계속 새서 파서
앞단을 다시 쓴 판단은 맞았지만, 다시 쓸 때마다 이전 판이 하던 일을 하나씩 떨어뜨렸다.
10라운드는 이 패턴을 **끊겠다고 쓴 커밋**을 감사했는데, 그 커밋이 넷을 새로 만들었다.
의도로는 못 끊는다는 뜻이다.

12라운드에서는 심각한 발견 **셋이 전부** 직전 수정 것이었다. 그런데 모양이 달라졌다 —
그 셋이 다 **검증을 늘린 자리**에서 나왔다. 겹침을 줄이려 만든 두 단계 경로 계산이
자기참조를 빼먹었고, 새로 넣은 검증 기록이 시험의 판단을 대신했고, 그 기록을 남기지
못하면 그림까지 사라졌다. 고치는 손보다 **재는 손이 만든 것**을 먼저 보게 된다.

**같은 버그를 반만 고쳤다.** 이것이 가장 자주 나온 모양이다. 한 부류를 고치면서 같은
부류의 나머지 절반을 남겼고, 남긴 절반이 다음 라운드의 심각한 발견이 됐다.

- ALTER·COMMENT 의 스키마 귀속을 고치면서 `CREATE UNIQUE INDEX` 와 스키마 붙은
  `ALTER` 를 빠뜨렸다
- 값 속 개행이 행을 쪼개는 것을 막으면서 `\x1e`·`\x1f` 를 남겼다. 구분자를 바꾸는
  방식이라 **다음 바이트가 똑같이 남아 있었다** — `|` → 개행 → `\x1e` 로 세 번
  되풀이됐고, 전송을 `row_to_json` 으로 바꿔 원리적으로 막고서야 끝났다
- 인라인 주석(`id int,  -- 설명`)의 자리를 고치면서 컬럼 **위**에 적는 주석을
  깨뜨렸다. 두 수정이 서로 자리를 바꿨을 뿐이다 — 쉼표를 기준으로 삼는 한 둘은
  같은 쪽에 있으면서 반대를 뜻하므로 갈릴 수가 없었다. 기준을 줄로 바꿔 끝났다
- FK 를 pg_catalog 로 옮기며 파티션 중복은 걸렀지만, 그 술어가 PG 11 부터 있는
  컬럼이라 10 이하가 통째로 죽었다
- 14라운드에는 이 모양이 **한 라운드 안에서 여덟** 나왔다. 앞의 넷은 13라운드 수정이
  남긴 절반이다: 라벨 겹침을 잉크로 재게 고치면서 `라벨↔라벨` 만 고치고 `라벨↔테이블`
  은 여백 상자를 그대로 뒀고(맞닿음 규칙도 한쪽은 `<`, 다른 쪽은 `<=` 로 서로
  반대였다), 자기참조 경로 계산을 두 단계로 나눈 것을 통로 lane 에만 적용하고 팔에는
  안 했고, 상수를 배치하는 쪽과 재는 쪽이 함께 쓰게 묶으면서 재는 쪽은 절반만 묶었다.
  뒤의 넷은 다른 네 갈래가 각각 찾은 것이다:
  - **`ERD_FONT` 만.** 글꼴 환경변수가 못 쓰는 값이면 왜 못 쓰는지 말해 주게 고쳐
    놓고, 바로 옆의 `ERD_FONT_BOLD` 는 `exists()` 한 줄로 남겼다 — 굵은 글꼴만
    조용히 기본값으로 떨어진다
  - **docx 만.** 표지 정보표의 행 폭을 docx 쪽에서만 맞춰, 같은 spec 이 HTML 에서는
    `ValueError` 로 죽었다. 그리고 **반대 방향으로도 한 번 더** — `UNIQUE` 표시는
    HTML 에만 있고 docx 에는 없다. 두 문서가 같은 재료를 읽는 자리는 늘 이 모양이다
  - **`areas` 만.** 4라운드가 spec 오타를 raw traceback 대신 사람 말로 바꾼 것이
    `areas` 한 키에만 걸려 있었다. `doc`·`derives` 오타는 여전히 traceback 이다
  - **`-f` 한 줄만.** 2라운드가 "`--check` 가 `SKILL.md` 를 본다" 고 적고 넣은 것이
    `[ -f SKILL.md ]` 였다. **문서가 이름 붙인 검사를 도구가 안 하고 있던** 자리이고,
    아래 "이름이 검사를 대신했다" 와 같은 부류인데 이번엔 이름을 문서가 붙였다

고칠 때 물어야 할 것 두 가지다. **같은 부류가 어디에 또 있는가**, 그리고 **이 수정이
경계를 옮기기만 하는가 없애는가.**

**검증이 없는 기능은 조용히 죽는다.** 4라운드에서 넣은 `선↔테이블` 이 5라운드 수정의
결함을 잡았고, 5라운드에서 넣은 `라벨↔라벨` 이 그다음 수정을 잡았다. 반대로 인라인
주석은 재는 것이 없어서 **두 판을 통째로 죽은 채** 지나갔다 — `pg_dump` 는 `COMMENT ON`
을 쓰므로 그쪽 시험만 통과하고 있었다.

**재는 장치도 자기가 재는 실패를 면제받지 않는다.** 10라운드에 처음 이름을 붙였고
("재는 쪽도 조용히 죽는다"), 12·13·14·15 라운드에서 다른 모양으로 네 번 더 나왔다.
**여섯 라운드 연속이다.** 뿌리는 하나다 — **재는 쪽은 아무도 재 주지 않는다.**

- 10라운드: 시험이 검증의 사람용 줄을 정규식으로 긁고 있었다. `(허용)`·`[경고]` 꼬리가
  붙자 마지막 항목을 놓쳤는데 하필 **0 이 아닐 때만** 놓쳐서 시험은 늘 초록이었다.
  `가로선 중첩 44` 가 통과했다
- 12라운드: 시험이 재는 쪽의 **판정**을 그대로 믿었다. `verify_clean()` 은 다시 세지
  않고 `warn` 목록만 봤는데, 그 목록을 만드는 것이 검사 대상인 코드다. 경고를 잠재우려
  `tolerate` 를 한 줄 넓히는 것은 다음 판이 충분히 할 법한 일이고, 그 한 줄이면 그림
  품질 보증이 통째로 조용해졌다. **검증을 늘린 그 커밋이 같은 손으로 무장을 해제했다**
- 12라운드: `ERD_VERIFY_LOG` 가 산출물을 볼모로 잡았다. 로그를 못 쓰면 그림이 한 장도
  안 나왔다. **재는 도구가 재는 대상을 망가뜨릴 수 있다**
- 13라운드: 소급해 만든 36개가 아무 데도 등록되지 않아, 기본 점검에서 한 번도 돌지
  않았다. 재는 것을 늘려 놓고 **돌지 않는 자리에 두는 것**도 같은 부류다. 14라운드에
  등록했다 — 다만 파일이 권한 그 한 줄(`import selftest_history`)로 등록했으면
  **개수도 안 오르고 아무 말도 없었을 것**이다. 등록하는 방법 자체가 조용히 실패할
  수 있다 (아래 "지금 있는 것" 참고)
- 14라운드: **재는 쪽에 숫자를 대 준 것이 재는 대상이었다.** 13라운드가 규칙을
  시험으로 옮겼지만 그 규칙이 읽는 다섯 숫자는 여전히 `erd.py` 것이었고, 넷은 188장
  어디서도 0 이 아닌 적이 없어 규칙이 발동할 일 자체가 없었다 — 카운터를 상수 0 으로
  바꾸는 한 줄이면 그림 품질 보증이 통째로 조용해졌다. 12라운드의 `tolerate` 한 줄과
  같은 자리이고, 이번에는 그 자리가 `counts` 라는 이름을 달고 있었다. 같이 나온 것들도
  전부 13라운드가 새로 넣은 것이다: 훑기는 호출을 지워도 안 붉었고, 등록은 제가
  실패한 것을 말하지 못했고, 봐주기 숫자는 **상한**이라 넓히기만 하면 조용했다
  (5→50 으로 벌리고 11라운드 회귀를 함께 넣어도 101개 전부 통과). 그리고 도구
  자신의 도구 — `eq()`·`has()`·`case()` 의 중복 검사 — 도 죽여 보니 아무 말이 없었다
- 15라운드: **재는 것을 늘린 커밋이 또 재는 눈을 감겼다 — 네 번째다.** 이번에는 눈을
  감긴 커밋이 바로 *앞 줄이 말한 그 수정*이다. 14라운드가 재는 장치를 고치고 "대부분
  이제 잡힌다" 고 33행 표로 적었는데, 독립 검증자가 그 장치를 **8개로 우회**했다.
  모양이 세 부류인데 셋 다 이 문서에 이미 이름이 있다. **단위 케이스가 먹이를 자기가
  넣는다** — 세는 함수를 직접 걸어 두면 자는 지켜지지만 그 자에 무엇이 닿는지는
  아무도 안 본다(`lab_boxes` 를 `[:0]` 로 굶기면 라벨 두 항목이 영구히 0). 이것은
  "재지 않은 것과 재 봤더니 0 인 것은 다르다" 의 **입력 쪽** 판이다. **배선을 이름으로
  본다** — `0 * sum(…)` 은 이름이 전부 제자리라 통과한다. 이것은 "이름이 검사를
  대신했다" 가 이번엔 *검사하는 쪽 코드 안에서* 난 것이다. **도구를 재는 케이스가
  도구가 만든 것을 잰다** — `_counting_ns()` 를 손수 구현으로 갈아치우면 단위 셋이
  `erd.py` 대신 그 스텁을 채점한다. 12라운드의 '재는 손이 만든 것' 그대로다. 그래서
  15라운드에는 (g) 실제 도판을 그리면서 `draw_erd` 프레임을 붙잡아 **세는 함수가 무엇을
  먹는지**와 **그 판이 실제로 부른 함수 객체가 겹침에 반응하는지**를 재고, (h) 배선은
  이름이 아니라 **모양**을 못박고, (i) `_counting_ns()` 자체가 `erd.py` 의 그 줄에서
  나왔는지·손댄 소스를 먹였을 때 동작이 따라 바뀌는지를 재는 케이스를 두었다

그래서 지금은 (a) 사람이 읽는 줄과 별도로 기계가 읽을 자리를 두고 **기록이 없으면
실패로 치고**, (b) '깨끗하다' 의 판정 규칙을 시험이 직접 가지며, (c) 봐주기는 케이스가
숫자로만 적고, (d) 계측이 실패해도 산출물은 낸다. 시험을 더할 때 물어야 할 것이 하나
더 있다. **그것이 어디서 돌아가는가** — 아무도 안 부르는 시험은 없는 시험이다.

14라운드에 두 가지를 더 붙였다. (e) **세는 함수는 산출물을 통하지 않고 직접 건다.**
`overlaps`·`thru_nodes`·`lab_hits`·`boxes_touch` 에 손으로 만든 겹치는 기하를 먹여
0 이 아닌 값이 나오는 것을 못박아 두면, 그림이 깨끗해서 규칙이 발동하지 않는 동안에도
자가 살아 있는지는 계속 재진다. 여기에 기록의 다섯 숫자가 **그 함수에서 나오는지**를
소스에서 읽어 함께 못박는다 — 자가 멀쩡한데 배선이 상수인 자리가 따로 있기 때문이다.
(f) **봐주기는 상한이 아니라 정확값이다.** `RENDER_ALLOW` 에 5 를 적으면 그 케이스의
그림 어딘가에 정확히 5 가 있어야 한다. 상한이면 늘리는 것과 회귀가 들어오는 것이
구분되지 않는다.

**안 한 검사를 통과로 적었다.** 5라운드에서는 선↔테이블 항목이 아예 없는데 `0·0·0`
이 찍혔고, 11라운드에서는 라벨을 그리지도 않는 개요도가 라벨 겹침 `0` 을 찍었고,
10라운드에서는 FK 조회가 통째로 실패하고도 `FK 0` 이 찍혔다. 셋 다 같은 말이다.
**재지 않은 것과 재 봤더니 0 인 것은 다르다.** 지금은 앞의 것을 `해당 없음` 으로 적고,
조회가 실패하면 문서를 만들지 않는다.

12라운드는 같은 짓을 **시험 여덟 개**에서 찾았다. `<node>` 가 하나도 없는 GraphML 이
"테이블이 다 실렸다" 를 통과했고(남은 `<edge>` 하나가 두 이름을 다 품고 있었다),
그림이 한 장도 없는 docx 가 "그림이 페이지에 맞는다" 를 통과했고(빈 목록을 도는
반복문), 단정 하나는 조건이 늘 거짓이라 아무것도 못 잡았다. 그러니 `해당 없음` 은
검증의 출력에만 있는 문제가 아니다 — **검사 대상이 없으면 통과가 아니라 실패로 친다.**
지금은 개수와 자리를 못박고(`<node>` 수 = 테이블 수, 그림 수 = 그린 PNG 수), 잴 것이
없으면 그 사실 자체가 실패다.

**이름이 검사를 대신했다.** 14라운드에서 새로 보인 모양이다. 위의 것은 *검사할 것이
없는데 통과로 적는* 자리였고, 이쪽은 *케이스의 이름과 주석이 검사가 하지 않는 일을
주장하는* 자리다. 읽는 사람은 그 이름을 목록에서 보고 "그건 지켜지고 있구나" 로
읽으므로, 없는 검사보다 나쁘다 — 없으면 다음 사람이 만들기라도 한다.

- `artifacts: graphml carries the ETL flows **the diagram draws**` 는 GraphML 과
  docx 만 봤다. 그림에서 ETL 흐름을 통째로 지워도 초록이었다
- `introspect: a server too old to answer is refused` 의 주석은 "하한을 **올려** 잡아
  멀쩡한 서버를 막지는 않는지 함께 지킨다" 고 적는데, **내려** 잡는 쪽은 안 봤다.
  하한을 `0` 으로 바꿔도 초록이다 (넣어 둔 두 벌이 그 앞 분기에서 먼저 걸린다)
- `errors: a control character in a name does not kill the build` 는 `\n`·`\x1e`·
  `\x1f`·`\x0b` 만 넣었는데, 그 넷은 파이썬 `str.split()` 이 이미 공백으로 친다.
  씻는 표를 통째로 비워도 초록이었다 — 표가 실제로 일하는 문자(`\x00`·`\x07`·`\x7f`)
  를 fixture 가 한 번도 안 썼다
- 위 셋 다 고쳤다. 고치는 방법은 하나뿐이다 — **이름이 말하는 것을 그대로 재게
  하거나, 이름을 재는 것에 맞게 줄이거나.**

**첫 실행만 봤다.** 앞선 아홉 라운드가 전부 **빈 디렉터리 · 새 DB · 한 번 실행**만 봤다.
그래서 두 번째 실행에서만 나는 것들이 통째로 남아 있었다 — 지난 판 그림이 새 글 옆에
실리고, 이전 판 문서가 있느냐에 따라 `MANUAL` 키 해석이 달라졌다. 다음 검토자는 같은
디렉터리에 두 번 돌리는 것부터 해 볼 것.

**커밋 메시지의 주장은 증거가 아니다 — 네 번 틀렸다.** 세 번은 "DDL 경로와 DB 경로가
한 건도 다르지 않다" 였고, 세 번 다 다음 검토자가 첫 시도에 반증했다. 확인한 범위
(스키마 하나, varchar 없음)를 확인하지 않은 범위까지 확장해 쓴 탓이다. 네 번째가
11라운드의 "무작위 200개 스키마에서 경고 0" 이고, 12라운드가 120개 중 19개로
반증했다. 넷 다 모양이 같다 — **좁게 재고 넓게 썼다.**

그래서 12라운드부터 검토자에게 버그 찾기와 함께 **직전 커밋의 주장을 하나씩 판정하는
일**을 맡긴다. 첫 판정에서 열 개 중 여섯만 참이었다.

같은 자를 이 기록에도 댄다. 13라운드의 주장을 따로 쓴 퍼저로 다시 재 봤다.

- 자기참조 겹침은 **진짜로 사라졌다**. 자기참조를 반씩 섞은 무작위 스키마 80개(같은
  씨앗)를 두 판에 걸었더니 수정 전은 36개가 경고했고(거의 다 `가로선 중첩`) 수정
  후는 2개다. `가로선 중첩` 경고는 한 건도 남지 않았다
- 다만 그 2개는 `라벨↔라벨 1` 이다. 즉 "경고 0" 은 여전히 **퍼저를 누가 썼느냐에
  딸린 말**이지 스킬의 성질이 아니다. 남은 것은 라벨끼리의 겹침이고, 아직 아무도
  그것이 진짜 잉크인지 세는 실수인지 확인하지 않았다 — 다음 검토자 몫이다
  - **14라운드 판정: 세는 실수였다.** 퍼저를 또 따로 써서(80개, 자기참조 절반) 재니
    수정 전 9개가 경고했고 그중 셋이 `라벨↔라벨` 이었다. 셋 다 겹친 상자를 뽑아
    좌표를 찍고 PNG 를 그 자리에서 잘라 눈으로 봤다 — 셋 다 `parent_0_id` 같은 라벨
    **둘이 두 줄로 나란히** 놓인, 아무 데도 안 닿는 그림이었다. 세로로 겹친 것은
    1~3px 인데 그 상자는 글자가 아니라 **자리 잡기용 여백**이다(높이 16 대 잉크
    10 남짓, 좌우 3px씩). 여백을 걷어 내고 다시 재니 `라벨↔라벨` 은 0 이 되고
    나머지 넷(`선↔테이블` 2, `가로선 중첩` 2, `라벨↔테이블` 2)은 숫자 하나 안
    움직였다. 그림은 한 픽셀도 안 바뀌었다 — 바뀐 것은 세는 규칙뿐이다
  - 세는 규칙을 느슨하게 한 것이므로 **이빨이 남아 있는지**를 같이 박아 뒀다.
    `verify: the label overlap check measures ink, not the placement padding` 은
    여백이 걷혔는지·13px 떨어진 것을 안 세는지뿐 아니라 **5px·0px 는 여전히 세는지**
    를 함께 본다. 세 방향 모두로 반증했다: 옛 규칙(여백째)으로 되돌리면 빨강,
    가로 여백만 걷어도 빨강, 상자를 한 점으로 줄여 경고를 끄면 빨강
- "21개가 빨강" 은 **재현되지 않았다.** 어떤 회귀를 넣었는지 커밋이 적지 않아서다.
  라우터가 자리를 피하지 않게 되돌리고 그린 쪽이 `warn` 을 숨기게 해 보니 예전 시험은
  2개, 지금 시험은 4개가 빨강이었다(늘어난 둘이 새로 넣은 훑기와 줄 대조다). 방향은
  맞고 크기는 확인되지 않았다. **주장에 숫자를 적을 때는 그 숫자를 낸 조작도 같이
  적는다** — 안 그러면 다음 사람이 참인지 거짓인지를 판정할 수가 없다
- 15라운드: 위 한 줄이 **그대로 되풀이됐다.** 14라운드도 뮤테이션 91개·census 188장·
  "22개 생존" 같은 수치를 적으면서 그것을 낸 도구를 트리에 남기지 않았고, 그래서
  검증자가 M 번호를 하나도 재현하지 못했다. 13라운드에 이름 붙인 것을 14라운드가 읽고
  같은 자리에 다시 빠진 셈이다 — **문서에 이름이 붙어 있다는 것으로는 안 막힌다.**
  15라운드 수정분은 하네스를 파일로 남기고 경로를 "지금 있는 것" 에 적었다

**고쳤다는 주장과 그것을 지키는 시험은 별개다.** 15라운드에서 이름을 붙인다. 14라운드는
45건을 고쳤다고 적었는데, 검증자가 그중 여럿을 **되돌려도 초록**인 것으로 확인했다.
"고쳤다" 는 그 시점의 산출물에 대한 말이고, "지켜진다" 는 되돌렸을 때 붉어지는지에 대한
말이다. 둘은 같은 커밋 안에 있어도 서로를 함의하지 않는다. 12·13·14·15라운드 절이 전부 이 어긋남의 사례다 — 매번 고친 것은 진짜였고, 매번 그것을 지킨다던 시험이
비어 있었다. 그래서 이 문서가 세 라운드째 적어 온 "무언가를 고쳤으면 그것을 잡아 줬을
항목을 하나 남긴다" 에는 뒷문장이 하나 더 있어야 한다 — **그 항목이 그 수정을 되돌렸을
때 실제로 붉어지는 것을 보고 나서 적는다.** 앞의 넷은 매번 앞문장까지만 지켰다.

**시험이 검사 대상 트리에 쓰면 시험은 저희끼리 싸운다.** 15라운드에 나온 새 모양이다.
i18n 케이스 하나가 깨진 카탈로그를 `scripts/lang/zz.py` 에 쓰고 `finally` 에서
지웠다 — 시험 벌 전체에서 공유 가변 상태를 쓰는 **유일한** 자리였고, 순차로는 아무
일이 없는데 동시에 돌리면 한쪽의 `unlink` 가 다른 쪽이 읽기 전에 파일을 지워 **그
케이스만** 간헐로 붉었다. 프로세스가 중간에 죽으면 파일이 트리에 남아 다음 사람의
`git status` 를 더럽힌다. 12라운드의 "재는 도구가 재는 대상을 망가뜨릴 수 있다" 와
뿌리가 같은데, 이쪽은 망가뜨리는 상대가 **다른 시험 프로세스**다. 규칙 한 줄로 둔다 —
**시험은 검사 대상 트리에 아무것도 쓰지 않는다.** 트리를 손봐야 하면 임시 디렉터리에
복사해 거기서 잰다.

수치는 두 방향으로 쟀다(2026-08-04, 이 저장소·이 기계). 검증자가 처음 재현한 것은
병렬 8회 × 2배치 = 16회 중 7회 실패이고, 15라운드 수정 뒤 같은 트리를 병렬 8회
돌려 **8회 전부 `all 161 passed`** 였다. 고친 부분만 14라운드 모양으로 되돌린 사본을
같은 방식으로 8회 돌리니 **2회가 붉었고, 붉은 것은 매번 그 한 케이스**였다. 재는 쪽
자체도 함께 확인했다 — 병렬 8회를 도는 동안 워크트리의 65개 파일 해시가 **하나도
바뀌지 않았다.** 병렬 실행 스크립트는 `scratchpad/r15-testkit/parallel.sh` 다.

**경고는 소음이 되면 죽는다.** 11라운드에서 스물다섯 도판 중 열일곱이 첫 실행부터
`[경고]` 를 달고 나왔다. 그대로 뒀으면 "경고 = 회귀" 라는 문서의 말이 한 주 만에
빈말이 된다. 주장을 낮추는 대신 세는 쪽을 고쳤다 — 라우터가 고칠 수 없는 겹침(컬럼
행에 못박힌 진출입 꼬리)은 세지 않고, 고칠 수 있는 것만 센다.

**눈으로는 못 잡는 것이 있다.** 선이 테이블을 관통해도 선을 노드보다 먼저 그리므로
노드 색에 덮여 보이지 않는다. 자기참조 루프 둘이 정확히 겹쳐도 하나로 보인다. 그림을
띄워 놓고 봐도 모른다 — 숫자로 재야 한다.

---

## 지금 있는 것

`python3 scripts/selftest.py` — **181개 항목** (2026-08-04, 16라운드가 끝나는 자리에서
실측), DB 없이 2분 남짓. `install.sh --check` 가 이것도 함께 돌린다. 입구는
`scripts/selftest.py` 하나이고, 항목은 여섯 파일에 나뉘어 있다.

**어떻게 쟀는가.** 총계는 빈 임시 디렉터리에서
`env -u ERD_DB -u ERD_PSQL -u ERD_LABEL python3 <경로>/scripts/selftest.py` 를 돌려
마지막 줄(`all 181 passed`)을 읽었고, 파일별은 `grep -c '^@case('` 와
`selftest_kit.LOADED` 를 함께 봤다. 둘이 갈리는 자리가 하나 있다 —
`selftest_history.py` 의 도커 전용 6개는 `^@case(` 로 세어지지 않고 서버가 있을 때만
등록된다.

| 파일 | 등록되는 항목 | 무엇을 담고 있나 |
|---|---|---|
| `scripts/selftest.py` | 70 | 입구이자 목록의 3분의 1 — 고칠 때 함께 남긴 것 |
| `scripts/selftest_history.py` | 40 (+ 도커 6) | 열한 라운드를 소급해 만든 것 — 항목마다 어느 라운드의 무슨 결함을 지키는지 적혀 있다 |
| `scripts/selftest_r14_config.py` | 25 | 14·15·16라운드의 설정 갈래 (`config.py`·`parse_ddl.py`·`lang/*`) |
| `scripts/selftest_r14_build.py` | 23 | 같은 라운드의 산출물 갈래 (`build_*.py`·`merge_schemas.py`) |
| `scripts/selftest_r14_install.py` | 13 | 같은 라운드의 설치·배포 갈래 + `docs:` 케이스 셋 (`B31` 참고) |
| `scripts/selftest_r14_render.py` | 10 | 같은 라운드의 렌더 갈래 (`erd.py`) + 파일 안 퍼저 |
| **합계** | **181** | `ERD_SELFTEST_DOCKER=1` 이면 **187** |

`ERD_SELFTEST_DOCKER=1` 이면 여기에 6개가 더해진다 — 진짜 서버가 있어야 도는 것들이고
PG 16·10 두 벌로 돈다. 안 돌린 개수는 집계 윗줄에 매번 찍히고, 그 줄 자체를 확인하는
항목이 하나 있다. **숫자 옆의 날짜가 요점이다** — 이 줄은 "131개" 인 채로 두 라운드를
지나왔고, 언제의 131 인지 아무 데도 없어서 틀린 줄이라는 것을 아무도 알아채지
못했다. 개수를 다시 확인하려면 그냥 돌려 보면 된다 — 마지막 줄이 총계이고, 파일별
개수는 각 파일의 `EXPECT_CASES` 와 `selftest_kit.LOADED` 가 들고 있고, 하한은
`selftest.py` 의 `CASE_FLOOR`·`TOTAL_FLOOR` 와 `selftest_kit.ENTRY_FLOOR` 가 들고 있다.

**이제 이 숫자를 재는 것이 있다.** 16라운드까지는 이 줄도, 네 언어 `INSTALL*.md` 의
예시 출력에 든 같은 숫자도 아무도 열어 보지 않아서, 15라운드가 여기 하나만 고치고 네
문서에 `all 131 passed` 를 그대로 남겼다. `docs: the case counts the documents state
are the counts a run produces` 가 다섯 자리를 지금 등록된 수와 대조한다 — 케이스가
늘면 그 케이스가 빨개지고, 그때 다섯 자리를 함께 고치면 된다. 문서가 낡는 것이
**조용하지 않게** 하는 것이 요점이다.

담긴 것은 전부 **한 번이라도 조용히 깨졌던 것**이다.

**등록은 파일 목록으로 한다, 손으로 적는 import 로 하지 않는다.** 12라운드 커밋이
42개를 만들어 놓고 아무 데도 등록하지 않았다. 그런데 순진한 등록(`import
selftest_history` 한 줄)은 **아무 말 없이 아무것도 등록하지 않는다** — `selftest.py`
를 직접 돌리면 그 파일은 이미 `__main__` 으로 올라와 있고, `from selftest import
CASES` 는 같은 파일을 두 번째 모듈로 다시 올려 그 사본의 목록에 항목을 붙인다. 돌고
있는 쪽은 제 것만 세고 '전부 통과' 를 찍는다. 그래서 목록과 도우미를 제3의 모듈
(`scripts/selftest_kit.py`)로 빼고, `selftest.py` 는 옆에 있는 `selftest_*.py` 를
**글로브로 찾아** 불러온다. 파일이 하나 더 늘 때 잊을 줄이 없다. 이름이 겹치면
`case()` 가 그 자리에서 죽는다.

**그런데 그 등록 장치가 제 실패를 말하지 못했다.** '조용히 두 배' 는 막았지만 '조용히
0' 은 아무도 안 막고 있었다 — 글로브 패턴이 어긋나거나 `load_extras()` 호출이 사라지면
39개가 통째로 없어지는데 종료코드는 0 이고 마지막 줄은 초록 `all 62 passed` 였다.
`install.sh --check` 는 `tail -1` 만 보여 주므로 사용자에게는 `✓ all 62 passed` 다.
14라운드에 셋을 못박았다: 옆에 `selftest_*.py` 가 **하나도 없으면** 그 자체가 실패,
파일 하나가 케이스를 **하나도 안 올리면** 실패, 파일이 `EXPECT_CASES` 를 신고했으면
그 수와 **정확히** 맞아야 한다. 총계 상수를 문서나 코드에 박지 않는 이유는 라운드마다
늘어서 곧 어긋나기 때문이다 — 세는 자리를 파일 옆에 둔다. 부르는 자리도 하나 줄였다:
`main()` 이 스스로 `load_extras()` 를 부르므로 입구의 그 한 줄이 사라져도 개수가
조용히 줄지 않는다.

**훑기도 제가 돌았는지를 말한다.** 13라운드의 `sweep_verify()` 는 호출을 통째로 지워도
아무 데서도 빨강이 뜨지 않았다. 지금은 통과한 케이스 수와 훑은 케이스 수를 집계 앞에서
맞춰 본다 — 어긋나면 그 자체가 실패 한 줄이다.

**등록했다는 말은 반증으로만 증명된다.** `selftest_history.py` 안의 단정 하나를 일부러
틀리게 고치고 `python3 scripts/selftest.py` 를 돌려 그 항목이 빨강으로 뜨는 것을 보고
나서야 등록됐다고 적는다. 개수가 올랐다는 것만으로는 부족하다.

시험은 부르는 사람의 환경을 하나도 물려받지 않는다. `ERD_*` 를 전부 지우고 돌린다 —
`ERD_LABEL` 을 켜 둔 셸에서 `--check` 가 멀쩡한 설치를 고장 났다고 알린 적이 있다.
시험은 부르는 사람의 설정이 아니라 코드를 재야 한다.

렌더링은 매번 자체 검증이 찍힌다. 봐준 것은 `(허용)` 로 표시하고, 잴 수 없는 항목은
`해당 없음` 으로 적고, 0 이어야 하는데 아닌 것이 있으면 같은 줄에 `[경고]` 가 붙는다.

시험은 그 줄을 읽지 않는다. `ERD_VERIFY_LOG` 를 켜면 `erd.py` 가 도판마다 JSONL 한
줄을 남기고, 시험은 그것을 읽는다. **기록이 없으면 통과가 아니라 실패다.** 사람이
읽는 줄은 사람 좋으라고 서식이 바뀌고, 그 서식 변화가 실제로 시험의 눈을 감겼다.
그 파일은 판마다 처음부터 다시 쓰이므로 언제 열어도 이번 판의 기록만 들어 있고, 쓸 수
없는 자리여도 그림은 전부 나온다 — 계측이 산출물을 볼모로 잡지 않는다.

**'깨끗하다' 의 뜻은 시험이 가진다.** 기록을 그대로 믿지 않고 규칙을 시험이 적용한다.
다섯 카운터(`label_table`·`label_x`·`thru`·`v_overlap`·`h_overlap`)가 전부 정수이고
전부 0 이어야 한다. `해당 없음` 은 라벨을 그리지 않는 개요도의 라벨 두 항목에서만
정직한 값이고 그 밖에서는 실패다 — **재기를 멈춘 검사는 깨끗한 검사가 아니다.** 항목이
이름째 빠져도 실패고(남은 것이 전부 0 이라 조용히 통과한다), 코드가 시험이 모르는
항목을 새로 재도 실패다(아무도 안 보는 숫자가 된다). 봐주기는 코드가 넘기는 `tolerate`
가 아니라 케이스가 `RENDER_ALLOW` 에 숫자로 적는다 — **코드를 고쳐서는 늘릴 수 없는
자리**에 둔 것이 요점이다. (그 자리의 한계는 `## 남은 버그` 의 `B43` 이다 — 값은
정확값이지만 **키 집합은 아무도 안 본다.**) 그리고 케이스가 물어봐 주기를 기다리지
않는다. 스무 개가 그림을 그리는데 셋만 묻고 있었으므로, 케이스가 통과할 때마다 그
케이스가 남긴 기록을 전부 훑는다.

**그리고 그 규칙이 읽는 숫자도 재는 것으로 친다.** 14라운드에 census 를 떠 보니 다섯
중 넷은 188장 어디서도 0 이 아닌 적이 없었다 — 규칙은 있는데 발동할 일이 없으니,
카운터를 상수 0 으로 바꾸는 한 줄이면 그림 품질 보증이 통째로 조용해졌다. 그래서
세는 함수(`overlaps`·`thru_nodes`·`lab_hits`·`boxes_touch`)를 `erd.py` 의 산출물을
통하지 않고 **직접** 건다. `draw_erd()` 소스에서 그 def 만 떼어 내 단독으로 컴파일하고,
손으로 만든 겹치는 기하를 먹여 0 이 아닌 값이 나오는 것을 못박는다: 같은 자리에 40px
나란히 달린 두 선은 1, 같은 선을 두 번 그린 것도 1(합류가 아니다), 끝점 하나만
공유하는 진짜 합류는 0, 5px 파고든 선은 관통이고 3px 는 아니다. 그림이 좋아져 실제
겹침이 0 이 되어도 이 넷은 흔들리지 않는다 — 재는 것이 레이아웃 품질이 아니라
**자가 살아 있는지**여서다. 반대쪽도 막는다: 기록의 다섯 숫자가 각각 어느 함수에서
나오는지를 소스에서 읽어 못박는다. 자는 멀쩡한데 배선이 상수인 자리가 따로 있었다.
`RENDER_ALLOW` 의 숫자도 이제 **상한이 아니라 정확값**이다 — 상한이면 넓히는 것과
그 아래로 회귀가 들어오는 것이 구분되지 않는다 (5→50 으로 벌리고 11라운드 회귀를
함께 넣어도 101개가 전부 통과했다).

**16라운드가 여기에 크기 사다리를 얹었다.** 세는 함수를 직접 걸어도 첫 줄에
`if len(node_rects) > 12: return 0` 을 넣으면 단위 케이스는 작은 판만 먹이므로 그대로
지나간다. 그래서 같은 불변식을 `SCALES = (2, 3, 5, 9, 17, 33, 65, 129, 257)` 로 판을
배로 키우며 되풀이한다 — 문턱을 257 아래 어디에 두든 어느 칸에서는 걸린다. **이것은
부류를 없앤 것이 아니라 실제 도판 규모 너머로 밀어낸 것이다**(`## 남은 버그` 의
`B1`·`B2`·`B7`).

**그런데 그 자에 무엇이 닿는지는 아무도 안 보고 있었다.** 15라운드가 막은 자리다. 위
문단의 넷은 세는 함수에 **시험이 먹이를 넣어** 부른다. 그래서 자는 지켜지지만 `erd.py`
가 그 자에 아무것도 안 올려도 조용하다 — `lab_boxes` 를 만드는 comprehension 을
`placed[:0]` 으로 굶기는 한 줄이면 라벨 두 항목이 영구히 0 이고 141개가 전부
초록이었다. 반대 방향도 있다: `lab_hits()` 가 단독 네임스페이스에는 없는 이름으로
갈래를 파면 **단위 시험에만 정직하고 실제 도판에서만 거짓**이 된다. 그래서
`verify: the counters the drawing path really uses are fed, and still bite` 는 진짜
판을 그리면서 `sys.settrace` 로 `draw_erd` 프레임을 붙잡아 두 가지를 잰다 — 돌아올 때
그 프레임의 지역값에서 **먹이의 크기**(라벨 상자 수 = 놓인 라벨 수, 테이블 사각형 수 =
테이블 수, 선 구간 0 아님)를 읽고, 그 판이 실제로 부른 **함수 객체를 그대로 꺼내**
자유변수 셀에 손으로 만든 겹치는 기하를 꽂아 불러 본다. 소스에서 떼어 낸 사본이 아니라
그 판이 실제로 쓴 그 함수를 재는 것이 요점이다.

**배선은 이름이 아니라 모양으로 못박는다.** 위 "다섯 숫자가 어느 함수에서 나오는지" 를
14라운드는 식 안에 그 **이름이 나오는가**로 봤다. `lab_hit = 0 * sum(…)` 은 이름이
전부 제자리라 그대로 지나갔고, `and False`·`if False else 0`·`[:0]` 도 같은 구멍이다.
지금은 그 자리에 적힐 수 있는 식의 모양을 정해 둔다 — `thru`는 정확히 `thru_nodes()`,
`v_overlap` 은 정확히 `overlaps(segs_v)`, `lab_hit` 은 정확히 `lab_boxes × node_rects`
를 도는 `sum(…)`, 그 둘을 만드는 comprehension 은 각각 `placed`·`tnames` **전체**를
돈다. 상수 곱은 이름이 아무리 멀쩡해도 `BinOp` 라서 걸린다. 레이아웃을 정말 고쳐 쓰면
이 케이스가 붉어지는데, 그때가 기록의 숫자가 어디서 나오는지 다시 적어야 할 때다.
(대가는 `## 남은 버그` 의 `B6` — 이 규칙이 `erd.py` 의 모양까지 강제한다.)

**재는 손이 만든 것을 재는 손이 채점하지 않는다.** 세는 함수를 떼어 내는
`_counting_ns()` 자체를 재는 것이 없었다 — 그것을 '정확히 동작하는 손수 구현' 으로
갈아치우면 위 단위 셋이 `erd.py` 대신 그 스텁을 채점하고, `erd.py` 의 세는 함수를
통째로 지워도 초록이다. 12라운드가 이름 붙인 자리 그대로다. 지금은 두 방향으로 묶는다:
나온 함수의 `co_filename`·`co_firstlineno` 가 **따로 파싱한 `erd.py` 의 그 줄**과
맞아야 하고, 소스를 한 줄 손봐 먹였을 때 **동작이 따라 바뀌어야** 한다. 뒤쪽은 소스를
안 읽는 구현으로는 흉내 낼 수 없다.

**도구를 재는 프로브는 도구가 실제로 다루는 값을 덮는다.** `eq()`·`has()` 가 제 실패를
말하는지 보는 프로브가 `eq(1, 2, 'x')` 와 `has('abcdef', 'zzz', 'x')` 딱 둘이었다.
그래서 `eq()` 가 list·tuple·dict·set 에는 절대 실패하지 않게 해도, `has()` 가 네 글자
이상 바늘에는 절대 실패하지 않게 해도 초록이었다 — 이 저장소 단정의 대부분이 목록·사전
비교와 긴 문자열 검색이다. **재는 쪽의 fixture 가 좁으면 재는 쪽도 좁게 정직하다.**
지금은 두 함수 다 실제로 쓰이는 값의 모양을 한 줄씩 덮고, 반대 방향(같은 값·있는
바늘은 지나간다)도 함께 본다.

**세는 쪽과 확인하는 쪽은 서로 다른 근거를 쓴다.** "6개는 진짜 서버가 있어야 돈다" 는
줄의 그 `6` 을 14라운드가 손으로 적지 않게 고쳤는데, 세는 함수(`_db_case_count()`)와
그것을 확인하는 케이스가 **같은 함수**를 읽었다. `.strip()` 하나만 빼면 그 수가 0 이
되고, 확인하는 쪽도 0 을 읽으니 자기일관이라 초록 — 사용자에게는 `0 cases need a real
server and were NOT run` 이 찍힌다. 손으로 적은 6 의 문제를 **없앤 것이 아니라 한 겹
안으로 옮긴** 것이다. 지금은 확인하는 쪽이 줄을 훑지 않고 **AST 로 `@case` 데코레이터를
센다.** 두 수가 어긋나면 어느 쪽이 틀렸든 그 자리에서 죽는다. (16라운드가 그 대조에
문장 첫머리 서식까지 함께 못박았다 — 다국어로 옮기면 소음이 된다.)

**신고는 선택이 아니고, 신고서 아래에 톱니를 둘 더 둔다.** `EXPECT_CASES` 는 '조용히
0' 은 막지만 **'조용히 줄기'** 는 못 막았다 — `@case` 셋을 떼고 신고 수도 함께 내리면
`all 138 passed` 로 조용히 줄어든다. 그래서 (1) `selftest_*.py` 가 `EXPECT_CASES` 를
신고하지 않으면 그 자체로 빨강이고, (2) 입구 파일이 `CASE_FLOOR` 라는 **하한**을 따로
들고 있다. 케이스를 더하는 것은 자유롭고, 줄이려면 **그 파일 바깥**의 숫자를 함께
내려야 한다 — 한 파일만 손보는 변경으로는 못 내리고, 내리면 diff 에 남는다.
`RENDER_ALLOW` 를 '코드를 고쳐서는 늘릴 수 없는 자리' 에 둔 것과 같은 이유다.
(1)을 `load_extras()` 에서 죽이지 않고 케이스 하나의 빨강으로 둔 것은 판단이다 —
신고 한 줄을 잊은 것 때문에 **시험이 한 개도 안 도는** 쪽이, 이 저장소가 세 라운드째
물려 온 바로 그 모양이라서다. 한 줄 빨강이 파일 이름을 대는 편이 낫다.

**바닥이 파일 이름에 걸린 문제는 16라운드가 반만 막았다.** 15라운드가 적어 둔 한계는
이랬다 — 바닥은 **파일 이름으로** 걸려 있고 목록에 없는 이름은 바닥이 1 이니, 파일을
**이름째 바꾸면** 그 파일의 바닥이 조용히 사라진다. 16라운드가 15라운드의 단서를 그대로
집어 **이름을 하나도 안 보는 바닥**(`TOTAL_FLOOR = 181`)을 하나 더 뒀다. 개명만 하는
것은 이 바닥을 통과하고(그래도 좋다 — 개명 자체는 아무것도 안 잃는다), 개명에 삭제를
섞는 순간 총계가 내려가 여기서 붉어진다. ⚠ 대가는 둘이다: 라운드마다 이 상수를 손으로
고쳐야 하고(13·14라운드가 총계 상수를 피한 바로 그 이유), 그리고 **같은 수의 빈
케이스로 채우는 조작은 여전히 통한다**(`## 남은 버그` 의 `B4`·`B8`).

**시험은 검사 대상 트리에 아무것도 쓰지 않는다.** 카탈로그가 깨졌을 때를 재는 케이스
하나가 `scripts/lang/zz.py` 를 트리에 쓰고 지웠다. 순차로는 아무 일이 없지만 시험을
두 벌 동시에 돌리면 한쪽의 삭제가 다른 쪽의 읽기를 앞질러 **그 케이스만** 간헐로
붉었고(병렬 16회 중 7회), 프로세스가 중간에 죽으면 파일이 트리에 남았다. 지금은
카탈로그 트리째 임시 디렉터리에 복사해 거기서 잰다 — 고친 뒤 병렬 8회는 전부
`all 161 passed` 이고, 되돌린 사본은 같은 8회에서 2회가 그 케이스에서 붉는다.
시험 벌 전체를 다시 훑어 이런 자리가 여기 하나뿐이었음을 확인했고, 시험이 도는 동안
워크트리의 파일 65개 해시가 하나도 바뀌지 않는 것도 함께 확인했다.

**문서도 이제 재는 것이 있다.** 16라운드까지 회귀 시험은 `*.md` 를 **한 번도 열지
않았다** — 그래서 `반증 안 됨` 네 행(#109~#112)이 전부 문서 행이었고, 되돌려도 아무
데서도 빨강이 안 떴다. 지금은 셋이 걸려 있다: 표의 모든 행이 제 머리와 같은 열 수인가
· 빈 칸이 없는가 · 어떤 칸도 140자를 넘지 않는가(`docs: every row …`), 문서가 적은
케이스 수가 이 판이 실제로 등록한 수와 같은가(`docs: the case counts …`, 다섯 자리를
본다), 표의 칸에 `확인 필요` 가 남아 있지 않은가(`docs: "확인 필요" never survives …`).
**재는 것은 표의 칸이고 산문이 아니다** — 문장 내용은 아직 아무도 안 잰다
(`## 남은 버그` 의 `B28`·`B32`).

**재는 장치는 제가 실패하는지를 케이스 하나로 확인한다.** `eq()`·`has()` 가 틀린 값에
정말 죽는지, `case()` 가 이름 중복에 죽는지, `load_extras()` 가 아무것도 못 찾을 때
죽는지, `verify_recs()` 가 기록이 없을 때 죽는지, `verify_clean()` 이 더러운 기록에
죽는지, 훑기가 '그린 장수 ≠ 기록 장수' 에 죽는지 — 여섯 다 죽여 보고 아무 말이 없던
것을 14라운드가 확인했으므로, 이제 그 여섯이 각각 한 줄씩 걸려 있다.

**무언가를 고쳤으면 그것을 잡아 줬을 항목을 하나 남긴다.** 이 목록이 계속 값을 하려면
그 습관이 필요하다. 되돌렸을 때 빨간불이 뜨는지까지 확인하고 남긴다 — 안 그러면
항목은 늘어나는데 재는 것은 늘지 않는다.

**그리고 그 확인에 쓴 도구를 남긴다.** 13라운드가 "주장에 숫자를 적을 때는 그 숫자를
낸 조작도 같이 적는다" 고 적었는데 14라운드가 뮤테이션 91개를 주장하면서 하네스를
남기지 않아 검증자가 M 번호를 하나도 재현하지 못했다. 지금 남아 있는 것은 셋이다.

- `scratchpad/r15-testkit/mutants.py` — 15라운드 수정분의 하네스
  (`list` / `show <이름>` / `run [이름…]`). 트리 사본에 뮤턴트를 넣고 전체 시험을 돌린
  뒤 **기준선에 없던 빨강**만 세므로, 다른 이유로 이미 붉은 케이스가 있어도 판정이
  흐려지지 않는다. 여덟 우회 뮤턴트(V4·V18·V19·V6b·V7·V8·V16·V24)와 표본 아홉이
  이름째 들어 있다.
- `scratchpad/r16-docs/mutants.py` — 16라운드가 `미확인` 56행을 되돌린 조작 48건의 표.
  이름 `M<행번호>` 가 그 표의 키다.
- `scripts/selftest_r14_render.py` 의 `__main__` — **저장소 안에 남은 유일한 도구**다.
  `fuzz`·`board` 로 판을 만들고, `--scripts` 로 다른 트리를 가리켜 수정 전/후를 나란히
  잴 수 있다. 같은 파일 `:1055-1080` 에 16라운드가 실제로 돌린 뮤턴트 표가 원문과
  함께 있다 — 하네스가 아니라 **레시피**다(시험이 제 사본을 만들어 제 뮤턴트를 채점하면
  15라운드가 지적한 자리로 되돌아가므로 일부러 그렇게 뒀다).

앞의 둘은 저장소 밖이라 **사라진다.** 다음 라운드에 옮기든 다시 쓰든 하되,
**수치를 주장하면서 도구를 안 남기는 것은 이 문서에 두 번 적힌 잘못이다.**

---

## 이 문서를 어떻게 유지하는가

**라운드를 돌린 사람이, 그 라운드가 끝나는 자리에서, 다음 라운드가 시작되기 전에
쓴다.** 커밋과 함께 낸다.

나중에 몰아 쓰면 안 되는 이유가 있다. 이 문서는 **같은 부류의 실수를 일찍 알아보라고**
있다. 10라운드의 발견 여덟 개 중 넷이 9라운드 수정이 만든 것이었는데, 9라운드가 자기
수정을 "구분자를 바꿔 개행에 안전해졌다" 라고만 적어 두는 바람에 그것이 *경계를 옮긴
것*인지 *없앤 것*인지는 아무 데도 남지 않았다. 그 한 줄이 있었으면 `\x1e` 는 10라운드
발견이 아니라 9라운드 안에서 끝났을 것이다. 뒤늦게 쓰면 무엇이 나왔는지는 적을 수
있어도 **무엇을 놓칠 뻔했는지**는 이미 사라진 뒤다.

한 라운드가 끝나면 **다섯 자리**를 채운다.

1. **`## 라운드별 상세` 에 그 라운드의 절을 하나 만든다.** 라운드마다 절 하나다 —
   다른 라운드의 절에 행을 얹지 않는다. 절은 네 부분이다.
   - **머리줄**: 반영 커밋(해시 + author date, **시각까지**) · 그 라운드에 붙은 사람
     수 · **시험 개수 변화**(예: `161 → 181`). 아직 커밋 전이면 `(미커밋)` 이라고 적어
     둔 뒤 커밋할 때 채운다. 시험 개수는 실제로 돌려서 잰다.
   - **관점 문단**: 무엇을 봤고 **왜 그 관점이었나.** 앞 라운드와 다른 관점을 골랐다면
     왜 골랐는지가 다음 라운드의 재료다.
   - **발견 표**: 그 라운드 발견만. 열은 `# · 발견 · 원인 · 수정 · 검증 결과 · 회귀
     여부 · 회귀 시험` 일곱이다. **번호(`#`)는 통번호를 이어 쓴다** — 절이 갈려도
     번호 계열은 하나이고, 각주와 `## 남은 버그` 가 그 번호를 가리킨다.
   - **`직전 수정이 만든 것` 문단**: 이 문서에서 가장 값나가는 자리다. 제 손으로 만든
     것을 제 손으로 적는 자리이고, 비워 두고 싶으면 그때가 바로 한 번 더 볼 때다.
     확인된 것이 없으면 그렇게 적는다 — 지어내지 않는다.

   **표 칸 채우기에서 다시 못박을 것 넷:**
   - **칸은 한 문장, 140자 이내다.** 길게 쓸 것이 있으면 `### 각주` 에 `[^n]` 으로
     빼거나 절의 문단으로 내린다. 한 칸에 문단을 넣으면 그것은 더 이상 표가 아니다 —
     15라운드까지 그랬고, 읽는 사람이 특정 발견을 찾거나 상태를 훑을 수가 없었다.
     `docs: every row of the REVIEW-LOG tables has the shape its header declares`
     가 이것을 실제로 잰다: 열 수가 어긋나도, 빈 칸이 있어도, 140자를 넘겨도 빨강이다.
     칸 안에 `|` 를 쓸 때는 `\|` 로 이스케이프한다.
   - **`수정` 칸 끝에 `[없앰]`/`[옮김]`** 을 붙인다. 경계를 없앤 것과 옮긴 것을
     구분하지 않으면, 옮긴 것은 다음 라운드의 발견으로 되돌아온다. **그리고 `[없앰]`
     이라고 적기 전에 정말 없앴는지 코드를 열어 본다** — #33·#43 이 `[없앰]` 인 채로
     네 라운드를 지나왔고, 실제로는 절반만 고쳐져 있었다(`## 남은 버그` 의 `B42`).
   - **`회귀 여부` 는 두 가지를 함께** 적는다 — 이 수정이 회귀를 만들었나, 그리고
     되돌리면 빨강인가. `반증 안 됨`·`미확인` 은 그 자체로 미완이고, **그 행들이 다음
     라운드의 작업 목록이다.** 빈칸으로 두지 않는다.
   - **재현하지 않고 옮겨 적은 수치에는 `옮겨 적음(출처: …)`** 을 붙인다. 이 문서가
     네 번 틀린 자리가 전부 '좁게 재고 넓게 쓴' 자리다.

2. **`## 라운드 요약` 에 한 줄.** 각 칸 한 문장이다. `발견 수` 는 그 라운드 절의 표를
   세면 된다. `시험 수` 는 라운드 시작과 끝의 실측값이다. 여러 갈래가 동시에 고쳤는데
   한 사람만 이 문서를 쓸 수 있었으면 **그 사실을 칸에 적는다** — 남의 발견을 대신
   옮겨 적으면 이 문서가 네 번 틀렸던 바로 그 모양이 된다.

3. **`## 남은 버그` 를 갱신한다.** 이것이 16라운드에 새로 생긴 자리이고, **다음
   라운드가 여는 첫 절**이다. 세 가지를 한다.
   - 이번 라운드에 **고친 항목을 지운다.** 지울 때는 그것을 지키는 회귀 시험 이름을
     그 라운드 절의 표에 함께 남긴다 — 시험 없이 지우면 다음 라운드에 되살아난다.
   - 이번 라운드가 **못 고쳤거나 안 고친 것**을 새 행으로 넣는다. 수정자마다 라운드가
     끝날 때 '못 고친 것 · 안 고친 것 · 이번 수정이 만들었을 수 있는 것' 을 신고하게
     하고, 그것을 그대로 옮긴다. `왜 남았나` 칸에 **소유자가 달라서 못 건드렸다** 같은
     사정을 적는 것이 중요하다 — 그게 다음 라운드의 갈래 나누기를 정한다.
   - 번호는 `B` 계열로 이어 쓰고, **지운 번호를 재사용하지 않는다.**

4. **`## 되풀이된 것` 에 이번 것이 이미 있는 항목의 반복인지 본다.** 반복이면 사례를
   붙이고, 새 모양이면 이름을 붙여 항목을 만든다. 세 번 이상 나온 것은 굵게 이름을
   준다. **긴 서술은 여기와 각주가 자리다** — 표의 칸이 아니다.

5. **`## 지금 있는 것` 의 숫자와 설명을 실제와 맞춘다.** 시험 개수(총계 + 파일별 +
   도커 추가분), 검증이 재는 것, 검증 결과를 읽는 경로. **숫자는 실측한다** — 빈 임시
   디렉터리에서 `env -u ERD_DB -u ERD_PSQL -u ERD_LABEL python3 scripts/selftest.py`
   를 돌리고 마지막 줄을 읽는다. 그리고 **언제 잰 것인지**를 함께 적는다.

**케이스 수를 바꿨으면 다섯 자리를 함께 고친다.** `REVIEW-LOG.md` 의 `**N개 항목**`
하나와 `INSTALL.md`·`INSTALL.ko.md`·`INSTALL.ja.md`·`INSTALL.es.md` 의 예시 출력
넷이다. 안 고치면 `docs: the case counts the documents state are the counts a run
produces` 가 빨강이다 — **그 빨강이 이 규칙을 지키게 하는 유일한 힘**이므로 케이스를
죽여서 조용하게 만들지 않는다. 그리고 `selftest.py` 의 `EXPECT_CASES`·`CASE_FLOOR`·
`TOTAL_FLOOR` 와 `selftest_kit.py` 의 `ENTRY_FLOOR` 도 같은 변경 안에서 함께 고친다.

**표의 날짜 칸은 그 라운드를 반영한 커밋의 author date 로 적는다 — 문서를 쓴 날이
아니다.** 여러 커밋에 걸쳤으면 첫~끝을 적는다. 하루에 열세 라운드가 몰린 적이 있으므로
**시각까지** 적는다 — 날짜만으로는 1~13라운드가 전부 같은 칸이 된다.

**"확인 필요" 는 라운드가 끝난 뒤에 남아 있으면 안 된다.** 14라운드가 라운드가 *끝나기
전에* 문서를 써서(위 첫 줄의 규칙을 스스로 어겼다) 세 자리를 비워 두었고, 그 셋은
다음 라운드가 실측해서야 채워졌다. 라운드 도중에 쓸 수밖에 없으면 비워 두되, **끝나는
자리에서 실측해 채우고 나서 낸다.** `docs: "확인 필요" never survives as a cell of the
REVIEW-LOG table` 이 표의 칸을 실제로 본다. 끝까지 못 채운 것은 표의 칸이 아니라
**`## 남은 버그` 에 행으로** 내린다 — `확인 필요` 는 오래 두면 확인된 것처럼 읽히지만,
`## 남은 버그` 의 행은 다음 사람이 여는 첫 자리다.

**라운드가 끝났다는 것은 독립 검증이 끝났다는 뜻이다.** 14·15라운드에서 값이 나온
것이 정확히 그 단계였고(과장 6·거짓 2, 우회 9), 16라운드에는 그 단계가 없어서 이
문서의 16라운드 절이 통째로 신고서로 남았다(`## 남은 버그` 의 `B0`). **주장하는 자와
뮤턴트를 만드는 자는 다른 사람이어야 한다.** 자기가 쓴 시험을 자기가 만든 뮤턴트로
재면 안 나오는 각도가 있고, 그것이 이 문서가 여섯 라운드째 적어 온
**"재는 쪽은 아무도 재 주지 않는다"** 다.

문서와 코드가 어긋나면 문서가 틀린 것이다. 고치고 넘어간다.
